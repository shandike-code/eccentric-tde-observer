# 已算两个方向的凸二维筛选 v1

源77577 x/y、77843 q/v、77927 z/k；77954差额审计作为前置证据。u=(1-alpha-beta)x+alpha*q+beta*z，alpha,beta>=0且alpha+beta<=1。r=y-x，d1=(v-q)-r，d2=(k-z)-r；目标为S0+2*b·theta+theta^T G theta，S0=sum r²，bi=sum r*di，Gij=sum di*dj。标量组合预测，不是新算子计算。固定物质20/原r20/物理dt，禁止改频率权重或核。

最多6遍全域（第1遍Gram及六端点频率通量，后5遍候选全点检验），每遍均包括所有9632组32方向4096深度。默认4CPU16GiB1h，父6GiB守卫，最多4096约束，0map/反馈/新候选dat/接受。哈希校验读取另计。

约束在凸三角形上逐边解析二次最小化；Gram退化/非正定相对1e-12则记未解决。每轮取0.99倍小问题最优系数，完整扫描实际浮点强度组合及全部点缺陷；每32组对正负两侧各保留一个超阈最坏点，转为标准化线性约束(-sign*d1/M,-sign*d2/M)·theta>=sign*r/M-1。保留hex原值和三维index；原点始终可行。最多4096约束，重复无新增即停止，不强行放松。若达到扫描预算，结果未接受。

最终必须当前候选真实全场扫描通过，不能以优化器返回成功代替：非零凸系数、全点非负有限、Linf/原M<=1.0000000001、流式与二次型L2比均<=.8、affine组合差/原M<1e-7、strict residual<1e-4。全/半系数预测边界L1/bol<1e-3且不大于原值*1.0000000001；通量取六端点的同权重组合并保留原频率数组，可独立复算。核外输入逐位相等，完全相同的三场直接复制避免次正规尾额外舍入；输出可以变化。所有点均参与，不删弱尾。

若全点Linf通过但成本/边界失败即保留候选拒绝，不宣称整个二维域无解（未联合求边界约束最优）；也不以0.99安全系数失败宣称无安全裕量最优失败。full_constrained_optimum_proven始终false。若通过，只具备独立审计资格，仍须后续真实full/half映射；已有半步仿射样本不是整个二维域严格误差界。

每阶段保留Gram、progress、prediction、来源与源码SHA、环境、调度证据及包；失败保全不重试。旧协议/大场字节保持。预算内未找到候选不推物理模型无解。
