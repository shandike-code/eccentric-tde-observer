# 已确认第20步后的固定1/128试探

## 输入与依据

76727于2026-09-24 22:48:28完成，83:28、COMPLETED/0:0。Mac独立审计4021文件、456反馈与608map回执通过；基态对照及两轮确认均过，六端点gas为正。独立接受记录`20260924-accepted-step20.json`接受的是confirm2 trial，不是响应目标。

定义$x_{20}$为该已接受编码物质，$r_{20}$为76727 confirm2 final响应残差，三范数8.172006557889262/0.2619104204778557/2.705001116611826。下一候选固定为：

$$
x_{21}^{\rm trial}=x_{20}+\frac{r_{20}}{128}.
$$

这是有限非线性步，不推进物理时间。物理旧层、phase1367、dt889.419892762322秒、密度、能量定义及率/转移核不变。基态和残差分母明确更新至$x_{20}$和$r_{20}$。沿用已确认1/128幅度，不因某范数下降就放大至此前失败的1/64。

## 有界执行

新run `outputs/hpc/common-step21-20260924`，新驱动`operations/common_outer_step21.py/.sbatch`。从76727 confirm2最新mapped后继起跑，同一候选最多8map，第4/8张各至多1pair，共最多2pairs。每对输入inner1e-4、两个boundary1e-3门通过才反馈。每对的两输入和后继分别保留，第4张失败不追认，第4张支持也仍执行预定第8张。

第8张原16门与对76727 confirm2两个端点四组合的三范数收缩全部通过，才`candidate_supported_requires_review`；计数20/new0，不自动接受21。之后仍需更新基态对照、两轮同态确认及Mac审计。物理域、完整态、资源或代码失败立即停，域失败也保存summary及complete归档。预算用尽未过则停止，不自动加map或折半。

heat1e-3、noise0.1、trust相对温度0.5/能量0.25/布居0.05不变。禁floor、clip、nan_to_num、删失败层、后归一化、缩短物理dt。

## 身份和资源

allocation内核接受记录/审计归档和实际源清单，重算confirm2两个物质响应并逐位核保存向量。基态取已接受trial本身；候选初始化前写入。exact_trial检查encoded/base/r/direction/alpha、T/H/He/比能/物理旧层，native镜像预检通过才declaration/maps。旧1/64检查器冻结，新入口沿用已经测试的1/128路径，原方程不变。

cpu_long32CPU、128GiB、16worker、BLAS1、hugepage0、4h。最多9新dat，入场12STATE_BYTES空闲；每worker native和/proc小于6GiB，每反馈态累计worker批墙钟小于900秒。USR1当前batch提交后停；只读watcher保存调度器终态，不自动重交。代码/输入/配置/环境/job/回执存档，旧大态不改不删。

## 解释边界

第20步两确认final残差三范数均略回升；两向量差0.03141504142/0.001780827090/0.01120942230，control pair漂移0.01581889311/0.000882676576/0.004934506191，都是有限间隔漂移而非严格误差界。第二确认最大单元四组合最不利比0.998198626215，改善约0.18%，尚无快速收敛证据。不能把有限步通过称为耦合柱、频窗截断收敛、完整径向/相位表或整盘涌现强度，也不能断言模型无解。预计本批1–2小时只是近期成本范围。
