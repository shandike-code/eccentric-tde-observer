# 第21步：正性约束双系数候选真实映射与条件反馈 v1

## 来源与目的

77102完成于2026-09-25 14:10:51，Mac核验6个文件、683代码声明及所有新增约束。
control/thermal/population分别用了3/3/2轮全场扫描，均找到非负候选。
预测残差与此前最后一次真实残差的比值依次为0.980458868751369、0.9725097569529314、0.975764149996243。
这些只是固定物质的辐射预测，不是新的物质接受。20步保持不变。

只使用经审77102末轮的有效全局系数：

| case | u | v |
|---|---:|---:|
| control | -5.502252447101244 | 0.44724228959376544 |
| thermal | -8.045775330593539 | 0.7368322658967155 |
| population | -7.000621134139923 | 0.6144693479691827 |

每case四态来自76957的同一物质trial连续历史，不跨case组合：

$$
q=x_{2}+u(x_{1}-x_{2})+v(x_{0}-x_{2}),\qquad
p=x_{3}+u(x_{2}-x_{3})+v(x_{1}-x_{3}).
$$

写入$q$时沿用扫描的float64运算顺序，逐点检查$q,p$非负和有限。
新候选以partial文件写完后原子发布，不覆盖已有文件；源态先后SHA不变。

## 一次完整且有界的批次

1. 核对77102、76957独立审计和归档；四态血缘、原trial、$x_{20}$、$r_{20}$及物理旧层逐项核对。
2. 每case先写候选并做一个真实$T(q)$，总计3张。初始化前先复制正确trial，核编码/基态/方向/残差/步长，禁止迁移为默认0.0625候选。
3. 全602片比较真实$T(q)$与预测$p$，同时逐位核对存储$q$和公式；保存全场及局部误差、L2误差和真实残差。
4. 真实残差须小于该case旧值的0.99倍、严格小于$10^{-4}$；边界谱/总通量小于$10^{-3}$；预测误差/场小于$10^{-7}$且预测误差/真实变化小于0.1；资源门、有限/非负均保持。
5. 三个首张验证结束后，只有control验证通过才再做control两张map及相邻反馈。零控制须稳定，才能对通过真实验证的thermal/population各做两张及一个完整反馈对。
6. 任一物理响应域失败即停止后续case；某候选首张验证失败就跳过其反馈。程序/身份/资源错误立即归档停止。没有自动续交或物质promotion。

最多9张真实map、3个反馈对；每case最多3张。若任何门失败，保存失败本身，不加图、不换门。
三种物质残差范数、原16门和原$r_{20}$比较必须保留，同时计算新control的全部4种端点配对。
新control不能替换原$r_{20}$使候选过门。固定phase1367、物理dt889.419892762322秒、rho与旧时间层。
禁止clip/floor/nan_to_num、删弱尾、后归一化、改变气体能量定义或缩短物理dt。

## 执行及审计边界

新目录`outputs/hpc/step21-positive-plane-validation-20260925`。
`qos_stu_cpu_long`，32CPU、128GiB，16 worker，BLAS1、hugepage0；5小时硬上限、USR1提前900秒。
父进程/各worker峰RSS小于6GiB；完整76块receipt检查；至少预留24个辐射态的空闲空间。
每case首张验证、反馈完成及终态都生成小工件归档；大型dat留学校，不删除历史。
信号保存中间产物，但本驱动不原目录盲重试。最多9张map是上限，不是要求凑数。

Mac后续必须核验归档、输入声明、每块资源、候选系数/四态身份、全场误差汇总、原物质门、新control比较及Slurm终态。
本批次即使成功也只支持固定物质辐射验证和第21步候选判定，不证明耦合柱、全相位/环带或整盘观测强度已完成。
