# 已确认第19步后的固定1/128试探

## 输入与决策

76554于2026-09-24 18:02:00完成，84分54秒、COMPLETED/0:0。Mac独立审计4021文件、456反馈及608map进程回执，基态对照及两轮确认全部通过。正式接受记录`handoff/evidence/20260924-accepted-step19.json`接受confirm2的trial，绝不将响应目标作为基态。

令$x_{19}$为该已接受编码物质，$r_{19}$为76554 confirm2 final响应残差，三范数为8.33020939467705、0.268477524599201、2.709653378977141。下一候选固定为：

$$
x_{20}^{\rm trial}=x_{19}+\frac{r_{19}}{128}.
$$

这只是非线性试探，不推进物理时间。物理旧层、phase1367、889.419892762322秒、密度、能量定义及全部率/转移核冻结。沿用已确认1/128幅度，不依据单次收缩擅自扩大。基态和原残差分母明确更新至$x_{19}$和$r_{19}$，新候选必须重新求辐射与反馈。

## 有界批次

新run `outputs/hpc/common-step20-20260924`，新驱动`operations/common_outer_step20.py/.sbatch`。从76554 confirm2最新mapped后继起跑，固定一个候选最多8map，第4/8张各至多1对反馈，最多2pairs。每对输入inner小于1e-4、两个boundary门小于1e-3才反馈；分别保留两输入及后继。第4张即使支持也执行已声明第8张，第4张失败不追认。

第8张原16门及对76554 confirm2两反馈端点四组合的三范数全收缩，才标记`candidate_supported_requires_review`；counter保持19/new0，不自动接受20。之后仍需新基态对照、两轮同态确认及Mac审计。域/完整态/资源/代码失败立即停止，域失败也写summary和complete归档；预算耗尽未过则停止，不自动折半或续跑。

heat1e-3、noise0.1、trust相对温度0.5/物质能量增量0.25/布居0.05不变。禁止floor、clip、nan_to_num、删失败层、后归一化或缩短物理dt。

## 身份、资源与可恢复证据

allocation内验证接受记录、归档与实际源清单；重求confirm2两个物质响应并逐位核保存向量。基态是已接受trial本身，trial在初始化之前写入；exact_trial检查encoded/base/residual/direction/alpha、T/H/He/比能及物理层，native镜像预检通过后才开始map。旧1/64检查器保持冻结，新入口复用已验证的1/128检查。

cpu_long32CPU、128GiB、16worker，BLAS1、hugepage0、4h上限。最多9新dat、入场12STATE_BYTES空闲；worker原生及/proc小于6GiB，每反馈态累计worker批墙钟小于900秒。USR1当前batch提交后停止。记录代码/配置/环境/来源/job/进程回执，旧dat及旧实现不动。只读watcher保存调度器终态，不取消或自动重交。

## 尚未建立的结论

第19步两轮确认final残差范数均略回升；向量差三范数0.03328527525/0.001835873554/0.01182698024只是有限间隔漂移，不是严格误差界。最差单元相对新control最不利比0.995980376757，改善约0.4%，不存在快速收敛证据。有限步通过不是耦合柱、频窗截断收敛、完整径向/轨道覆盖、涌现角分辨强度或整盘观测谱，也不证明此模型无解。预计本批1–2小时仅依据近期实际成本，不外推全项目完成日期。
