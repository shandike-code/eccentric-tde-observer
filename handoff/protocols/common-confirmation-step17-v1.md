# 第17步候选的基态与同态确认

76204的map04原12/16门，map08原16/16且相对x16两端点四组合三范数全过。Mac审计3087文件、304反馈worker、608map worker重现两次判决，物质响应均在正气体热能域。仍计16个接受步，本协议仅确认x17，不产生x18、不推进物理时间。

采用已验证4+2+2结构：control为x16零位移基态，追加4map并完整评估两态；成功后confirm1为固定x17追加2map与反馈，成功后confirm2再2map与反馈。最多8map、3对反馈、4小时，32CPU128GiB16worker、BLAS1、hugepage0。失败不派发下一阶段，保留归档，无自动重交/扩预算。

control的辐射种子来自common-confirmation-20260923/confirm2已确认x16的最新后继；物质来自common-step17-20260923/inputs/outer_base_material.npz，alpha0，r16元数据。两者encoded、温度/H/He/比能、rho/phase/dt须逐位一致。输入目录只写trial/config，不伪造一个已经完成映射的零控制state。confirm1来自step17独立endpoints-map08/mapped_final，trial/base/r/direction/alpha1/64不变；confirm2用confirm1实际后继，新child保存三槽，前child不再改写。

零控制不调用有限步接受；需内层1e-4、边界1e-3、五反馈稳定门1e-3、正物质响应和全态源/资源门。其后只比较此前有支持的pair08与新基态的四组合三范数比<1；pair04已经被拒，不作为确认前置、不抹去历史失败。两轮新确认各需原16门和四组合三范数比<1。原16门的残差分母仍为冻结r16，新基态是额外约束，不偷偷换分母。

prepare核源作业COMPLETED、Mac审计归档SHA及候选/参考输入的清单大小SHA。trial初始化前写好，Linux codec/native物质与镜像/相位/时间严格检查。保持phase1367、dt889.419892762322s、物理旧层与能量定义；trust温度.5/能量.25/布居.05；反馈态900s、每worker原生/proc<6GiB。源码和协议声明冻结，停止信号完成当前批次后停。postcheck沿用common_outer_step17的接受数16版本，不修改旧历史。

终态confirmed_requires_mac_review仍不自动计17。需要独立复核和判断同态漂移之后再决定；有限步确认不证明耦合柱、频域截断或整盘I_nu已经可靠。
