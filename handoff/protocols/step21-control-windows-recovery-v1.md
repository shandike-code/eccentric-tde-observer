# 77264 首反馈前授权构造故障的有界恢复

故障发生在第8张map之后、反馈之前。原驱动使用禁止接受的zero-control协议作模板，
但common_native_feedback.new_protocol的构造契约要求有限试步授权，故其guard正确拒绝。
旧驱动/协议/失败状态和所有dat保留，新入口recover_step21_control_windows.py在独立目录运行。

输入必须匹配Mac失败审计1228files/608map回执、原8map、无active_map/无反馈、3个已保留端点。
构造使用已审77126 thermal有限模板，仅为满足未修改的通用构造器契约；要求它与zero模板的原接受门、
formal门、x20/r20/物理旧层声明相同。最终协议写盘/执行前必须换回control trial并关闭全部物质接受授权，
设置alpha0、diagnostic_only、physical_time_advanced=False。不是把失败控制授权打开来绕过guard。
真实协议预构造及native物质身份检查提前到初始化/任何新map之前，测试必须复现原故障再验证修正路径。

run=outputs/hpc/step21-control-windows-recovery-20260925。
先从旧run map8 mapped_final初始化新旋转槽；原trial在初始化前复制。
随后逐SHA复制小型8map回执与保留端点清单，并只继承连续history，不复用旧旋转槽路径作为新槽。
新声明分别记录原seed、resume_seed、inherited_map_count8、maximum_new_maps8、源失败审计及逐文件来源。
第8张后的原保留端点直接用于补算pair08，原dat只读；必须结算反馈且通过原零控制7门，才允许map9–16。
不要求新窗口漂移门在pair08通过才继续，因为原实验就注册了两窗口趋势测量；物理/原反馈/资源失败仍立即停。
第16张后再保留新的三态和pair16。总实验仍16maps/2pairs，新计算严格8maps，0物质接受/0基准替换。

原x20/r20/phase1367/dt889.419892762322s/rho、全部物理/接受门和窗口比值定义不变。
32CPU/128GiB、16worker、BLAS1、hugepage0；4小时上限，USR1提前900秒；磁盘预留8完整态。
worker/parent各6GiB门、76块/全9632频率不变。源与代码SHA前后核查，每反馈对和终态归档。
断信号保留新目录，不盲目原目录重试。独立审计必须核源8map与新8map的归属，不能声称本次新算16张。
