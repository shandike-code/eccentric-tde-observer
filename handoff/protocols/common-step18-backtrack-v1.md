# x18质量加权收缩失败后的有限回溯

## 前提与目标

76405完成8map、2对反馈，均15/16，仅质量加权收缩失败；四组合质量加权比约1.156–1.164，热能域有效、加热与内层噪声门通过。Mac逐块、完整态、能量账本、源与编码审计已复现失败。后4map未改善该质量加权问题。因此停止同一个1/64候选的加map路线，而非放宽门或接受18。

本批先刷新固定基态x17，再依次尝试x17+r17/128、必要时x17+r17/256。x17是已接受trial，r17仍取76260 confirm2 final；基态控制的新响应只用于独立四组合比较，不替换固定方向或原门分母。物理旧层、rho、phase1367、dt889.419892762322秒保持不动。不承诺减小步长必定改善；两次都失败就停止并要求方向诊断。

## 严格预算及条件

control 4map及1对基态反馈：零位移原稳定性门、内层门、物质域与完整态/资源门通过才继续。不给零位移套用有限步接受。记录其相对r17的漂移和已有两对1/64失败态对新基态的四组合，不追认已拒绝态。

half 1/128最多8map，第4/8张在内层过门时各做完整反馈。两端点都通过原16门和对新基态四组合三范数时立即停止，标记half_supported_requires_review，不再quarter、不自动接受18。第4张未够精度可完成该候选既定8张，保留早期判决；第8张内层/加热/噪声/物质域未解决就停止本批。

只有half第8张已实际完成反馈、原16门中只有收缩门失败（或原门全过但新基态比较不收缩）、完整态与物质域有效时才运行quarter。quarter同样最多8map和2对反馈；没有1/512或第三个候选。总上限20map、5pairs、32CPU128GiB16worker、4h。物理域/资源/代码故障即停，不靠减小dt、裁剪、floor、删点、nan_to_num或后归一化。

## 身份和算子

原common_feedback_bridge固定1/64检查保持不变。本独立驱动只允许明确声明的0、1/128、1/256，对encoded/base/r/direction/alpha、原物理层与rho/phase/dt、温度/H/He/比能解码逐位验证，并保留原trust温度0.5/能量0.25/布居0.05。初始化前保存每个trial及config，allocation内适配原native模板并逐位核T/H/He/rho镜像及时间。原反馈worker和共同频域算子不变，alpha的变化只改变物质数值位移。

基态种子来自已接受x17的最新后继；half与quarter均从新control的同一最新后继开始，不能把half物质误带到quarter。每对实际反馈保留两个输入及mapped_final独立dat；旧run和冻结代码不改。预计最多24份新dat，准备时要求28份STATE_BYTES的空闲空间以留余量。每worker原生/proc<6GiB，新反馈态累计worker批墙钟<900秒，BLAS1、hugepage0。USR1当前batch收尾后停止。所有来源由已审归档及SHA锚定，协议/完整依赖固定SHA。

新run outputs/hpc/common-step18-backtrack-20260924。候选支持后必须另行Mac审阅和同态确认；所有路径仍接受数17、new_material_steps=0。质量范数的log热能分量不是物理能量份额，有限步支持不是代表柱耦合、更不是整盘I_nu。
