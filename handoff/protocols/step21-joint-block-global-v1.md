# 两组三块联合方向的真实全频检验

先决证据：77817局部384组核心试验，经Mac数组哈希与逐频归约审核通过。沿用77577 map10原输入x及T(x)，用77817保存的两个candidate覆盖23..25、47..49共6个自然块；其余块逐位复制x。全候选q和半候选h=(x+q)/2写入新run，未选块半态直接复制原字节，保留次正规尾。原x20/r20/物理旧层/dt/网格/率核全部不变，不改物理模型。

new run outputs/hpc/step21-joint-block-global-20260926。32CPU128GiB、16worker、4小时硬限。全局仍按原76个128组自然块运行原算子（局部384组只用于造修正），worker/parent回到原6GiB守卫，不沿用局部16GiB。来源、trial初始化前写入及encoded/native身份、全代码SHA前后审计。

先真实T(q)、T(h)共2map，全点有限非负、未选输入逐位不变。按原缺陷尺度，full L2<=.999，full Linf/half L2/half Linf<=1.0000000001，半步仿射误差/原L2<=1e-6；严格辐射<1e-4，边界L1/bol<1e-3且不高于原值*1.0000000001。对外侧22/26/46/50及全部76块同等检查，不删除弱尾。

任何门失败即停于2map，不做反馈；通过才full map2与pair02，原七门/物理域过才full maps3..10与pair10。最多11map(10full+1half)、2轮反馈、0新接受/0物理时间推进/0r20替换。pair02对77577是初始化位移，pair10对pair02的八map漂移四组合三范数/r20均<.001才窗口稳定。即便全过也不是耦合大气收敛或整盘I_nu。

USR1提交当前batch后不派新worker，错误保全不重试。每阶段小工件包与清单用于Mac审计；局部阵列/大dat不进Git。禁止放松失败门、继续无限微步或直接把局部GMRES视为收敛。
