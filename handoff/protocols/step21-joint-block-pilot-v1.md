# 联合相邻三块的有界局部试验

77790短步真实映射12门通过、两轮原七门通过，但八map窗口L2/质量加权未过；不重复同方向微步。
新run outputs/hpc/step21-joint-block-pilot-20260926。选核心23..25、47..49（半开group区间2944:3328、6016:6400），把已观察到的邻块反馈纳入同一局部修正。

输入特意保持77577 map10输入/输出，与77648一致，以隔离扩大核心的作用；不是误用77790最新态。物质x20、r20、旧时间层、dt、频率控制体、率核全不变。原数据/代码哈希前后核对，trial encoded/native/物理身份检查。joint_frequency_block只把原全局active区间裁为目标段，沿用原精确Doppler halo planner；没有重采样、合并频率组或改物理边界。

每组先重放原一次源映射：field相对差<=1e-12且defect相对差<=1e-6；否则停止，不运行该组Krylov。成功才16次GMRES（restart8、2cycles、rtol1e-5，允许未完成但不称线性收敛）。沿原修正方向取全点非负步长再最小化局部L2；不裁剪。另做独立半步原算子检查：仿射差/原L2<=1e-6。最多4次非Krylov局部映射每组，Krylov内部算子调用另计。

候选须局部L2/Linf均减半、边界谱绝对变化不增加、有限非负、半步检验通过、16次迭代以内、时间不超过24倍该联合块一次map（新增独立半步，给16次Krylov及4次实际映射留有固定开销余量）。每worker<16GiB（核心扩大为三倍，独立新资源门，不改旧6GiB门）；外层2worker，各30分钟硬限，32CPU128GiB cpu_long allocation，2小时Slurm上限，BLAS单线程。该试验并行度受两组和内存限制，不声称32倍加速。

0全频map、0反馈、0物质接受、0全局candidate，最多保存两份联合局部NPZ。局部通过仍可能把误差推向22/26/46/50块，必须下一阶段真实全频+half检验才有全球意义；不得自动提交该阶段。任意失败保全，不盲重试，不改门。后续报告核验真实分辨率replay、GMRES未收敛标志、资源、局部范数、半步误差与边界。
