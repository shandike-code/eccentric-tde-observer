# 五块联合核心试验（独立新协议）

78032两轮原七门和正热能通过，但八map质量加权漂移0.003601680527未过0.001。热log占质量平方99.982844%，主要频率贡献含旧核心外22/46/50。此证据支持扩大局部数值支撑试验，不证明其必然解决反馈。

固定77577的map10原输入/原输出，与77817三块试验一致，隔离支撑宽度；不采用新端点另改变第二个因素。物质x20/r20、dt889.419892762322s、物理率核、频率网格和外halo保持。新核心22..26及46..50，半开组区间2816:3456、5888:6528，各640组。joint_frequency_block沿原精确Doppler halo规划，不合并组、不改变物理边界。

原map重放须逐位一致，并保留原field<=1e-12和defect<=1e-6尺度核验；失败停止该组，不进入Krylov。通过才最多16GMRES迭代（restart8/cycles2/rtol1e-5）；info非零允许保存有限方向，不称线性收敛。原方向取全点非负步长，局部L2线搜索，原算子计算候选及独立半步。最多4次非Krylov局部映射，Krylov内部调用另计。局部L2/Linf均减半，边界绝对变化不增，half仿射误差/原L2<=1e-6，有限非负；不裁剪或改能量定义。

资源：32CPU128GiB/2h/2worker，BLAS单线程；并行限于两组，不声称32倍加速。三块实测峰约9.1GiB，按640/384线性估计约15.2GiB；halo与临时数组使其仅为估计。故新worker守卫24GiB，两个worker最多48GiB，留其余给父进程和系统；保留总耗时<=24次本组rawmap科学成本门。原三块最长1302s，五块线性估计2170s；新每worker绝对3600s硬限，达到即杀进程组，不加预算。该资源门仅适用新试验，不改旧16GiB/1800s协议。父wrapper传USR1/TERM，worker/replay/source_map均查停止标志，relay进程组整体终止。

run outputs/hpc/step21-five-block-pilot-20260927；只保存两份local NPZ（每份约1.25GiB），学校保留，不自动回传Mac。0全局map/0物质反馈/0新接受/0全局candidate。局部门通过不自动触发全域运算；需独立审计、成本判断与另定全域协议。外侧21/27/45/51仍可能响应放大。源代码及声明前后哈希，trial全字段/native身份检查；备份pre-five-block-pilot.bundle，旧代码不改。失败保全、不盲重试。
