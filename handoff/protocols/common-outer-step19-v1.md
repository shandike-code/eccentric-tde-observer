# 已确认第18步后的固定1/128试探

## 输入和依据

76469在2026-09-24 14:49:47 COMPLETED/0:0，1:19:38。新基态和两轮同态确认都通过原门与四组合；Mac审计4021文件、456反馈与608map回执，六端点气体热能均正。独立记录`20260924-accepted-step18.json`接受的是confirm2的trial，不是响应目标。

将已接受编码物质记为$x_{18}$，将76469 confirm2 final保存并在allocation内重算核对的物质响应残差记为$r_{18}$。下一候选仅为：

$$
x_{19}^{\rm trial}=x_{18}+\frac{1}{128}r_{18}.
$$

这是非线性试探，不推进物理时间。物理旧时间层、phase1367、889.419892762322秒、密度、能量定义和全部率/转移核不变。步幅选择沿用刚确认的1/128；此前1/64质量范数失败，不重新无依据扩大。三种范数均要收缩，不能仅挑质量范数。

## 执行合同

新目录`outputs/hpc/common-step19-20260924`，从76469 confirm2最新mapped后继开始；固定一个候选最多8maps，第4/8张各至多一对完整反馈，最多2pairs。两个输入的内层门1e-4、边界门1e-3通过才反馈。第4张结果和第8张结果分别保存；早期失败不追认，第8张原16门及对76469 confirm2两端点的四组合全部收缩才标候选支持。支持后仍需要新的基态/同态确认和Mac审阅，不在本批接受19。

physical domain/完整态/资源/代码失败即停止；第8张任何门未过则预算结束，不自动折半/追加map。沿用noise0.1、heat1e-3、trust温度0.5/能量0.25/布居0.05；禁裁剪、floor、nan_to_num、删失败层、后归一化、减物理dt。

## 身份和资源

在allocation中核归档SHA及全部已接受来源、重放confirm2两响应并与保存向量逐位相同。新基态等于已接受trial，残差分母明确更新为$r_{18}$；trial在初始化前落盘。用已验证exact_trial核encoded/base/residual/direction/alpha、物理旧层与T/H/He/比能解码，真实native镜像预检通过才开始map。原固定1/64的bridge检查保留，新的1/128入口不调用该旧限制；微物理和共同频域算子未修改。

cpu_long 32CPU、128GiB、16worker，BLAS1、hugepage0，4h上限。最多9新dat、入场要求12STATE_BYTES空闲；每worker原生及/proc小于6GiB，新反馈态累计worker批墙钟小于900秒。USR1在当前batch提交后停止。每对两输入及mapped后继独立保留；源/代码/协议/environment/job/回执冻结，旧结果与大dat不改不删。物理域拒绝也保存终态summary和complete小归档。

两个确认final的残差向量漂移三范数为0.05105163/0.00354415/0.01555270，并非严格误差界；所有残差范数略回升，因此不能宣称最终收敛或保证第19步通过。未得到整盘角分辨强度、真实谱线或频窗截断收敛。
