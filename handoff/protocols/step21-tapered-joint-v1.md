# 固定频率渐消方向：真实全域验证 v1

来源保持77577 map10原x/T(x)及77817局部核心candidate q（23..25、47..49两个384频率组核心）。77905已审：原统一步长即无安全裕量最佳预测L2比0.837545，不能过预注册20%成本门。该方向不继续。没有物理无解结论。

新方向在每核心j=0..383定义w_j=sin(pi*(j+0.5)/384)^2，z_j=(1-w_j)x_j+w_j q_j。两个核心同一固定形状，无自由拟合参数；核外逐位复制x，包括次正规数。半候选h=(x+z)/2，核外仍逐位复制。凸组合保持非负但禁止以裁剪或floor修复无效输入。频率、角度、深度网格和原算子未改，w仅用于数值初值修正，不是发射率或频谱加窗。

频率权重与邻频转移算子一般不交换：禁止将T(z)伪造为(1-w)T(x)+wT(q)。必须新算T(z)、T(h)。假设为平滑减弱核心边缘修正可以降低外侧22/26/46/50放大；未获证实，不事后修改w。

run outputs/hpc/step21-tapered-joint-20260926，32CPU128GiB、16worker、4小时硬限，原76自然块；parent及worker原6GiB门。先各1真实full/half map，检查全部9632组32方向4096深度，301个小分片归约以及全部76块。完整L2/旧L2<=0.8；完整Linf、半步L2/Linf均<=1.0000000001；独立半步仿射误差/旧L2<=1e-6；严格辐射<1e-4；两边界量<1e-3且不高于旧值*1.0000000001。保留全部弱尾和邻块。

失败停于两map，0反馈；通过才control map2+pair02，原七门与物理域通过才maps3..10+pair10。总硬限11map（10control+1half）、2反馈，无自动物质接受/基态替换/物理dt修改。pair02比较旧77577为初值位移，pair10对pair02的八map漂移按原r20四组合三范数均<.001才窗口稳定。即使全过也待独立审阅，不证明耦合大气/全定义域/整盘I_nu完成。

trial初始化前复制并做encoded/native身份检查；冻结源码及来源SHA，前后验证。USR1当前batch安全收尾，不盲重试；错误保全小包。备份pre-taper-validation.bundle；旧大态和历史协议不改。20%是数值成本筛选，不能替代物理收敛门。
