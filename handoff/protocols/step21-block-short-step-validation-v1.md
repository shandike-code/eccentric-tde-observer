# 77783受约束短步的真实全频验证

2026-09-26。新run outputs/hpc/step21-block-short-validation-20260926。
77783扫描完成并经Mac独立小工件审计，t=0.008802697738053965，Linf步长上界0.009780775264504405。
限制点[3200,0,2144]在邻块25；预测L2比0.9969689992745943，改善仅0.3031%，不是已证明加速。

使用77577 map10输入x、77648局部24/48块候选q，依77783固定系数构造新s=(1-t)x+tq，未选块逐位复制x。
第二候选h在两块为0.5*x+0.5*s，其余逐位复制。t从扫描结果/独立审计/原公式三重核对，不能临时调节。
固定x20/r20/物理旧层/dt/网格/率核。新trial在初始化前写好，encoded/native/物理相位身份全查。
原大场和局部NPZ、77783与77701来源、全部声明源码SHA前后核验，不改旧状态或门。

32CPU128GiB、16worker、4小时硬限。先实际全频T(s)、T(h)两map，按原R0范数检查：
全步L2<=.999，全步Linf、半步L2/Linf<=1.0000000001，半步仿射缺陷/原L2<=1e-6；
两map严格辐射<1e-4，边界L1/bol<1e-3且不超旧值*1.0000000001；全点有限非负、未选块输入不变、父/worker各RSS<6GiB。
额外核实际全步L2比与77783预测之相对差<=1e-6。预测是待检验命题，不用它替代原算子。

任何真实门失败立即停止，最多两map；全部过才full继续map2并pair02。
原七门和物理域过才maps3到10与pair10；四组合三范数/原r20均<.001才标窗口稳定。
全批最多11map、两对反馈，0接受、0时间推进、0r20替换。pair02对77577为初始化改变，不是八map漂移。
即便全过也只是有限窗口控制诊断，不是严格误差界、收敛耦合柱或整盘I_nu。

USR1后当前批提交不派下一批，失败保全，不盲重提。每阶段归档小包与SHA，Mac独立全slab/邻块/反馈审计。
短步收益很小，后续不能重复同方向无限微步；本次检验主要用于判明全域受约束方向能否兼顾反馈稳定性。
