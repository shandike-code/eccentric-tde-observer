# Step21：已验证外推之后的第二次只读正性扫描 v1

前提：77371完整归档独立审计，11map/2pair、接受步20、物理旧层不动；
两反馈原七门均过，pair11相对pair03八map三范数窗口仍未过0.001。
这次不重复旧六态组合，而检验外推后生成的新历史能否继续消除辐射慢模。

使用77371保留的map3、map11端点清单与全部11行连续历史，逐一核对实际映射对。
本文件中的$x_{k}$均为77371第k张map后的辐射态，与物质态编号无关。
两组输入索引为(1,9,10)、(2,9,10)，对应实算输出(2,10,11)、(3,10,11)。
不假设未保存的其它辐射态存在。所有trial/base/direction/r20/old身份仍完整验证。

复用`scan_step21_wide_plane.py`六态流式算法：

$$
q=X_{2}+u(X_{1}-X_{2})+v(X_{0}-X_{2}),\qquad
p=Y_{2}+u(Y_{1}-Y_{2})+v(Y_{0}-Y_{2}).
$$

最小化离散全数组无权残差L2的Gram二次型，不把此目标当物质收敛门。
两组各最多6遍全场正性检查、4096个切面；系数L1预算191，统一退回0.99，病态阈值1e10。
所有点，包括纯零段与弱尾，保持原数值，不clip/floor/删点/重归一化。
每片保留负值数量、局部尺度、最小值、边界量和六态hex负值见证。
预测q和p均须非负、有限，系数和误差<1e-12、L1<192，全局残差<1e-4，
相对本次源map11真实残差<0.99，边界L1/bol均<1e-3。
只有预测比严格<0.8才值得后续32CPU实算；20%为算力收益筛选，不改变任何科学门。

4CPU/16GiB/default QOS/1小时；BLAS1/hugepage0，进程RSS<6GiB。
零新map、零物质反馈、零大型候选写入、零接受，不推进物理时间。
全输入SHA在allocation内前后验证，旧代码和旧工件不改；失败归档后停止，不盲目续跑。
将源审计、终态、配对清单及代码声明冻结入新目录；预算不因发现潜在候选自动扩大。

完成后先独立核验来源、602片归约、切面见证和成本门。
若出现合格预测，另登记一份真实验证协议后才考虑32核任务；不直接套用77371硬编码来源的验证驱动。
若两组均无明显收益，停止此两基组重启策略，转向误差传播/求解器分析，不自动再扫描相近基组。
