# 已确认第16步之后的一次有界试探

前提：76075完成，Mac逐文件核4019个工件，456个反馈worker与608个map worker，六端点账本/源/资源/响应编码检查通过。基态稳定、两次确认原16门及相对新基态四组合三范数通过，独立记录20260923-accepted-step16.json据此记第16个有限非线性步；不是物理时间推进或耦合柱收敛。

新迭代基态为确认的trial encoded_state x16，不能用target_material替代。学校allocation内重放confirm2的两端点物质响应，残差须与已审保存值逐位相等。r16取final响应残差，base_encoded_state=x16、base_residual=finite_direction=r16，数值alpha=1/64，x17=x16+r16/64。温度/H/He/比能从原codec解码；不裁剪、不设floor、不改物理旧层、rho、phase1367或dt889.419892762322秒。原信赖域温度0.5、能量0.25、布居0.05；新基态和分母在新协议显式声明。

32CPU128GiB、16worker、BLAS1、hugepage0、4小时；最多8新map，追加第4/8张后各保留两个已测输入及最新后继的独立dat副本。只有两个输入满足原内层1e-4和边界1e-3门才实际计算该对反馈；否则继续到预定8张上限，不偷换输入态。最多2对反馈。原子率/独立共同域四力/完整态门及原16接受门不变，反馈态墙钟900秒、每worker原生/proc<6GiB。

新候选每对除了原16门，还相对已确认x16的previous/final残差作四组合三范数收缩比较。第4张对科学门未过但物理域仍有效，可以继续同一固定候选到第8张；非正气体热能、物质响应异常、资源/代码失败立即停止。第8张有支持则candidate_supported_requires_review，仍不自动记17；无支持为budget_complete_not_accepted。无自动扩预算、改alpha或重交。

源身份由Mac审计归档清单及SHA锚定，不只重新散列当前文件。trial在初始化前写入并逐位核对，初始化仍使用已验证child流程和native镜像身份审计。原档案不改，独立新run；USR1当前批次提交后停止。新postcheck复用common_native_feedback的检查体，仅将明确计数改为16，旧函数/旧判决字节不动。阶段工件归档不含dat，代码/协议/测试固定SHA并前后验。

当前同态残差仍漂移，连续过门不能提供严格误差界；后续需要真实耦合残差下降、精度/网格/频域截断检验，才讨论大气表和整盘I_nu。此任务仅解决下一有限迭代的可接受性。
