# 联合核心方向的全点最大缺陷约束与成本筛选

77843已审计：全步L2比0.4111277、Linf3.8530744；半步0.65157294、2.01151008。最大点落在新外侧50块，22/26/46也增；只有最大非增门失败。零反馈，源与完整大态保留。

新run outputs/hpc/step21-joint-line-cost-20260926。只读取77843原输入x、y=T(x)、六块候选q、v=T(q)，前三者路径与真实map输出SHA受前后保护。固定r=y-x、d=(v-q)-r和M=max|r|，逐全域所有点求|r+t*d|<=M、0<=t<=1的交集上界U。计算L2二次式S0+2t RD+t^2 DD，取t=0.9 min(-RD/DD,U,1)，无下降方向或U=0即无非零候选。

最多三遍全场：第一遍全301个32freq slab的S0/DD/RD与四个边界谱；第二遍全点Linf上界和精确hex限制点；第三遍逐点浮点组合检查有限非负、预测L2/Linf与边界。不写候选，不调用转移算子，不删除弱尾。

只读新成本门：预测以及第三遍L2比均<=0.8（至少20%改善），才标值得独立复核；原科学门保持不变，最大缺陷不增、严格辐射上界<1e-4、边界L1/bol<1e-3且不超原值。若成本门未过，不进入昂贵真实map，即使存在收益很小的非零步。另报告不取0.9裕量时该区间内最优L2比，用于界定这个固定方向的上限；不据此自动调步长。

默认4CPU16GiB、1小时硬限、父进程<6GiB，0新map/反馈/大候选/物质接受/物理时间推进/r20替换。完整SHA声明，USR1检查点停止；失败保全不盲重试。预测通过也须Mac独立审计和新真实算子验证，不能直接进入物质反馈。

该方向核心范围不同于77783单块方向，且成本门从历史0.1%改为新的20%；这是新试验的算力选择标准，不回写旧数值验收或旧判决。只否定/支持当前一维方向，不推断模型是否有自洽解。
