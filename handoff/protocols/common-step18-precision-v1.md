# x18固定物质内层精度诊断：4 + 条件4 map

76383以COMPLETED 0:0耗时35:32结束，预算8map完整；全部内层残差仍高于1e-4，未执行任何反馈。Mac核1233文件和608进程回执、源物质/种子身份及逐块汇总，残差1.740040899e-4单调降到1.021829335e-4，边界指标均低于1e-3且下降，资源有效。这里没有加热、物质能量域或外层收缩的测试结果，不能说物质失败或接受。此前8map预算保持原样；以下为审计后另外声明的一次有界诊断，不自动扩旧run预算。

保持x18及其全部encoded/base/r/direction/alpha逐位不变，仍以x17为基态、r17为残差分母。seed采用76383独立保留的endpoints-map08/mapped_final.dat，必须与其最后输出SHA吻合。源以已审归档清单固定，allocation内散列验证。已接受数17，不构造x19，不改物理旧层、rho、phase1367、dt889.419892762322秒或任何科学门。

先4map。第4张后保留previous/final/mapped_final三个独立dat；末两张未达原内层1e-4/边界1e-3即停止，不派发第5张。只有资源/有限性有效、末态残差较本新批首态下降且末两态过原内层门，才计算第一对完整反馈。物质响应异常/非正气体热能/完整态或资源错误立即停；物理域有效但部分原16科学门不通过时，允许同候选再4map。第8张后若内层门过则做第二对反馈；总共最多8新map、2对反馈，无第三阶段。此计划的进展门只是预算条件，绝不是物质接受门。

每对仍原16门及候选previous/final相对已确认x17两端点的四组合三范数收缩；共同co频窗与独立lab四力逆变换、原子率计算不改。最终支持须Mac审阅和同态确认/新基态对照，不能自动计18。反馈未知时不填替代数值。

cpu_long32CPU128GiB、16worker、BLAS1、hugepage0、4h；每worker原生及/proc<6GiB，每新反馈态累计worker批墙钟<900s。初始散列/响应检查在allocation；默认4核仅可做独立轻量审计。USR1当前批提交后停，失败不重交。新run common-step18-precision-20260924，冻结operations/common_step18_precision.py/.sbatch、测试、协议和完整依赖SHA，所有旧run/code不动。

本诊断旨在区分“固定候选辐射内层未足够精确”和“候选反馈本身不合格”。若4张后仍无足够内层进展，就停下来复盘；不从局部残差收缩率外推完成时间，不宣称当前模型无解，更不把任何一次门通过当成整盘I_nu。
