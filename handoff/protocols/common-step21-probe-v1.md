# 第21步失败后的单次幅度诊断

## 依据与问题

76808完整8map、2反馈对正常结束。Mac逐文件和逐块审计确认pair08仅最大单元收缩门失败：比1.0058592400049267，四组合最不利1.0058927826050046。noise0.04859767881696502已过；完整态、正gas与资源通过。

基态最大单元为索引2，候选为索引1（从0计数）。固定索引1，热能对数残差2.49082417830731降至2.4592809735504453，但HeIII/HeI对数比残差0.929191030513091升至1.0528991608036078。第4到8张该氦残差几乎不变。这支持检查有限方向的响应，而不支持继续原幅度无限加map；仍不构成严格内层误差界或物理无解证明。

新批不是预期必过的修复：线性插值甚至提示半幅仍可能失败。一次半幅实测的目的，是区分幅度相关响应与当前方向对最差单元的不利变化；不以两幅度断言完整Jacobian或全方向导数。

## 固定对象

新run `outputs/hpc/common-step21-probe-20260925`，新驱动`operations/common_step21_probe.py/.sbatch`。
基态仍为接受20的confirm2 trial本身，原方向及分母仍是76727 confirm2 final残差r20。旧物理层、phase1367、dt889.419892762322秒、密度、能量定义和微物理/转移算子不变。

先算alpha=0的基态对照4map/1pair，再有条件算唯一新候选：

$$
x_{21}^{\mathrm{probe}}=x_{20}+\frac{r_{20}}{256}.
$$

基态从76727 confirm2最新mapped后继起跑。对照通过后，新候选使用对照最新后继作为数值种子，不改变物质基态或原方向。对照产生的残差只作新比较，不覆盖r20或原门的分母。

## 执行与停止

总计最多12map/3pairs：control4map；half最多8map，第4/8张各一次反馈。每次两输入inner1e-4、boundary谱和总通量1e-3过才反馈。加热1e-3、noise0.1、原16门、trust温度0.5/能量0.25/布居0.05全部不变。

零控制只执行基态稳定与物理域检查，不走有限步接受。若对照不稳定或其两端点使76808 pair08四组合转为全部收缩，则停在对照，留给Mac审计，不继续试探。仅稳定且旧候选四组合仍失败才运行1/256。

新候选第4张不提前接受；无物理域/资源/完整态/代码故障才继续已定第8张。第8张原16门与对新control两端点的四组合三范数均收缩，才记`half_supported_requires_review`。接受数始终20/new0，仍需两轮同态确认及Mac独立审阅。若只剩收缩失败，记`half_rejected_requires_direction_review`；其他失败记`stopped_at_half`。不得自动试1/512、增加map、改变方向、缩短物理dt或放宽门槛。

输入为审计归档中确认的x20/r20和物理旧层；核接受记录、来源SHA、实际源清单、seed SHA，初始化前写trial。exact_trial和native_identity核encoded/base/r/direction/alpha/T/H/He/比能/旧层。零控制及新候选依赖、环境和协议冻结。

资源cpu_long32CPU/128GiB/16worker、BLAS1、hugepage0、4h。最多15新dat，入场17STATE_BYTES余量。每worker native与/proc小于6GiB、每反馈态累计worker批墙钟小于900秒。USR1当前batch后停，失败保留归档、不盲重交。旧大态不改不删。

## 审阅要求

先检查control漂移，再比较1/128和1/256在同一材料索引的四分量变化、最大单元切换、净能量和H/He绝对布居。必须同时保留三范数及四组合，不能因HeIII占比小而删除其log残差或更改预登记范数。局部差商与有限间隔漂移都不是严格误差界。若半幅仍失败，下一步是构造并验证方向诊断，不连续缩步求过门。未获得耦合柱或整盘涌现强度。
