# 较长跨度正性初猜：真实映射与两轮零控制反馈 v1

77366扫描完整审计后，选预测全局残差比最低且所有预测门与成本门通过的唯一候选。
本次选`x6-x14-x15`，有效系数$u=-80.37545614416956$、$v=0.99$，预测比0.7016517740493879。
固定物质仍为已接受第20步的trial，原残差基准、旧物理时间层、相位1367、物理步长889.419892762322秒、密度均不变。
本批无物质接受授权，不是第21物质步，更不是全盘强度表。

## 输入与运算

沿`step21-wide-plane-v1.md`的六态公式，保留每个输入与实际后继的SHA配对。
只把选中的$q$流式写入新目录，使用与扫描相同float64差分次序，逐点核$q,p$有限且非负。
partial完成并核大小后原子发布；已有文件拒绝覆盖，坏初猜不能靠裁剪修复。
先复制精确control trial再初始化，核encoded/base/direction/residual/relaxation等完整数组。

模板使用已审77126的finite thermal协议满足通用构造器契约，构造后写盘前改成零control trial与三项接受授权false。
与77299零控制协议逐项核acceptance/formal门、原物质/残差/旧时间层；实际构造/native检查在任何新map之前执行。
保留所有旧协议原字节，不篡改其授权绕过守卫。

## 完整有界执行顺序

1. 首张真实$T(q)$后逐片重建候选$q$并逐位核存储字节，比较真实结果与仿射预测$p$。
   记录602片的全局和弱尾局部误差、L2误差及实际残差。真实结果有限非负、76块完整，worker/parent<6GiB。
2. 原验证门保留：真实全局残差<1e-4、相对旧map16<0.99，边界L1/bol<1e-3，
   预测误差/全局场<1e-7且误差/真实变化<0.1；另要求真实残差比<0.8，验证成本收益确实实现。
   任何首张验证门失败即结束，不做下游map或反馈。
3. 通过后再算两张map，末两输入端点过严格内层门才计算pair03正式反馈。
   原零控制七门/物理响应全部有效才允许继续；程序、身份、资源错误直接停。
4. 首反馈通过后追加八张map（4—11），计算pair11。最多11张真实map、2个正式反馈对。
   不因首反馈看起来好而跳过已注册的第二窗口，也不因窗口漂移超门改科学门。
5. pair03对77299 pair16的四组合是**外推后响应位移**，不是8张普通map漂移；pair11对pair03才是8张map窗口。
   两者都保留完整128×4向量差的L2/质量加权/最大单元范数除原$r_{20}$三范数，阈值0.001。
   同时保存与77299对照及原$r_{20}$距离，禁止换基准让第21步过门。

每个首张验证、反馈对、终态都归档；轮次独占目录、端点在下一阶段前保留。
信号沿既有停止路径传入worker派发与反馈，保留半成品并停止，本驱动不在原目录自动恢复或盲重交。
新目录`outputs/hpc/step21-wide-plane-validation-20260925`；32CPU/128GiB/cpu_long/16worker，4小时硬上限，
USR1提前900秒，BLAS1/hugepage0，预留12个完整态空间。大态留学校，旧态不删。

成功仅表示本数值加速在该固定物质态下完成真实映射和反馈诊断；仍需Mac独立审计。
局部弱尾误差不因全局小而省略，窗口漂移不是精确解误差界；不能预先许诺整盘自洽大气的完成时间。
