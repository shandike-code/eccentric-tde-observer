# 共同频域基态与两次同态确认 v1

前提是75987已完成且Mac审计1854文件、304个worker回执、四个物质响应与原16门通过。仍保持接受计数15。此入口不生成新候选，不推进物理时间，不修改任何冻结历史代码。

## 顺序和硬预算

32CPU/128GiB、16worker、BLAS单线程、关闭hugepage，4小时上限。control最多4张新map；confirm1和confirm2分别最多2张。每阶段仅一次两态完整反馈，最多8map和3对反馈。任何故障、物理域失败、反馈或收缩门失败均停止后续阶段，保存工件；无自动续交或原地覆盖。

control由step16-backtrack-20260923/control的最后已测后继态起跑，物质必须逐字段等于冻结outer_base_material，alpha=0；严格decode和native镜像/phase/dt验证。confirm1从common-precision-maps-20260923独立保留的map08 mapped_final起跑；confirm2从confirm1最后后继起跑。两者都保持原1/64候选、原base/residual/direction，信赖域温度0.5/能量0.25/布居0.05，不替换原有限步信号分母。

每个child在初始化前复制真实trial并检查，继承配置全部源声明和SHA；可能重复核验较大历史数据，此I/O不是死锁。child结束后不再写其三槽，下一child独立保存，避免端点覆盖。旧run不动。

## 基态与候选判据

共同频域worker复用已验证common_native_feedback，完整计算原子率和独立四力。每态76块，原拥有权/正值/有限/镜像/源守恒/资源门不变，实际态墙钟严格小于900秒；原生和/proc内存各小于6GiB。

零位移控制仅执行两态反馈和物质响应，不能调用有限试步接受来制造零步信号。需要两个原算子残差<1e-4、边界两指标<1e-3，五项反馈稳定门（各分量率和三类加热）均<1e-3，物质响应在正气体热能域。响应异常保留账本，未求值量不填替代值。

以新基态previous/final残差为两个独立比较端点。先对75987两对旧候选分别计算四个candidate/base端点组合，每个组合L2、质量加权、最差单元范数比均<1，才继续。confirm1/2各自需原16门全过，并再次通过这四组合三范数比较；两次都通过才标confirmed_requires_mac_review。仍不自动增加接受数、不继续第17步。

旧冻结base_residual仍用于原16门和噪声/信号；新基态比较是额外约束，不是悄悄更换分母。相邻辐射态差或同态残差漂移不是严格误差界，暂不声称内层误差已被严格控制。

冻结本入口、sbatch、测试、此协议及全部现有Python依赖与输入哈希，前后核验。USR1完成当前批次后停；不足阶段不判通过。每阶段保存小工件归档与清单，不包含dat。有限步确认仍不等于耦合柱收敛，有限频域截断、环带/轨道覆盖和涌现I_nu是后续独立任务。
