# 77927失败的单遍只读分解

固定77577原x,y=T(x)，77843统一核心候选q,v=T(q)，77927渐消候选z,k=T(z)，共六完整场。只读取现存字节并前后验证来源哈希；两次拒绝和独立半步样本已审计。不重新运行物理核。

r=y-x，s=v-q，a=k-z；核心内p=(1-w)r+ws，核心外p=r。定义c=a-p，则a=p+c。记录每32组的五个平方L2/max（r,s,a,p,c）、交叉内积sum(p*c)、闭合误差以及actual最大点的index/a/p/c。另按全域、所选核心、核心外归约，重建||a||²=||p||²+||c||²+2<p,c>。c是实际非交换差，包含实际浮点/非仿射贡献；只有严格仿射且精确算术时等于(AW-WA)(q-x)，不可无证宣称纯物理项。

逐点验证z的固定sin平方候选身份；核外q和z必须等于x，输出不要求相等。所有六场有限非负，不clip/floor，不丢尾；原20物质、r20、dt不变。默认4CPU16GiB、一小时、父进程6GiB守卫，最多一遍统计（来源哈希读取另计），0map/0反馈/0新候选/0接受。无成本门选择新步长，不自动提交后续。

输出小型decomposition.json、声明、状态及包用于Mac独立归约；失败保存、不盲重试。解释哪些缺陷来自加权后残余，哪些来自非交换差，保留交叉项不能将平方范数当独立正贡献相加。用于下一算法决策，不证明模型无解或大气收敛。
