# 第21步：固定已接受基态的长窗口响应漂移诊断 v1

## 为什么改任务

77126的9张map/3反馈对已完整审计：两个物质候选均拒绝；热反馈、物理域与噪声门本身通过。
同期control与原$r_{20}$向量L2差约0.527，而相邻control差约0.00562。
两套端点的有限差商虽有局部共同下降区间，预注册833点有限幅度网格却无预测全门通过点。
不据此宣称非线性模型无解，也不盲试混合候选。优先测量固定$x_{20}$跨较长迭代窗口的反馈变化。

## 固定身份与预算

只使用77126/control的trial（已接受$x_{20}$）及其最后mapped_final辐射态作为新初值。
初始化前复制并逐位检查trial。原$r_{20}$、物理旧层、phase1367、dt889.419892762322秒、rho不变。
保持原全频9632×32×4096与完整128×4残差；0新物质候选、0物质接受、0基准替换。

新目录`outputs/hpc/step21-control-windows-20260925`，最多16个新map，在第8和16张后各做完整两端点零控制反馈。
两窗口分别是77126末反馈到新pair08、新pair08到pair16。所有端点组合都计算残差向量差的L2、质量加权、最大单元范数，
再分别除以固定原$r_{20}$对应范数，不比较“范数之差”。另保留pair16相对于77126的总变化。
诊断一致性条件为每个窗口的全部4×3个比值均严格小于$10^{-3}$；两窗口都通过才记录一致性见证。
该阈值是新诊断指标，不替代或放宽原物质接受门，不声称精确解误差有界。
若未通过，输出未稳定证据后结束，不自动加预算；即使通过，也不自动更换基准或接受第21步。

原严格内部残差$10^{-4}$、边界$10^{-3}$、完整零控制7门、物理域和资源门不变。
任意物理响应、输入身份、资源、原零控制门失败即停止归档；信号保留部分结果，不原目录盲重试。
禁止clip/floor/nan_to_num、弱点删除、重新归一化、改变能量定义或缩短物理dt。

## 资源与交付

32CPU/128GiB、16worker、BLAS1、hugepage0；4小时硬上限，USR1提前900秒，预留12个态空间。
每map检查76进程回执与6GiB门。源码/源SHA前后核验，每pair和终态小归档，dat留学校不删历史。
下一步必须独立审计两窗口的原始反馈、向量差和固定分母；一致性失败不当作模型无解，一致性通过也不当作严格内层收敛证明。
