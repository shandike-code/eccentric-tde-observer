# 74111扫描通过；下一步只验证一张原算子map

## 扫描实测结果

74111的`status=complete/outcome=prediction_feasible`，九项预测门全过；没有写候选dat、没有新物理map或物质响应。运行声明记录默认4CPU/16GiB、anode22；物理代码709bdb6，声明时Git HEAD为追加作业记录后的b13c24f。日志stdout/stderr均空。此次复核时sacct无完成行，scontrol已无该作业记录；因此只能按完整工件确认驱动完成，不能补造Slurm退出码、完整墙钟或MaxRSS。

预测最大归一化残差从最好实测3.0231933890773347e-5降到2.442668537069619e-5，改善19.202372369%；预测全场残差平方L2为1.5315715106724885e-5，anchor为2.1880210589073266e-5。边界L1=3.742192341748508e-5，bol=3.1778905117185e-5，均小于原1e-3门。完整场的候选/预测输出均零负值、最小值为零。

Gram特征值[3.913634223829491e-11, 1.0107700271243927e-7, 7.783029395789437e-4]，声明相对阈值1e-12下秩3，条件数约1.988696171e7。不能把“可分辨”解释为“系数对扰动不敏感”。

四系数按73929末次、73888末次、74057倒数第二、74057末次排列：

```text
[0.01925459292288146,
 -1.9539925233402755e-14,
 -15.499999999999895,
 16.480745407077034]
sum = 1.0; L1 = 31.99999999999983
```

系数达到原幅度上限附近，不擅自扩大上限。很小的第二系数也原样保留，不改成零。初始无约束解产生1095848/1066036个负值（候选/预测）；随后两轮为1934/2616、2/6；第四次全场扫描为0/0。第三次的最小负值为-5e-324，也按失败处理，未裁剪、未删除频点。最终零负值是按声明的有限精度运算观察到的结果，不是精确实数正性定理。

## 新的单map验证

新增`operations/half_four_map_candidate.py`和`operations/validate_half_four_map.py/.sbatch`，旧代码不改。计划新run `outputs/hpc/half-four-map-validation-20260920`，32CPU/128GiB、16worker、上限60分钟，最多一个候选dat和一张原算子map。此时尚未提交。

预测、声明、清单分别以SHA固定：

```text
prediction.json: 0cdcd1b720663f1a6e2bbafdbb2b44d4432dacee8dc7afbfbf6d6c61f3a1f5c6
declaration.json: 49d5dc124aab3fe0d995621e95153abe6c16b55500069efdf418fdc18c74c6cb
snapshot-manifest.json: e56a9b56c051271778be4aecb46163bade1d55d74c88f68ea5fb2c0e210d5e7c
```

先检查九门完整、数值实际满足门、basis来源/顺序、rank、预算、系数构造与实际最好基准一致。全场以扫描的同一求和顺序构造候选及预测，任何负值拒绝。候选原样写盘；物质trial先复制后初始化，初始化前后全字段/seed/空history/native核验，避免默认迁移替换候选。

真实map以后，重新构造每个候选单元并要求与写盘输入逐位一致；比较真实输出与预测输出的全场差，要求其L2小于实际映射缺陷L2的1%。另核实际最大残差改善、边界谱/bol、worker内存。所有源dat前后核SHA，旧物理dt、基态、方向、能量和转移核不变。USR1传递到pipeline STOP，不启动后继新块；中断保留现场，不自动重交准备器。

Mac相关63项测试通过，包含原初始化/跨历史测试及新增独立仿射算子、精确构造、字节改变拒绝、极弱负数拒绝、假通过标志/系数/秩/预算/顺序错配等。真实74111小工件通过新入口数值核验。学校端复跑同组后才提交。物理预测准确性尚未验证，不能把九门扫描通过当作反馈通过。

## 保全

学校家目录和Mac `outputs/review-20260920/`各有`half-four-map-scan-74111-small.tar.gz`，19,898,294 bytes、654文件，SHA256 `a83cd8c22449acf2411df382878f9370e2a12e348146cb350286cdfa0081a1ea`。整包和全部逐文件哈希已核，解包目录`half-four-map-scan-74111-received`。旧dat保留。Git证据`handoff/evidence/20260920-74111-prediction.json`。

改前Git备份`outputs/review-20260920/pre-half-four-map-validation.bundle`。只有实际map验证全过，后续才另行声明最多2张map/1对正式反馈。当前仍未接受物质步，不构成自洽柱或整盘强度。
