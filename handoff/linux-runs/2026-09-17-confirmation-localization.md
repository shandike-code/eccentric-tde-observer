# 有界只读定位：71765 的主导辐射块与加热回升

确认八张已完成，但末端辐射残差和加热变化均未过门。先做一次定位，不启动重复 map 或新候选。

新增 `operations/localize_confirmation.py` 和 `.sbatch`，在独立默认 QOS 4CPU/16G、20 分钟 allocation 中读取停止态 `hhe-r003125-confirm-20260917`：

1. 校验 config/冻结源、8×76 个 map 记录的输入血缘和频率归属，复现聚合残差，报告全局归一化与块自身尺度的主导块。
2. 对最后 map 的保留输入/输出检查完整 SHA256，按原始 `(9632,32,4096)` 张量定位主导块的最大变化，明确方向、辐射层、256 父层及128半柱层归属。不声称已定位早期被轮换覆盖的大态。
3. 校验两轮四个 feedback manifest、各块部分 NPZ 和完整 NPZ，重放分块求和。将总加热差的 L1 分解为带符号块贡献：每块的差乘总差的符号及深度宽度，再除正式总尺度，所有块贡献相加应等于正式 L1。允许负贡献；另报逐块绝对差总和与净差之比，避免把跨块相消误当独立可加误差。
4. 按16子层→256父层→镜像128半柱的已修正映射，分解物质层贡献；比较吸收和发射变化；对四端点计算现有新残差诊断及相邻差，不构造新方向或替换旧验收门。

只写新目录 `outputs/hpc/confirmation-localization-20260917`；禁止覆盖。旧 `.dat`、反馈、协议、物质候选与所有门保持原样。小工件在审计始末检查身份。该任务会读取两份9.41GiB原态以核验哈希，故在allocation中执行，不在登录节点做数值和大I/O。

24 项相关测试通过，包含带符号贡献的相消闭合、镜像层归属、频率重复/缺漏及非法权重拒绝，和原能量/新残差检查。合成测试不能代替真实工件核验。新脚本独立声明自身及batch文件SHA，运行环境记录提交与Slurm参数。

## 首交资源调用修正

71814 在 13:09:44 以 FAILED/1:0、5秒结束，进入数据读取之前原资源守卫拒绝了 `require_allocation(4)`。此参数代表重型worker数量，不是申请CPU数；本诊断仅一个串行进程，应调用 `require_allocation(1)`，保留原每worker 6GiB+主进程2GiB守卫。没有减小原资源保护，也没有为错误实现增加内存。新增默认4CPU/16GiB应通过、4GiB应拒绝的真实守卫回归测试；旧失败调度记录保存于平台 `outputs/hpc/confirmation-localization-71814-scheduler.txt`。新尝试使用 `confirmation-localization-20260917-v2`，不掩盖首次失败。

71815 第二次准备在2秒内被路径守卫拒绝：内部绝对Path误送给只收相对路径的 `safe_path`。保留失败目录与调度记录；新增明确的仓库内绝对→相对转换，仍拒绝目录穿越、外部绝对路径和符号链接逃逸，并加入该边界回归。未读取大辐射态或执行新科学map。第三次尝试使用 `confirmation-localization-20260917-v3`。以上两项都是新审计入口的接口缺陷，不是旧物理计算失败。

## 实测完成：71816，45 秒

第三次尝试在 2026-09-17 13:13:25–13:14:10 COMPLETED/0:0，4CPU/16GiB，26项相关测试在Mac/Linux均通过。真实八张聚合残差、四个反馈端点分块和、两份保留辐射态SHA均核验通过。`scheduler.json`已在独立输出目录保存；小型报告和全部输入哈希见 `handoff/evidence/ustc-confirm-localization-20260917.json`。不需要再次运行这个诊断。

**主导残差换块。** map1–3 是 block25，从map4起变为block23。map8实际最大变化位于频率索引3036、方向31、辐射子层1784，对应全柱父层111、半柱物质单元111；输入强度0.02801968829，输出0.02803003282，差1.03445235e-5。所有索引从0开始。这里定位的是强度变化的最大值，不是强度本身的峰值。

**加热回升伴随相消减弱。** 逐块绝对变化在各轮正式分母下的总和从0.024888213降至0.022369267（-10.12%）；净L1却从0.000505639886升到0.001217227487。两者之比从49.2212降到18.3772。分母只变-0.1032%，净分子增加约140.48%，因此回升不是分母骤降造成。两轮emitted逐块逐层变化均严格0，absorbed变化仍在。主要正投影贡献含block19–23、46–47，反向贡献含block29–33；带符号贡献闭合误差分别1.95e-18和0。

**暗尾并非这里的主要加热变化来源。** map8 block74自身相对变化0.153746，但全局归一化仅3.64576e-297，最后一轮带符号加热贡献1.19414e-299。不能继承旧候选“尾块严格为零”的描述，也不能把该自身相对变化当主导能量瓶颈。

**三个最差位置不同。** 最大辐射变化归属半柱cell111；无正目标热能最差cell106；新能量方程归一化缺陷最大cell3。最后final新范数L2=100.32183、质量加权3.687715、maxcell32.17498，仍只作诊断。两轮相邻新残差差L2分别0.0442472、0.0581408，不能直接替代旧内层噪声/试步信号比。

此结果能解释全局残差曲线为何突然变慢、相邻加热门为何非单调；它不证明渐近误差模式、精确内层极限或物理根存在性。下一步先建立新残差同口径基态及内层误差控制，再确定方向验证或有界精度实验，不能仅按block74自身变化去重算高频尾，也不自动继续八张。

## 原基态追溯入口与当前访问状态

代码核查得到：旧基态残差由 `scripts/phase7b9f_converged_feedback_residual.py` 产生；声明是 `outputs/phase7b9f_preregistered_converged_feedback_residual.json`，其基态物质为 `outputs/phase7b7h_second_material_iterate.npz`，原previous/final反馈分别为 `outputs/phase7b9f_previous_feedback.npz` 和 `outputs/phase7b9f_final_feedback.npz`。这些是源码声明的路径，不表示学校侧已核验文件存在及哈希。本地review目录目前只确认有 `phase7b9f_base_material_residual.npy`，它不足以直接恢复新能量/布居残差。

下一次先核验上述协议、摘要、反馈及其原基态身份，再决定重放已存反馈还是重新准备同一基态的辐射；不能从旧编码残差单独猜出全部新残差，不能擅自用0.03125候选当作历史原基态。2026-09-17 13:57跟进时SSH返回Permission denied(publickey)，无法进一步核验学校文件或新提交作业；已通知用户重新认证并暂停自动跟进。71816完成证据已在本地，归档不受影响。

## 访问中断期间准备的基态转换核

新增纯函数 `operations/recover_equation_baseline.py::recover_equation_residual`。原有限残差为 `r_legacy = encoded_target - encoded_base`，所以在确认原编码、残差、codec及旧热能尺度身份后，可解码 `encoded_base + r_legacy`，用 `(e_base-e_target)/old_gas` 得到新能量分量，布居三分量则是原残差的相反数。它保留电离能变化，不是仅翻转旧热坐标符号。转换有浮点编解码误差，不声称字节精确。

8项新增测试通过，连同定位、新残差和原账本共34项：与直接能量/布居方程交叉比较、热坐标不变但电离能变化的例子、零点保持、非法尺度、未求值/非有限响应和simplex边界拒绝。仅为Mac合成测试；SSH失效后未在Linux执行此新增组，未对真实基态作转换。

待认证恢复，优先核查 `phase7b9f_preregistered_converged_feedback_residual.json` 固定的 `phase7b9d_encoded_base_material_state.npy`、`phase7b9f_base_material_residual.npy`、旧时间层和摘要。若原反馈仍在，用原反馈重放结果交叉核对转换；若只剩编码及残差，可保存明确标为旧端点重建的诊断，并保留其旧精度与无法新增相邻噪声估计的限制。不能将其命名为重新求解的辐射基态，也不能仅凭转换后范数决定新方向。旧响应无定义的43层候选不适用这条转换路径。
