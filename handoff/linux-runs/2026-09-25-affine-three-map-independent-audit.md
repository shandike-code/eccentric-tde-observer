# 76957中期审计：三次真实映射通过，物质反馈仍待计算

本报告只评价76957已经完成的三个外推候选真实映射，不对仍在运行的后续反馈提前判定。control、thermal、population各一张原算子map完成，正式接受数仍20。

## 工件与复算范围

学校归档`population-validation-1790304399314204202.tar.gz`包含三个case首次map检验的完整小型快照，3267693字节，SHA256 `0f3641c7a23dc7ec2a055fcf2a8b9c2e906370cf7f3366bfcc81fe630eae67bb`。Mac保存在`outputs/review-20260925/`，解包目录`step21-affine-76957-three-maps-received`。

独立脚本`handoff/audit_tools/review_step21_affine_maps.py`核488个文件、680条冻结代码声明、228份map进程回执。三份trial与76931归档逐字节一致，候选哈希与真实map输入一致，配置/初始化/native身份记录一致。每case76块完整覆盖9632频率组，强度非负、标量有限；按原公式独立重算最大残差、边界谱和总通量变化、资源门，全部吻合。

每case602个16频率slab覆盖全部频率。Mac从平台记录的每slab最大误差、强度尺度、真实增量复算两项全局预测误差比及原续算门。Mac没有读取三维大型强度态，没有重积分场L2，因而该部分是全场统计的独立再聚合，不是假称第二次完整辐射求解。平台实际读取并比较了全频率×角度×深度。证据`handoff/evidence/20260925-step21-affine-three-map-review.json/png`；图已查看，同时展示全局改善、全场预测差异及弱尾局部差异。

## 实测结果

| case | 实测全局残差 | 实测/旧值 | 预测误差/全场强度 | 预测误差/真实最大增量 | map墙钟 |
|---|---:|---:|---:|---:|---:|
| control | 5.1368888205e-6 | 0.7375932735 | 4.2928011e-14 | 8.3568114e-9 | 141.92s |
| thermal | 4.8012024315e-6 | 0.7706355322 | 4.2927189e-14 | 8.9409247e-9 | 143.27s |
| population | 4.8745769086e-6 | 0.8249473759 | 4.5979222e-14 | 9.4324538e-9 | 146.32s |

边界谱变化依次1.0126843560e-5、1.7722399466e-5、6.8619458444e-6；两类边界门通过。独立/proc峰最高3587520KiB，native与父进程也低于6GiB。这里的map墙钟不包括之前准备、大态SHA、候选写入和后续归档，不能拿2.4分钟估算整个任务。

## 不能遗漏的弱尾限制

三个case最差的128频率块都是block74，自身实际相对残差分别0.16048557、0.15938984、0.15936810。因此实际全局残差小不是所有频率局部相对误差小。

对于预测与实测之差，最差16频率slab均为索引`[9488,9504)`。局部预测误差比为0.10486730、0.02380732、0.02444100；相应局部强度尺度为1.4132942392e-310、5.5974464298e-310、6.6758922228e-310，绝对误差约1.3e-311至1.6e-311。它们小于float64最小正常数，处于非正规数范围；这是对数值量级的确认，尚未定位具体哪个算术环节造成差异，不宣称“已证明只是舍入”。

原方案的全局算法续算门通过。局部弱尾差异完整保留，不删频点、不floor、不改门，也不把全局4e-14说成每个单元4e-14相对精度。对加热、率和物质响应的影响由后续完整反馈继续检验；本报告不根据强度弱就直接跳过反馈验证。

## 决策、备份与后续

保持76957现有条件批次运行：control再两张map及一对反馈；control稳定且物理域有效后，thermal/population各再两张和一对。最多9map/3pair，原物理dt和原16门不变；正式接受数20。没有加任务、没有重复外推、没有修改活跃数值依赖。

审计脚本置于`handoff/audit_tools/`，不属于活跃作业`code_claims`所冻结的src/scripts/hpc/operations/diagnostics集合，也不由作业导入；同步它只补充可复算的审阅工具，不改变求解过程。运行前已检查源身份和维度、原聚合公式与正性/资源门；运行后未见非有限指标、负强度或进程故障。弱尾现象如上保留，不能据此推断大气闭合或整盘强度。

Mac备份`outputs/review-20260925/pre-affine-three-map-audit.bundle`、`automation-before-affine-three-map-audit.toml`。原76957目录、旧输入和既有历史态均保留。下一次以新的反馈工件和物质门为决策依据，不从本报告外推项目完成时间。

10:55:06实测：76957仍RUNNING，control已3map、thermal/population各1map，均无active半态，control反馈正在执行，stderr空、watcher活。启动预算共9map，当前完成5张，不额外提交。快照`handoff/evidence/20260925-step21-affine-three-map-progress.json`。学校同步前备份`/home/scc/pb24511938/pre-affine-three-map-audit.bundle`。新增审计器对真实小归档端到端复算通过；Markdown检查仍仅历史六份文件，不修改它们。
