# 74075之后：显式四对真实map的只读扫描

新入口`operations/scan_half_four_map_subspace.py/.sbatch`，保留所有旧源码。四对的固定顺序：73929最后map、73888最后map、74057倒数第二map、74057最后map。后三态复用路径必须具有相同哈希；不把三条历史拼成一条时间链。三份state和74075审计JSON的SHA来自已经逐文件核验的Mac备份，写入新代码。

固定物质alpha=0.001953125、phase1367、dt889.419892762322秒。运行时再次核全部物质数组、native、网格、物理旧时间层和冻结算子源码。Mac真实工件检查确认三份配置的517项src/scripts/hpc源清单相同。大dat的大小和SHA在allocation内前后检查。

以74057最后map输入作为anchor。四系数和为1，在三个独立残差差向量上求全场辐射缺陷平方L2最小值；沿用已测试的三维小系统与正性约束求解器，不改率核或转移核。相对秩阈值1e-12，秩不足3时输出`no_resolved_candidate`，不静默降秩。正性约束最多6轮、4096条；每频率块最多选2条最强归一化负值约束，但所有负值都计数，最终还要完整扫全场。小系统约束容差不代替全场正性判决。

全局系数步仅限制L1<=32，不改变任何单元。预测必须全场有限非负，最大残差比现有最优实测至少改善1%，边界L1和bol均低于原1e-3门；约束预算未解决也不能过门。保留全场L2目标与最大范数门的区别。物质范数不参与优化，不通过选择种子制造物质步通过。

默认4CPU/16GiB、上限60分钟；零新map、零候选dat写入。新run计划名`outputs/hpc/half-four-map-scan-20260920`。只有预测全部通过，后续才另建独立32CPU/128GiB、16worker、最多一张真实map的验证实验。扫描不是新的正式辐射解，也不保证仿射预测准确。

测试包括独立最小二乘对照、真实map配对、未完成/错配拒绝、退化秩、1e-250量级负值约束、非法源强度、全局系数界、零尾端以及预算/秩失败不能被小残差覆盖。真实小工件核验只读；物理核不修改。改前备份`outputs/review-20260920/pre-half-four-map-scan.bundle`。

运行前需完成Mac/Linux相关测试、核三端提交与队列、发具体PRE-RUN CHECK，再提交一次新run；已有目录拒绝覆盖。运行后保全status/declaration/system/prediction/cut_progress/snapshot-manifest与日志，检查完整门、秩和警告，再决定下一阶段。本计划写入时尚未提交作业。
