# 74235：四例局部校正有效，下一步检验全频耦合

## 已核验的完成状态

2026-09-21 09:06北京时间SSH恢复。队列为空，run `outputs/hpc/paired-block-krylov-20260921`的status为complete，summary齐备，执行前后原输入/输出完整字节不变；零新全频map、零反馈对、零全局候选写入。原四例配置和代码0f512ca未改变；运行声明记录的Git HEAD为39620ec（仅增加提交记录）。此前Mac/Linux相关41项测试通过。

复核时scontrol记录已过期。下表是各worker的计算段墙钟/RSS，不是作业总墙钟或Slurm总MaxRSS；不补造调度退出码。父作业及四worker的stderr均为空。

| 物质/块 | 缺陷L2比 | 缺陷最大值比 | 正性步/线步 | 计算秒 | 峰值MiB | 相对一次局部map耗时 |
|---|---:|---:|---|---:|---:|---:|
| 基态/14 | 2.19827e-5 | 9.17770e-5 | 1/1 | 219.402 | 3160.492 | 19.184 |
| 基态/47 | 0.101991 | 0.327251 | 1/1 | 233.638 | 3204.875 | 19.121 |
| 候选/14 | 2.60018e-5 | 1.29867e-4 | 1/1 | 218.679 | 3160.316 | 19.140 |
| 候选/47 | 0.0990039 | 0.253565 | 1/1 | 235.913 | 3204.930 | 19.132 |

四例原映射均逐位重放。每例8项局部门均通过；比较使用原缺陷固定尺度，未通过改变候选亮度分母制造收益。block14频率边界3.54074894e14至4.28899153e14 Hz；block47为6.75595689e15至7.54772227e15 Hz。最小候选强度均正，约2.16e-6或1.52e-9（原数组单位）；没有裁剪。边界缺陷比依次1.70e-5、0.01505、2.64e-5；候选/47原值和新值均0，因此比值未定义，审阅CSV明确留空，不把它说成100%改善。

## 不能越过的结论边界

四例均用满16次GMRES迭代，info=2，线性审计均false。其scaled Linf分别为1.46286e-4、0.339980、1.58879e-4、0.318219；mean consistency分别约1.12e-5、0.00531、1.67e-5、0.00211。这里证明的是有限校正方向使原算子局部缺陷下降，没有得到通过线性审计的精确校正解。

另一个审阅发现：四例line_fraction全部为1，因此线候选就是已求值端点。预测误差0在这里检验的是同一点重复求值的一致性，没有独立验证内部插值点的仿射关系。旧JSON门与判决保持，但不能把这个零误差写成额外的算子线性证据。后续须明确设置内部探针。

本轮固定邻块halo，尚未检验修改两个频率块后其他块的响应；不能据此称全频加速有效。计算成本约一次局部map的19.1倍，接近预设20倍门，也不能单凭残差下降倍率宣称总耗时节约。

## 已核查的历史全频失败

本轮补读原Mac `phase7b9u_block73_failure_capture_summary.json`：7B9t在已完成73块后，block73（组9344至9472）抛出`PhysicalDomainError: no positive affine step preserves non-negativity`，复现时return code为1，候选块SHA前后相同，说明该块未在异常前被写入。这是实际正性方向失败，不能通过删掉尾部、给零值加floor或无限加深Krylov处理。7B9z只保留部分已通过舍入审计的块，明确没有授权全局候选残差或物质反馈。

## 下一有限实验：两块校正、全频原映射验证

已有证据支持检验局部收益能否穿过频率块耦合，而不支持立即对全部76块施加Krylov。下一任务应实现独立驱动与协议，冻结如下范围：

1. 基态和候选仍分别使用74235的原输入state1及原输出state2；全部trial、旧时间层、phase、dt、native核、频率/角度/深度和输入SHA保持。重建block14/47同样的16步校正，将方向及两个块的候选另存，核其原局部报告；74235未保存这些数组，因此重建是为生成新候选，不冒称可直接恢复原数组。
2. 每个物质状态构造完整场的端点与半步各一份：只在14/47施加同一方向的全步或0.5步，其余74块沿用原输入。所有频率仍保留且重新求值，不删掉未加速的块。半步用于独立插值探针。全场保持非负，不给高能尾部施加旧有失效的Krylov方向，不做floor或阈值置零。
3. 对两物质状态各两个完整候选调用冻结原全频map，共最多4张新map，零物质反馈。每个候选所有76块/9632组都必须求值并检查覆盖、正性、有限性、输入/输出血缘和SHA。预算以新协议钉住，不自动追加第5张。申请cpu_long32CPU/128GiB、16worker；组件和真实准备器检查通过、代码三端同步之后才提交。
4. 直接比较原输入的已测缺陷、全步候选的真实全频缺陷，以及半步的实际缺陷和两端点线性插值。L2及最大缺陷使用同一原尺度，保留逐块贡献、边界变化和完整频组。将14/47以外的缺陷变化列为halo传播证据，不能只展示原两块；实际耦合范围从频率stencil确定，不能假定仅相邻编号块受影响。检查62/73及零尾部，禁止隐藏差的块。
5. 预期可能出现两种结果：局部收益被跨块耦合抵消，则停止两块混合校正路线；全场缺陷及边界均改善、半步预测闭合且全部物理/血缘/资源门过，则再定义一对正式物质反馈。全频map通过仍不等于物质步接受。数值门必须在运行前由新协议明确，不能看到结果后选阈值或放宽已有正式门。

这一计划保留所有尾部并以原算子重算全场；它改变数值初值，不改变物理模型。当前并未提交上述新作业，后续任务先完成实现和准入检查。

## 保全与POST-RUN CHECK

双端包`paired-block-krylov-74235-small.tar.gz`：19,808,104 bytes、667文件，SHA256 `00f3d757893bcb74bc2c8a4067a5b8f361d26831f82afbc20a5f088f2109bec1`，在学校家目录和Mac `outputs/review-20260921/`。整包及667文件已校验，解包`paired-block-krylov-74235-received`。summary SHA为`91812ae368b41a2df0921a3840e5dca4630c7591278183d950c229526cdb2cb0`。原dat不动。

`operations/render_paired_block_review.py`由已核JSON生成CSV、review.json和comparison.png，保留线性审计false、未测内部插值点、零边界分母三项限制；真实工件标量比例复算通过，JSON所有浮点有限。图已目视检查，坐标、标签、图例可读，无异常负值或缺项；对数轴显示局部缺陷收益，成本图显示20倍门及实测余量。

没有新的物质能量或率计算，不能用这幅图更新原正式反馈判决。当前仍未接受物质步，未得到自洽柱或整盘强度表。
