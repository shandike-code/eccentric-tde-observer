# 16:22 巡检：基态种子历史第一轮反馈完成，继续原预算

作业 73308，`outputs/hpc/paired-precision-base-seeded-20260919`，默认 4CPU/16G、2 worker、anode16。读取时运行 1:11:52，已有三张 map；两轮上限不变，不另交作业。

## 已完成结果

新增三张 map 的算子残差为 2.62092355e-4、2.42313553e-4、2.25905075e-4，墙钟分别 869.17、859.61、875.61 s。第一轮反馈对应前两张 map 的**输入**，不是第三张输出；反馈共耗时 1574.85 s。

第一轮 previous/final 均 0/128 非正目标气体热能单元；最小目标/旧气体热能比为 0.998439975 / 0.999817883。最大光致电离分量变化 6.13562669e-5，复合率变化 1.30165013e-6；原子、直接和正式加热变化约 0.00366928。比之前共同初值短程对照的 0.19173777 明显缩小，但仍超过 1e-3。

原协议的正式 encoded 残差判据：

| 指标 | 值 | 门的状态 |
|---|---:|---|
| 内层噪声 / 试探信号 L2 | 0.028600869 | 通过 |
| 候选 / 基态 L2 | 1.016483181 | 不通过 |
| 候选 / 基态质量加权范数 | 1.221136689 | 不通过 |
| 候选 / 基态最差单元范数 | 0.994452192 | 通过 |

这些是冻结协议的 encoded 残差比，不是新方程诊断范数，不混用两者。总体接受 False；失败门是三个加热门、内层辐射门、L2 与质量加权收缩门。内层噪声门通过不等于已严格收敛或具备真实误差上界。正热能保持也不能掩盖两个外层残差范数未收缩。

## 备份及检查

学校家目录和 Mac `outputs/review-20260919/` 均存 `paired-precision-73308-round1-small.tar.gz`，51,198,431 bytes，SHA256 `40a107e9bd347af88e35618e61df07ebf7affac96bbe60836abcfa6929e06c0f`。

这是运行中的**第一轮快照**：包含完整 round1、输入快照、根目录小文件、一次读取的 state 和当时 stdout/stderr；不包含正在写的后续 map 目录或任何 dat/lock。state 与日志采集时刻可能略有差别，不能把快照当最终停机现场。Mac 解包到 `outputs/review-20260919/paired-precision-73308-round1-received/`；875 个文件大小/SHA、两反馈 NPZ 与账本 SHA、协议与 summary SHA、152 个反馈块 SHA 和每态 76 块完整归属全部通过；两个反馈 NPZ 的数值数组均有限。本轮没有新增物理计算或修改物理代码。

stderr 有 Matplotlib `constrained_layout not applied` 告警。实看原 `feedback.png`：曲线可见，但上下子图文字重叠，右侧失败门文本被裁切；图例写了 “converged state”，与当前辐射门未过不符。它是冻结绘图模板的展示缺陷，不能用图例宣布收敛。保留原图原字节，正式比较时另出标记为未收敛端点的独立图；本次数字和判决引用 JSON。

## 决策

继续 73308 原来最后一张 map 和第二轮反馈。73309 已完成且停在预算终态，不自动延长。等第二轮完成后，先完整双端归档，再对同一物质的两条历史比较逐层热能、率/加热和完整残差向量；不能仅凭一条保持正热能而选择它为物理解。暂不改变物理 dt、方向、能量定义或验收门。
