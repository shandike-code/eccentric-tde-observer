# 74031：重放成功，辐射历史差异主要传入气体热能

## 实测与复核

代码`f266cb4`，默认4CPU/16GiB，anode18，74031在2026-09-20 18:13:46–18:14:36 CST运行50秒，COMPLETED/0:0。新目录`outputs/hpc/half-history-replay-20260920`，零新map。Mac/Linux各28项相关测试通过。stderr只有首次字体缓存提示，图已视觉检查：标题、坐标与图例可读，无裁剪。128层横坐标仅为物质半柱索引，没有转换成几何高度。

对73929和73888的previous/final四端点：全部76块核心频率所有权完整无重叠，各块SHA通过；按原加法顺序重组full数组，再复现256母层均值和前128物质层，全部逐位一致。两final物质响应残差与正式保存向量逐位一致；独立账本与响应的目标总能量及H/He布居一致至相对容差1e-12。所有读取的反馈、账本均有限，目标气体热能无非正层，能量split自检为0。这些检查排除了本次对比中的工件混用、频块汇总、归层和响应重放错误，不证明原物理模型完整。

## 定量结果

| 量 | 基态初猜73929相邻变化 | 旧历史73888相邻变化 | 两final跨历史差距 |
|---|---:|---:|---:|
| 编码残差差向量L2 | 0.01731637071 | 0.03962950069 | 3.836479051 |
| 编码残差差向量质量范数 | 0.001576562215 | 0.004980047577 | 0.485031056 |
| 光电离体积L1（最差分量） | 0.000022929788 | 0.000035525050 | 0.004571336276 |
| 总复合体积L1（最差分量） | 0.000000426200 | 0.000000274999 | 0.000014517615 |
| 原子净加热体积L1 | 0.000492357943 | 0.000802542414 | 0.106096186678 |

跨历史向量差的L2约为两条链自身相邻变化的222倍和97倍；这不是误差放大因子或严格误差上界。跨历史加热比使用与正式相邻指标相同的体积测度/归一化方式，但它是新诊断比较，不是篡改原协议判决。

跨历史残差差的平方L2中99.9640523%来自编码的气体热能对数分量，质量范数平方中该份额为99.9806851%；余量来自H/He布居比。平方编码范数的份额不是物理能量份额。贡献最大的单元为115、114、116、113、121、122、123、117。以cell115为例，两条链目标气体热能分别1.613970957e13与7.456949107e12 erg/g，均正但明显不同。

同物质发射功率数组在两条链及各自相邻端点之间逐位不变；吸收功率的跨历史全局峰值归一变化为0.0003502901。净加热是两个大项相减，其跨历史体积L1达到0.1060962。不能把局部相消放大因子乘以不同测度的聚合变化来构造因果证明。

逐层能量账本给出更直接的解释：相同旧物质能量和物理步长下，目标气体热能差等于两链辐射能量增量之差，再减去新电离能之差。最大辐射增量差8.682760467e12 erg/g，位于cell115；全柱最大新电离能差仅2.572364844e4 erg/g。因此在这组实测端点，目标热能差主要由净辐射加热差传入。仍不能据此宣称全模型无解、多解或已经得到精确固定点。

## 决策：固定物质下有界扫描历史差方向

不继续减半物质步，也不直接用当前残差重建Jacobian。新增`operations/scan_half_history_direction.py`，复用已验证的全场流式扫描与正性区间算法，针对每条链的最后一个真实map输入X和输出Y，扫描一个全局仿射系数；系数和恒为1、绝对值和上限32。X/Y全场均须非负；不对单元clip/floor。四个大态在allocation内前后验SHA，不写候选、不算新map。

新入口使用一般的末次map选择器，支持8张历史，不复用旧代码仅认2张历史或固定另一物质幅度的入口。先核74031完成及源码/输入快照，再核两trial全字段、native、物理离散控制。默认4核16GiB足够流式读场，最多20分钟。此步主要为磁盘流式扫描，32个worker不适用；若预测通过，下一阶段才提交独立32CPU128GiB真实原算子验证，并另行声明候选写入和map预算。预测失败则记录限制因素，不放松正性/系数门。

## 保全

`half-history-replay-74031-small.tar.gz`学校家目录与Mac `outputs/review-20260920/`双端，81,617,641 bytes、957文件，SHA256 `b2b32fdb7151d2b36378374211066722e146c800b5013a0390d192d1c6b30058`。整包及全部逐文件SHA已核。Mac独立解包`half-history-replay-74031-received`。包含全部小输入快照、JSON、NPZ、CSV、图和日志，无dat；原结果不改。Git小证据`handoff/evidence/20260920-74031-history-replay-audit.json`保留完整指标与逐层编码差贡献。
