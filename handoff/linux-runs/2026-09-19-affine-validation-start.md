# 20:10 巡检：慢模代数预测通过，启动真实映射验证

73415已complete，未写候选、未运行新map。小包`slow-mode-scan-73415-small.tar.gz`已在学校家目录和Mac `outputs/review-20260919/`双端保全，726060 bytes，SHA256 `b9a75ae9620d13d873a290d39bcf9a155189bc63ed8cd3b5179f7c074797ef03`；Mac逐一核14文件。完整预测另存`handoff/evidence/20260919-slow-mode-prediction.json`。

## 预测，尚非实际收敛结果

设每条历史最后三态为a、b、c，满足已记录的连续映射b=T(a)、c=T(b)。受保护算法选一个全局系数s，候选为`b+(s-1)*(b-a)`，预测映射为`c+(s-1)*(c-b)`。这不是逐层裁剪，也不是把s等同实际已做s张map。只有新算子映射才能验证预测。

| 历史 | 全局系数s | 正性限制上界 | 原实际残差 | 预测残差 | 预测边界L1 |
|---|---:|---:|---:|---:|---:|
| 基态种子 | 12.05820517 | 70.30683459 | 2.11212029e-4 | 1.22176372e-4 | 1.37744879e-4 |
| 旧历史 | 36.94444706 | 2498.36060（另受96限制） | 3.14358496e-4 | 2.41157542e-4 | 2.32552570e-5 |

两条基态强度非负、方向可分辨、全局系数与正性/边界/预测最大残差保护全部通过；候选和预测映射的负强度计数均0。两条预测都仍高于严格1e-4，不因预测优于2.5e-4就接受内层或物质步。

## 新作业与预算

| Job | 新run（outputs/hpc下） | 资源 | 预算 |
|---|---|---|---|
| 73435 | affine-base-map-validation-20260919 | cpu_long32CPU/128GiB，16worker | 45min，1张完整map |
| 73436 | affine-old-map-validation-20260919 | 默认4CPU/16GiB，2worker | 45min，1张完整map |

代码`1e40a82`，`operations/validate_history_extrapolation.py/.sbatch`；两端13项测试通过，含分块写入与公式逐值一致、原输入不变、负值/NaN/短读拒绝、实际残差恶化不能由预测覆盖。代码变更前备份`pre-affine-validation-0e211ac.bundle`。

普通文件分块读取a、b并写候选，限制常驻数组内存；不把三个大态全部复制进RAM。先完整核basis SHA，候选写完再核原六态中相应三态；不改变原字节。新trial在pipeline初始化前显式复制并逐字段核对、native核验，防止默认迁移替换为0.0625。每个新run冻结config/声明/种子和源码SHA，显式NUMPY_MADVISE_HUGEPAGE=0；不改历史协议或计算核。

写入期间的`anderson_candidate.partial`可能很大；若失败，保留它但小包必须排除该partial以及dat/lock，不能把它当小JSON工件上传。候选完整合法后才更名dat并入种子声明。

## 真正要检查的结果

每个run的`validation_result.json`记录实际map残差、边界、内存、实际与预测残差之差。pipeline aggregate会拒绝频率缺口/重叠、非有限块指标以及负输入/输出强度。外推仅在实际最大残差相对原态<0.99、边界两门<1e-3、worker RSS<6144MiB时记validated；这不是严格收敛或物质接受。

本轮没有反馈预算，不把单个map拼入旧历史凑连续反馈。若真实验证通过，之后再独立声明追加真实map/反馈预算；若失败，保留失败候选和原态，不能改系数事后挑最好结果。标量残差吻合也不证明整个算子仿射，当前未计算完整预测映射与实际输出的逐单元差。
