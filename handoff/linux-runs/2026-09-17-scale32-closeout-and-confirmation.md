# 71606 结束与一次内层门槛确认

作业 71606 在 2026-09-17 11:27:19 COMPLETED / 0:0，总墙钟 2:07:40（包含六次测速、初始化、32 张科学映射及四轮反馈）。无 active map 或 pending feedback。科学试探未接受；原始小型摘录见 `handoff/evidence/ustc-scale32-closeout-20260917.json`。

| 科学反馈轮次 | 加热相邻变化 | final 非物理响应单元 | final 最差剩余热能/旧气体热能 |
| --- | ---: | ---: | ---: |
| 1 | 0.00675172 | 34 | -1.64587 |
| 2 | 0.00523024 | 36 | -1.86809 |
| 3 | 0.00267282 | 43 | -2.01436 |
| 4 | 0.000722180 | 43 | -2.06608 |

最后 map 残差 0.000319538，门 0.00025；最后两个 map 的残差分别 0.000334774、0.000319538，均未过门。边界谱/bolometric、率和三类加热稳定性在第四轮均通过。物质响应仍因无正气体热能而抛异常，噪声和三种物质残差尚未求值；不能把未求值当作残差无穷大或布居已测得负值。候选本身的物理域检查通过，与冻结辐射给出的目标物质响应失败是两件事。

## 下一项有限任务及依据

新 run `outputs/hpc/hhe-r003125-confirm-20260917`，继承停止端点 `829600014fd346e0ea1701e48a78514813159bc918f6f8869814dcb4bdd953f7` 和完全相同的 0.03125 trial。32 CPU/128G、16 worker、墙钟上限 2h；新增最多 8 张、每 4 张一次正式反馈，共最多 2 轮。无重复测速，不改变物理 dt、基态、方向、能量定义或验收门。

这不是继续期待负热能自动消失。目的仅是把“辐射残差仍比门高约 28%”这个混淆因素尽量排除，验证合格内层下物质响应是否仍被拒。按最近收缩趋势 8 张可能跨门，但这不是保证。驱动只接受刚结束的 32 张来源、残差处于原门的 1–1.5 倍且三类加热已稳定的状态；它不会把确认任务自身作为无限递归续算来源。

若内层门与反馈稳定性通过后仍是相近的 43 层热能缺口，下一步转向冻结响应/能量方程及原方向的诊断，不能继续以“多算几张必改善”解释。单个候选失败不能否定静态大气或宣称全动态 NLTE 已不可避免。若门未过或加热重新不稳，报告实际值并重新决策，不自动重复这 8 张。

## 工期边界

实测同 allocation 的科学 map 约 130 秒，正式反馈对约 300 秒。8 张加两轮约 27 分钟纯阶段墙钟，加初始化、全态哈希和共享 I/O 等，排队以外可按约 30–45 分钟安排本次确认；更换节点或负载变化可超过这个范围，资源上限 2h 不是预计完成时间。

当前仍无被接受的这一代表柱物质步，更未有整个耦合柱/周期、径向与相位覆盖、角分辨生产强度表及 observer 接入的完成证据。已有强度工作态不是自洽大气输出。后续要得到表面向外的角分辨强度，再覆盖源定义域，检查频率/角度/深度、能量与历史依赖，最后进入 Phase 4/5A。

不能从一次映射的 130 秒推导整盘完成日期。设有效局域解数为 N、每点完整耦合和验证成本为 t，顺序总算时至少为 N*t，再加建表插值和整盘接入检查；目前 N 和 t 均未验证。例如假设 100 个独立局域点、每点 1 小时，仅求解就约 100 小时，这只是条件算例，不是本项目工期预测。先得到一个可靠自洽局域解，才能把其余范围收窄。

Mac 35 项相关测试通过；新代码仅增加 `operations/confirm_inner_threshold.py`、`.sbatch` 和测试，不改冻结数值核。部署后需 Linux 同组复测并验证实际首批块。

## 确认任务已实际起跑

2026-09-17 11:41:52 提交作业 **71765**，已在 anode17 RUNNING，32 CPU/128G、16 worker。提交代码 `949d01e`；Linux 同组 35 项测试也已通过。2026-09-17T11:43:45.274408+08:00 已完成真实 native 物质注入与原相位/dt 核验、辐射初值复制与哈希校验，首张提交 16 个块。原始回执见 `handoff/evidence/ustc-confirm-start-20260917.json`。

监督会话 `tde-confirm-watch` 与目录 `outputs/hpc/codex-confirm-watch-20260917` 已工作；本地 30 分钟跟进已切换到 71765，保留“结束后立即诊断并执行有界下一项”的要求。两小时是资源上限。

## 默认 4 核的独立审计

用户补充授权默认 4 核方案也可使用。主确认任务继续占 cpu_long 的 32 CPU；新增默认 QOS、4 CPU/16G、20 分钟上限的小作业，读取三个已停止 run 的最后一轮反馈对，共六个端点。重放原布居更新与能量账本，核对原失败层数；比较旧气体热能、辐射能量增量和电离能变化，并输出候选总物质能量减去旧能量和辐射增量的代数差值。

该差值在冻结响应无法反解正温度时仍可计算，便于审计求解路径；它只是能量方程分量，不是替代原完整物质残差、噪声门或验收规则，更不证明存在物理解。不同候选的内层精度历史不同，不能把差值变化冒充合格 Jacobian。不读取活动辐射态，不写旧 run，不改变主任务。

新增 `operations/response_energy_audit.py` 与 `.sbatch`；输出独立目录 `outputs/hpc/response-energy-audit-20260917`。Mac 原账本与新增代数差值/拒绝域相关 12 项测试通过。

默认审计实际作业 **71768** 在 anode16 运行，2026-09-17 11:47:30–11:47:43 COMPLETED / 0:0，共 13 秒；与主任务 71765 同时运行。Mac 与 Linux 各 12 项相关测试通过。原始调度终态已留在审计目录 `scheduler.json`，聚合结果与源哈希在 `handoff/evidence/ustc-response-energy-audit-20260917.json`。

六个端点均复现原账本的失败层数，总能量拆分自检误差为零。最新 0.03125 final 最差 cell106 的辐射能量增量/旧气体热能为 -3.0660815，电离能变化/旧气体热能仅 -1.75e-10，剩余热能比 -2.0660815。该单元的原能量方程代数差值/旧气体热能为 +3.0438429，有限但显著非零。0.0625 最差单元在 cell64，不能将不同最差单元比值直接当作同一单元的导数。两种候选的内层精度也不同，不能从本次只读审计构造合格 Jacobian。

这些结果把当前负热能定位为冻结反馈下冷却增量超出旧气体热能，而非电离能修正或能量拆分的数值差异；尚未证明这个反馈是最终内层解，也未证明非线性方程没有物理根。若 71765 的合格内层仍给相同结果，优先审计通过冻结响应反解目标温度的求解路径与直接守恒方程残差之间的关系，不能再凭“更小步长必然更安全”盲目回溯。
