# 77371完整审计：窗口漂移减小，但仍未满足稳定性门

## 实际完成与科学结论

77371于21:09:27—22:57:37运行，1小时48分10秒，COMPLETED/0:0，32CPU/128GiB、16worker。
完成11map、pair03与pair11；没有新增物质接受，接受步仍20。终态由只读监督捕获，非退出前状态替代。

两轮原七门全部通过。pair11原子/直接/formal加热相邻变化均约4.75799016e-5；
previous/final最低气体热能分别为6.980420155849643e12、6.983172347396547e12 erg/g。
这些是正气体热能与相邻反馈检查，不代表新物质方向能使耦合残差收缩。

pair11相对pair03是真正的外推后八map窗口。完整128×4响应向量四种端点组合差，
除原r20对应范数，最坏值如下：

| 范数 | 八map最坏比值 | 门 | 结果 |
|---|---:|---:|---|
| L2 | 0.0023531056029001992 | 0.001 | 不通过 |
| 质量加权 | 0.0070800207645173785 | 0.001 | 不通过 |
| 最大单元 | 0.001819204657235552 | 0.001 | 不通过 |

质量加权数值相较77299末窗口0.019134462570106685小约63%，但初始辐射态不同，
这不是同初值配对因果试验，不能将63%推广为算法普适收益或真解误差减少。
pair03的14.57%是外推前后响应位移，不与此0.708%拼成一个收敛率。

map11全局残差3.1155349493379027e-6，边界L1 2.4591371731318305e-6、bol2.4580589180413857e-6，
block74自身变化仍15.546%。相邻响应差L2从pair03的0.0025690318674833185减至0.0018069929807949902，
final与r20的距离却增至0.8067122999714628；比较对象不同，并不矛盾。原r20保持不换。

## 独立审计与限制

完整包`complete-1790348222232820529.tar.gz`，151077043字节，
SHA256 `d8966055fb1fb702579e23e504f16f5e9156fb72b808d26bf5cd5952138ee3b8`。
Mac原包在`outputs/review-20260925/`，原字节解包到`step21-wide-validation-77371-complete-received`。
审计3537文件、688代码哈希声明、836 map进程回执、304反馈进程回执；最大4040144KiB，低于6GiB门。
源身份、频组完整性、非负/有限值、原门、响应/能量账本及独立fsum四组合三范数归约均通过复核。
审计通过意为准确重现判决，**不是把未过的窗口判为通过**。

证据`handoff/evidence/20260925-wide-validation-complete-review.json/csv/png`及
`20260925-wide-validation-77371-terminal.json`。图已查看；右图连接的是两种不同比较，
横轴明确标了外推位移与八map漂移，不能把连线斜率解释成时间收敛率。
Mac未重积分物质ODE、未重读大型辐射态；缺逐片平方和，不宣称全场L2独立重算。

## 有界下一步

新增`operations/scan_step21_restarted_plane.py/.sbatch`与协议
`handoff/protocols/step21-restarted-plane-v1.md`，只用77371保留的早晚历史。
两套输入/实算输出为(1,9,10)/(2,10,11)、(2,9,10)/(3,10,11)，所有索引指辐射态。
原六态流式代数直接复用，不修改旧脚本；原始trial/x20/r20/物理旧层和整个源归档继续核验。
默认4CPU/16GiB，1小时上限；两组各6遍、4096约束，0map/0反馈/0大型候选/0接受。
收益对本次map11归一化，严格超过20%才考虑另登记32CPU实算协议。
若没有合格预测，停止这两基组策略，不自动再试相近基组或堆普通map。
独立审计入口`handoff/audit_tools/review_step21_restarted_plane.py`已按新来源与配对定义准备，
尚未获得新扫描真实工件，不能提前声称其端到端通过。

Mac新增身份/判决拒绝路径测试及复用代数测试38 passed（0.87s），batch语法通过。
运行前还须学校干净版本测试；大输入SHA/实际来源预检由allocation完成，登录节点不流式扫描大场。
这次预测不会关闭耦合问题或产出整盘I_nu，不能给总完成日期，也不能推出模型无解。

备份Mac `outputs/review-20260925/pre-wide-final-audit.bundle`、`automation-before-wide-final-audit.toml`，
学校`/home/scc/pb24511938/pre-wide-final-audit.bundle`。旧数值代码、协议、工件均保留。
讲义CLI草稿已纠正“剔除r20”“气体热能正证明方向正确”“两类距离矛盾”等错误；不能照用模型摘要作科学判决。

## 收尾：SSH通道失去响应，新任务未提交

代码与审计提交`945a344`已在Mac和GitHub；向学校发送增量bundle时通道停止响应，
随后学校验证命令也无输出。独立`ssh ... true`在20秒超时；两个等待中的本地scp/ssh操作已中断。
无法确认远端是否收到完整bundle、是否已快进或执行测试，不能报告三端同步或Linux测试通过。
没有执行任何sbatch命令，新4核扫描尚未提交。原77371已取得COMPLETED终态和完整独立审计，不受影响。

恢复后先检查学校工作树/HEAD、bundle完整性及队列；必要时重传
`outputs/review-20260925/restarted-plane-code.bundle`（基于facc518）。
确认同一版本，跑三个scan测试文件和batch语法检查后，才提交新命名扫描。
禁止在传输状态不明时假定已启动；自动跟进暂停，待用户确认重新连接后恢复。
