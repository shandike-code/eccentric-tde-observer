# Linux 运行报告：block-check 第二次尝试（Slurm 62585）—— 全部通过

重跑 `hpc/check_block.py`（第一次 `62576` 因墙钟不足被 SIGUSR1 杀死，见
`2026-09-14-block-check-62576.md`）。**本次辐射对照与 H/He 反馈块均完成并通过。**

## 1. 标识与实测资源

| 项目 | 值 |
|---|---|
| Git commit | `c4d735e5dc39100396408993f293de10f7b2ddc6` |
| Slurm job ID | `62585`（`tde-block-check`） |
| 分区 / QoS | `Students` / `qos_stu_default` |
| 分配 | 4 CPU、16 GiB、1 节点 `anode01` |
| 墙钟 | 申请 `01:00:00`；实测 `RunTime=00:17:57`（USR1 推迟至 50:00，余量充足） |
| 终态 | `JobState=COMPLETED`、`ExitCode=0:0` |
| run 目录 | `outputs/hpc/block-check-2`（按 `hpc/README.md:135` 新建目录重做） |
| `config_sha256` | `480b9a37ccbe3327bd610fa03a17a734c49f12480d0298f0d0c0e7ccc4cb95b8` |

**未修改任何源码、容差或配置**；`tracked_worktree_dirty` 与 62576 时相同。

## 2. 时间线

| 阶段 | 完成时刻 | 耗时 |
|---|---|---|
| 提交 / 启动 | 17:08:47 / 17:08:48 | — |
| initialize（Planck 种子写入 `input.dat`） | 17:08:55 | ~7 s |
| input hashing（10 GB 稀疏） | — | ~11 s |
| **map worker（128×32×4096 块）** | 17:14:05 | **301.86 s** |
| reference worker（legacy phase7b9du，第 9 次 Picard） | 17:18:44 | **242.85 s** |
| 辐射对照写入 `block_check.json` | 17:18:44 后 | ✅ 通过 |
| **H/He 反馈块** | 17:26:43 | **458.03 s** |
| 作业结束 | 17:26:45 | 合计 `00:17:57` |

## 3. 核心结果：跨平台辐射一致性（门 `1e-11`）

| 指标 | 值 |
|---|---|
| `bitwise_equal` | `false` |
| `maximum_absolute_difference` | `1.6940658945086007e-20` |
| `global_relative_difference` | **`1.1592029676575186e-15`**（≈ 5×机器精度 eps） |
| `comparison_tolerance` | `1e-11` |
| **`radiation_adapter_passed`** | **`true`** ✅ |

与第一次 `62576` 的辐射对照**结果完全一致**（同一 `global_relative_difference`），说明这是可重复的舍入差异而非偶发噪声。底层诊断量逐位吻合：

| 量 | 新 worker | reference worker |
|---|---|---|
| `maximum_absolute_radiation_change` | `4.332406597471013e-06` | 逐位相同 |
| `block_relative_radiation_change` | `0.28295686602642756` | 逐位相同 |
| `boundary_spectrum_l1_numerator` | `23200301.86492975` | `23200301.86492976`（1 ULP） |
| `mapped_boundary_absolute_scale` | `139828318.7164526` | `139828318.71645263`（1 ULP） |
| `minimum_mapped_intensity` | `4.1599025576277925e-08` | 逐位相同 |

`material_migration`：`encoded_state_changed: false`、`physical_kernel_changed: false`，
温度 / 氢 / 氦相对变化 `2.59e-16` / `0.0` / `6.94e-18`。

**另有一条未被要求但值得记录的再现性证据：** 两次独立作业（`62576`、`62585`）的
初始化输出 `input.dat` SHA-256 **同为** `8f6d7149c4560405d52fa547fc7cb179259079f338cf2b66ea19dc39fccb492e`
（各 10,099,884,032 bytes）。即 cold Planck 种子在 Linux 上跨作业逐位可再现。

### 关于两次 `protocol_sha256` 不同的说明

`block_check.json` 中 `new_worker.protocol_sha256` 两次不同（`bcebf355…` vs `480b9a37…`）。
已核实这是**路径差异而非代码变更**：`hpc/pipeline.py:167` 将 `new_worker` 的
`EXPECTED_PROTOCOL_SHA256` 设为**该 run 的 `config.json` 文件哈希**，而 `config.json`
内嵌 `"run": "outputs/hpc/block-check-2"`（`pipeline.py:102`）。已验证
`mapped.json.protocol_sha256` 恰等于 `sha256(outputs/hpc/block-check-2/config.json)`。
`reference_worker` 的 `protocol_sha256` 是历史固定常量 `51a670f5…`，两次完全相同 ——
旧 runner 未受任何影响。

## 4. H/He 反馈块：第一次未跑完，本次通过

| 指标 | 值 |
|---|---|
| `feedback_adapter_finite` | **`true`** ✅ |
| `feedback_block_report.runtime_s` | **458.03 s** |
| `feedback_block_report.peak_process_rss_mib` | **3512.60 MiB** |
| `minimum_owned_comoving_mean_intensity` | `2.3816599183550505e-06`（正值） |
| `partial_sha256` | `fff87a1c0b6576aed7e931d3f9f5fdb72727322e130e74dcacd1d5fff7d74b21` |
| `full_column_or_material_acceptance_tested` | `false` |

按 `check_block.py:79-95` 的设计，这里只调用 `run_worker_adapter` 在合成端点上跑
**真实微观物理**；**不是**一对独立收敛的反馈对，也不构成全柱或物质接受判定。

## 5. 性能对照：Linux 比 Mac 慢约 58–74 倍

| 指标 | Mac M4 | 学校 Linux | 比值 |
|---|---|---|---|
| map worker 墙钟 | 5.37 s | 301.86 s | **56.2×** |
| reference worker 墙钟 | — | 242.85 s | — |
| H/He 反馈 worker 墙钟 | 6.18 s | **458.03 s** | **74.1×** |
| map worker 峰值 RSS | 2449 MiB | 2267.18 MiB | 0.93× |
| H/He 反馈 worker 峰值 RSS | 2955 MiB | 3512.60 MiB | 1.19× |

**结论：RSS 与 Mac 同量级（内存不是瓶颈），墙钟慢 56–74 倍。** 尚未分离共享盘 I/O
（`nexdata_p300s` NFS）与 BLAS 差异各自的贡献。

**规划影响（阶段 A 完整映射）：** 一次完整映射 = 76 个频率块（`pipeline.py:186`）。
按本块实测 `301.86 s / 128 组`、2 worker 外推：`75×302/2 ≈ 3.15 h`，而
`qos_stu_default` 的 QoS 墙钟上限为 **4:00:00**（实测 `sacctmgr show qos`）。
余量很薄，且 `pipeline.py:182-184` 每批都会重算全部已提交块的 SHA，存在随块数增长的
重复 I/O 开销。已据此把 benchmark 墙钟从默认 `03:30:00` 提到 QoS 上限 `04:00:00`
（作业 `62643`）；流水线按块批边界可恢复，超时不会污染状态。

## 6. 平台问题

- **`sacct` / `sstat` 对本集群所有作业返回空**（本次复核：`sacct -j 62585`、`-j 62576`、
  `-S 2026-09-14`、`-a` 全部无输出且 rc=0；`sacctmgr list cluster` 仅见集群名 `training`，
  无 job 记录；`AccountingStorageType=accounting_storage/slurmdbd` 且
  `AccountingStorageEnforce=associations,limits,qos` 已启用）。**这直接影响
  `hpc/supervise.py`：它以 `sacct` 判定作业终态，在本平台会 10 次轮询后抛
  `RuntimeError` 停止自动续交。** 备用通道：`scontrol show job <id>` 在 `MinJobAge=300`
  秒内仍返回 `JobState`/`ExitCode`。详见独立记录。
- 稀疏文件逻辑大小 29 GiB（3×10,099,884,032 bytes）/ 实际占用约 385 MiB。本平台无
  `quota` 命令；`df` 显示 `/home` 为 983 T 共享盘，905 T 可用 —— 不是个人配额。

## 7. 这轮证明了什么 / 没证明什么 / 下一步

**证明了：**

- 辐射数值核 Mac→Linux 迁移一致（`1.16e-15`，门 `1e-11`），且**可重复**（两次独立作业同值）。
- 初始化 Planck 种子在 Linux 上跨作业**逐位可再现**。
- H/He 反馈适配器在本平台可跑完且输出有限、正值；物质迁移无编码/物理核改变。
- 单块实测性能与 RSS，可作为完整映射的成本外推依据。

**没有证明：**

- 完整柱收敛、全频反馈闭合、物质试步判定（`full_column_or_material_acceptance_tested: false`）。
- 2 worker 并行的实际扩展性 —— 本块是单 worker 实测，线性外推未经验证。

**下一条命令：** 等待 pipeline benchmark `62643`（`outputs/hpc/hhe-r025`，2 worker，
门槛 `2.5e-4`，cold 种子，`TDE_MAPS=1`），读取 `state.json` 的 `history[0]` 残差与
`maximum_worker_rss_mib`。
