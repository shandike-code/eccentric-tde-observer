# 23:37巡检：跨历史全场预测成立，转入真实反馈和多方向诊断

73581和73582均complete、队列空、stderr空。73581完成一张真实map，五项数值验证通过；73582只读完成，前后原四态SHA不变。没有新物质接受。

## 全场验证

- 实际残差与预测均为7.657069076118395e-5；边界L1 7.9066151479e-5，bolometric变化1.9004878238e-5。
- 实际map墙钟132.05s，worker峰值3501.82MiB，完整频率所有权、非负和有限性检查通过。
- 实际输出减预测输出的全场相对L2为1.6880817403e-16，最大误差/全场强度峰值为1.2526562865e-15。
- 预测误差L2/实际映射改变量L2为4.3944644570e-12，远小于本轮声明的0.01；边界预测L1误差1.2004953939e-16。

因此这一个跨历史组合的仿射预测在数值精度内成立，已经超越仅比较标量残差的检验。但它不证明所有方向都线性，也未计算这个候选的正式物质反馈。改善仍只有约1.21%，不把它称作慢模已经解决。

候选SHA为`9500d88fd1c5a84ba0e1b37fec8d49987bc50a2dac29bb89cb91bd9596aa4fa6`；真实输出`cross-history-map-validation-20260919/state_1.dat`的SHA为`fe38c6646c78d6ab8d190449c76bbe43d4e0ee5b97a87237ff342c75de71e6ac`。

## 正性限制的定位

输入预测中12785284个辐射网格元素、映射预测中12781674个元素满足base恰为0而old大于0，全部位于0起算频块72–74；这不是128个物质层的热能失败计数。前者old强度最大8.13537e-263，后者9.26623e-263，约为全场强度峰值的2.10e-261和2.39e-261；其中2635014/2647668个old值是subnormal。

最大样本在频率索引9342，频率边界7.82934e17至7.84108e17 Hz，角索引18/13及对称深度附近。它们确实使正gamma产生负值，但没有截断本轮原始最优负gamma。不能由极小强度推出净加热误差已可忽略，也不能从零值单独判断其成因；不删除、不裁剪、不加floor。

## 双端保全

| 小包 | bytes / 文件数 | SHA256 |
|---|---|---|
| cross-validation-73581-small.tar.gz | 810836 / 96 | 472583b8a14671c30b3fb90242fcbbf43459c5aee53a3686eccf7a5fb1b6aba0 |
| cross-validation-73582-small.tar.gz | 646952 / 12 | 6c7129bc3ecc8dc50830c7844e6f1c3a14af8f2e97ab0ec8c90882afafa994b3 |

学校家目录和Mac `outputs/review-20260919/`各存一份；Mac核整包及逐文件SHA。排除dat/lock/partial，原大态保留。Git证据为`20260919-cross-validation_result.json`、`20260919-cross-obstructions.json`。

## 两项下一步及科学边界

**32CPU/128GiB、16worker、60min：真实反馈。** 新`prepare_cross_feedback.py`/`cross_feedback_precision.sbatch`从上述已验证真实输出起步，先复制全同物质trial并核native，最多2张新map、每2张一轮正式反馈和能量账本，无自动续交。入口必须核full-field check，不能仅凭标量validation标志起跑；新history为空，不拼接旧map。所有物理量和严格1e-4门不变，alpha仍0.00390625。这个任务检查真实加热与物质响应，不假定它们随强度仿射变化。

**默认4CPU/16GiB、30min：只读多方向扫描。** 新`scan_multihistory_subspace.py/.sbatch`用两条已停止历史各两个映射，共四个输入/输出对、六个原态。以已验证凸混合为正起点，在三个差残差方向中最小化全场未加权预测残差平方L2；直接累加差向量的Gram和右端。按1e-12相对特征值阈值只解可分辨PSD方向，报告特征值、保留秩和原系数，不把截断病态代数方向误作修正物理强度。

沿起点至原最小二乘组合，只允许统一步长0–1；全场候选和预测映射正性、系数L1<=32共同限制步长。不修改任何单元。最后独立核预测最大残差比现有7.65707e-5改善至少1%、边界两门<1e-3、非负有限；任何失败完整保留。候选表达式固定为`anchor_field+t*(raw_combination-anchor_field)`，未来写入必须使用同一运算顺序，不能随意换成重排后的权重和。

扫描写候选预算0、新map预算0。即使通过，也需要另声明完整原算子验证及全场误差检查，不能据预测接受物质步。两项独立，无相互依赖，不机械重复约1%收益的单方向混合。

变更前Git保全`pre-multidirection-44f760b.bundle`。Mac34项相关测试通过，包含已知三模仿射固定点、退化/非法Gram、负原组合的统一回溯、原字节不变、拒绝仅标量种子及既有物质身份/映射血缘检查。当前尚无被接受的物质步或自洽柱。
