# 重连后：73929完成，但同物质的两条辐射历史仍不一致

## 现场和正式判决

用户确认重连后，SSH实测成功。73929在断线期间正常完成8map/2round，状态`diagnostic_round_complete`，无待完成反馈，队列为空。定时任务已恢复ACTIVE并回读确认；没有重复提交，也未取消Slurm作业。

最后两张map残差9.358014006e-5、8.765974774e-5，墙钟157.04/145.49秒；最后边界谱L1为1.294225964e-4、bolometric为5.105478437e-5。末轮反馈313.79秒。第二轮atomic/direct/formal heating分别约0.000492357943/0.000492357943/0.000492357436，均通过；噪声/信号0.01094383710，photoion最大2.292978778e-5、recomb最大4.261999213e-7，均通过。

只有物质L2、质量加权收缩门失败：比值1.004188258663、1.080676565897；最差单元比0.997937715236通过。仍没有接受物质步。第一轮尚有三个加热门和辐射门失败，不能把第二轮结果套到第一轮。

四端点均0/128非正目标气体热能，四份反馈NPZ全部数值数组有限。末轮final最低绝对热能在cell63，9.338853746e12 erg/g；相对旧气体热能最小在cell64，比值1.012393869。不能把这两种最差层混用。原适配器仍有已知绘图布局警告，不把旧图当已通过排版审核的结果。

最新辐射state2 SHA `9e3bae04ee804c46b6d54fc5271488f80060a638bbf0b823bd4c728137f3399e`，尚未作为反馈输入求值；正式末对使用map7/8的state0/state1输入，不能替换为最后输出。

## 保存的真实残差向量比较

核两条链的完整trial所有字段逐位相同，物理旧时间层与基态残差声明相同；各自最新反馈pair、协议SHA、物质SHA和正式残差文件SHA均一致。用同一128层质量测度重算已保存final残差范数，复现正式三个比值（相对容差1e-12）。没有重新求物质响应，previous残差尚未重放；这是保存的final向量比较，不能宣称全反馈重放已完成。

| 范数 | 73929基态初猜 | 73888原历史 | 两final向量之差 |
|---|---:|---:|---:|
| L2 | 15.654153704 | 15.742513674 | 3.836479051 |
| 质量加权 | 0.664923543 | 0.661722561 | 0.485031056 |
| 最差单元 | 3.468919187 | 3.472119879 | 0.772178026 |

表中第三列是向量相减后取范数，不是前两列标量相减。同一物质的两条链虽都通过正式内层、加热与噪声门，最终物质向量仍明显不同。因此现有相邻稳定门尚不能排除辐射初猜/历史的影响。这不是多解或无解证明，也不支持把半步/原步有限差商直接解释为可信Jacobian。

下一步优先处理固定物质下的内层历史差异，不继续减半物质幅度。可在默认4核上做只读同平台反馈重放及逐层率/加热/热能差异诊断，比较各链相邻漂移和跨历史向量差；如设计固定物质下的辐射历史组合，应沿用已验证的正性、系数幅度、完整频块所有权与真实原算子验证流程，不能把预测组合当解。所有后续新map须单独声明有限预算，不机械续旧run。

## 保全

`half-base-seed-73929-small.tar.gz`在学校家目录与Mac `outputs/review-20260920/`双端保存，83250412 bytes、1878文件，SHA256 `7df738fe3d86a2ce49f39d8fa4ffee532a04f53d40cb5cfb9005b74977fd7dd1`。整包及全部文件SHA已核，提取到独立`half-base-seed-73929-received`；原dat未改。Git保存state、两正式summary与`20260920-73929-stored-residual-comparison.json`，后者明确标记未重放previous及无严格内层误差界。
