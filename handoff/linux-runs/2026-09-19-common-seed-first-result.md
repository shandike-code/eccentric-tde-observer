# 共同初值最小候选完成：物理域恢复，但反馈远未稳定

2026-09-19 11:17 起巡检确认默认作业 73238 的控制状态 complete；cpu_long 73241 仍在运行。本报告只判读已完成的 73238，不提前推断另一个候选。

## 数据保全与完整性

`common-seed-a00390625-20260919` 的完整小工件、stdout/stderr 已打包，排除 dat 与 lock。学校 `/home/scc/pb24511938/common-seed-73238-small.tar.gz` 与 Mac `outputs/review-20260919/common-seed-73238-small.tar.gz` SHA256 均为 `710d406a474b1fb21be5570e2e77c379f9e479d84974bbd3349311635d080325`。本地逐项核对 557 个输入快照和两个反馈 NPZ 哈希，均一致。

`scontrol` 的作业记录已过期，未补造退出码。完整结果和两个反馈 manifest 的状态门通过支持程序计算完成；不等同于整个反馈对稳定或物质步接受。

## 数值及判决

物质 alpha=0.00390625，与旧最小候选全部物质字段相同；输入辐射改为已验证的历史基态。两个 map 分别耗时 597.32、598.12 s，输入算子残差分别为 3.190967e-4、2.852550e-4，均高于候选 2.5e-4 门。

| 项目 | 初始辐射上的反馈 | 一次映射后的反馈 |
|---|---:|---:|
| 非正目标热能单元 | 0/128 | 0/128 |
| 最小目标/旧气体热能 | 0.16027123 | 0.99693959 |
| 相对当前 Linux 基态的方程 L2 比 | 1.02252890 | 0.99225159 |
| 质量加权 L2 比 | 1.03494335 | 0.99956173 |
| 最差单元范数比 | 1.00643401 | 0.99360177 |

分母取已验证零位移实验的 previous 端点；分子分母使用相同固定尺度方程残差。final 的三个诊断比值虽均略低于 1，却不能接受：当前两个反馈之间的加热指标约 **0.191738**（门 1e-3）、最大电离率分量变化 **0.00144111**（门 1e-3），均失败。复合率最大变化约 1.7521e-6。相邻方程向量 L2 差为 **9.203675**，所以暂态足以影响“略下降”的解释。

旧同物质路径最后有 55 个非正目标热能单元，当前短程两端点均为零。已直接排除“该候选物质在所有辐射输入下必产生这 55 个失败”的强命题；没有排除其充分收敛后的响应仍会失败。不同初值、迭代数及内层误差未控制，不能宣称多个固定点、找到正确 warm 解或证伪旧方向。下一步须结合相邻候选结果，再建立同候选不同辐射历史的有界精度比较，不采用“正热能即过门”的捷径。

## cpu_long 性能监测

73241 在 11:17 左右运行约 36 分钟，首张提交 32/76 块，stderr 为空。`sstat` 记录 batch MaxRSS 约 32,118,652 KiB；不能把此统计字段无条件解释为所有进程瞬时 RSS 总和。块 0 墙钟 587.39 s、峰值 2266.84 MiB；块 31 为 513.04 s、3258.55 MiB，均未触及 6144 MiB 单 worker 门。默认作业不同候选的块 0 约 12.01 s，显示当前高并发作业没有重现历史加速，但不是同候选、同节点的受控测速。

anode01 当时 CPUAlloc=102/128、CPULoad=25.42，FreeMem 约 66 GB；共享节点和频块/候选/执行条件都可能影响速度。现有数据不能单独判为 OOM、CPU 争抢或内存带宽瓶颈。不在运行中改并发或配置，不直接申请重复计算；保留 3.5 h 原预算，继续观察完成/故障，必要时另做冻结输入性能诊断。

## POST-RUN CHECK

哈希、native 身份及两态正式状态门通过，方程向量有限性检查通过；没有删失败单元或增加能量。当前目标热能均正，但辐射、加热稳定和一个电离率分量门未过，接受状态 False。没有生成新图。短程反馈强烈变化与“尚有内层暂态”的解释相容，但这不是对最终固定点的预测。
