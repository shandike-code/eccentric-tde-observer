# 固定物质控制窗口完成：漂移未过门，转入较长跨度的只读外推诊断

## 结论与证据

作业77299于2026-09-25 18:48:13—20:04:11运行，墙钟01:15:58，COMPLETED 0:0，stderr为空。
32CPU/128GiB、16worker/anode03。完成8张新map，加上77264继承8张，实验总计16张、两个正式反馈对。
接受物质步仍20，新接受0；没有半成品映射，没有更换原$r_{20}$。

完整归档`complete-1790337819331948199.tar.gz`，150265710字节，
SHA256 `b1697de8f74f12bd1784844f009a62e3511fbb52d6c14677d50ec43399aec826`。
Mac保存于`outputs/review-20260925/`，独立解包目录`step21-control-recovery-77299-complete-received`。
审计`handoff/evidence/20260925-control-recovery-complete-review.json/png`，终态`20260925-control-recovery-77299-terminal.json`。

独立审计4296文件、686代码哈希声明、304反馈进程回执、1216映射块进程回执。
确认完整输入/代码血缘、频率归属、反馈替换汇总、深度映射、能量锚、编码分量和原门。
窗口归约另用math.fsum从响应向量计算，未复用生产窗口函数。Mac没有重积分大型辐射场或物质ODE。
最高进程RSS 4042292KiB，低于6GiB门；无非有限量/物理域失败，图已目视检查。

## 数值判决

窗口定义不变：两个反馈对各有previous/final，取四种端点组合；对完整128×4响应向量差计算
L2、质量加权、最大单元范数，分别除以冻结原$r_{20}$的同种范数。下表是每列四组合的最坏值，
不是范数之差，也不是每层自身相对误差。三列全部严格低于0.001才通过。

| 窗口 | L2 | 质量加权 | 最大单元 | 窗口门 |
|---|---:|---:|---:|---|
| 77126末反馈到map8 | 0.00580129710138 | 0.0206627935569 | 0.00271109799876 | 不通过 |
| map8到map16 | 0.00522152669405 | 0.0191344625701 | 0.00258088116333 | 不通过 |

质量加权漂移只减少约7.4%，第二窗口仍是门槛的19.13倍。每轮相邻零控制原7门均通过；
第二轮atomic加热差0.000137649285146、最小气体热能6.991171339760285e12 erg/g。
第二轮最终响应三范数8.221754205335307 / 0.26698231122592647 / 2.7057203539203707。
相邻响应差L2由0.00497523403554降至0.00452720466105，而相对原$r_{20}$的差L2由0.545705793635增至0.566171142179。
这是参考距离与步间变化的不同，不足以宣称发散，也都不是对未知精确解的误差界。

全局辐射残差map1到16由4.9310322221475354e-6降至4.63015506865246e-6；
block74自身尺度变化仍从15.9477%降至15.7444%。保留弱尾，不证明它导致物质漂移。
边界L1从3.3593e-6降至2.4037e-6；bolometric变化先接近零后回升至9.1386e-7，
记录这个非单调行为，不能选择只有单调的指标证明全态收敛。

## 下一步决策和预算

不在原目录无界追加映射，不以新控制响应替换原$r_{20}$来救旧物质候选。
现有相邻历史正性外推预测收益仅约2%；先在已保留较长跨度历史上检查是否有更有用的正性初猜。
新增`operations/scan_step21_wide_plane.py/.sbatch`及协议`handoff/protocols/step21-wide-plane-v1.md`。
固定物质下两套六态映射配对，二维残差差分Gram、逐点正性约束；不存在的中间态不假造。

4CPU/16GiB/default/1小时；两组各最多6遍全场扫描、4096约束、进程RSS<6GiB。
原预测科学门不变，另预注册严格20%残差收益的算力筛选。这里只读，无候选大态写入、无新map、无反馈或接受。
若有候选，独立审计后另注册32CPU真实映射与反馈；没有候选则保留诊断失败，不扩大为模型无解。
当前尚不能给整盘自洽强度表完成日期。

## 运行前验证与备份

Mac新增和复用测试32 passed（0.87秒）：非连续映射配对/错配拒绝、解析两模仿射算子、正规方程、
小型流式Gram对照、六态负值检查、subnormal约束、重复流读取、病态Gram、成本与科学门分离，
以及旧二维约束器的有限预算和负路径。编译与sbatch语法通过。Linux验证和启动证据另存，不预写成功。

Mac备份`outputs/review-20260925/pre-control-window-final-audit.bundle`和`automation-before-control-window-final-audit.toml`；
学校`/home/scc/pb24511938/pre-control-window-final-audit.bundle`。旧77264/77299工件、代码与协议均未改。
完整结果核查图没有数值伪迹；强度不是发射率，单柱控制诊断不是全域大气或整盘观测谱。

## 已启动

代码提交`bf7b8708873fc775d7e7013e1aedecc6f0605357`已同步Mac/学校/GitHub。
Linux同32项测试通过（19.43秒）。学校实际两组配对、固定物质身份声明一致、全部大态大小通过预检；
完整SHA仍由allocation执行，不把文件大小预检称为完整校验。

作业77366于20:19:18启动，anode04，qos_stu_default，4CPU/16GiB、1小时硬上限21:19:18。
run为`outputs/hpc/step21-wide-plane-scan-20260925`。启动证据`handoff/evidence/20260925-wide-plane-77366-start.json`。
只读平台监督工具`handoff/audit_tools/watch_readonly_scan.py`只记录调度/状态摘要；Claude只在启动和终态作无工具审阅，
不会提交/取消/重试、改数值代码或科学门。监督预算90分钟，独立于数值作业；故障显式留档，不将模型审阅视为独立数值审计。
平台Claude位于`/home/scc/pb24511938/opt/node-v22.18.0-linux-x64/bin/claude`，需同目录node加入本次进程PATH。
