# 73863减半试验结果与同候选内层精度复核

## 已完成什么

2026-09-20 12:55 CST巡检：73863已完成4张map和两轮正式反馈，队列空，状态`diagnostic_round_complete`，无active map或pending feedback。新候选确为0.001953125：Linux原始候选精确解码、试验编码身份、native温度/布居镜像和物理相位/时间步检查全过，物质SHA为`07a3700d8767f5bd7addf7b2932d3b44742e803e2aed52c83e2e86b82ce07b86`。因此上次Mac逐位检查失败没有导致跳过Linux守卫。

| map | 辐射残差 | 墙钟秒 |
|---|---:|---:|
| 1 | 1.624918518e-4 | 142.02 |
| 2 | 1.475034929e-4 | 132.52 |
| 3 | 1.363259272e-4 | 141.05 |
| 4 | 1.266811775e-4 | 147.97 |

最后边界谱L1为6.953840718e-5、bolometric为6.043348877e-5。峰值worker RSS约3501.40MiB。两轮反馈319.90/310.13秒。

| 正式指标 | map1/2反馈 | map3/4反馈 | 门 |
|---|---:|---:|---|
| atomic heating相邻变化 | 0.1003741837 | 0.002729606153 | 小于1e-3 |
| 内层噪声/试步信号 | 1.334965082 | 0.04387484817 | 小于0.1 |
| candidate/base L2 | 1.011474091 | 1.015916224 | 收缩 |
| candidate/base质量加权 | 1.096155158 | 1.132086174 | 收缩 |
| candidate/base最差单元 | 0.9985893398 | 0.9989600489 | 收缩 |

第二轮photoion分量最大3.811451314e-5，recomb分量最大5.031234031e-7，通过率稳定门。第二轮direct/formal heating分别0.002729606153、0.002729605253，均未过。第一轮7个门失败，第二轮6个失败：三个加热门、内层辐射门、L2和质量收缩门；第二轮噪声门已通过。两轮均未接受物质步。

四份NPZ的所有数值数组有限。四个端点均0/128非正目标气体热能。第二轮最差均cell114，剩余气体热能previous/final为6.310840525e12/6.468731117e12 erg/g，分别是该层旧热能的0.54611385/0.55977704。第一轮previous最低绝对热能位于cell63，但最差相对比例在cell64；final最差在cell114。不能由单一最小值宣称全场变化单调。

辐射残差下降、加熱相邻变化下降，物质L2和质量加权比却上升。这反映这些量测不同对象，不能选其中改善的一项宣称物质收敛。因为内层残差与加热门尚未通过，也不能据当前结果断言更小物质位移一定更差或原方向精确不下降。

## 图像与警告

stderr有原适配器的`constrained_layout`警告。已实际打开第二轮`feedback.png`：上下图文字重叠，失败门列表超出右边界，图例遗留`converged state`而本轮内层门未过。此图不能作为可发布结果或收敛证据；冻结旧图/旧脚本不修改，判决读取完整JSON。没有把绘图警告当成数值发散，也没有把退出成功当科学通过。

## 下一步及为什么给8张预算

冻结此run，创建`outputs/hpc/half-step-precision-20260920`，只复制同一个0.001953125物质文件，全部字段逐位不变。初猜使用原run最新state1，SHA为`c9d72f3303a51706c1d66fba3ac6280845c6cb4eabbcb24b89de54070d6bd066`；正式第二轮反馈实际使用state2/state0输入，最新state1不能冒充已经反馈的态。

本次新声明最多8张map，每4张反馈一次、至多两对，32CPU/128GiB/16worker/60分钟。上一批最后残差仍比严格门高26.7%，单纯换物质不能解决该候选内部精度不足；给两个四map区间，是为了观察接近严格门时反馈和三范数如何变化，减少每两map就做一次昂贵反馈的次数。此预算不保证过门，不按四个点承诺收敛时间，不自动追加。旧run的4张预算及结果不改。

新的`prepare_half_step_precision.py`核source恰为4map/2round，最后反馈对应map3/4输入、summary/protocol/trial身份一致、完整16门恰为本次六失败模式，再复制全部trial字段并执行native输入检查。初始化前trial必须已存在，初始化后再次核字段和种子，严格1e-4、物理dt、密度、方向、能量定义、科学接受门不变。不重解码迁移旧候选，不clip/floor。

若任一轮正式接受则停止审阅，不直接称耦合柱收敛。若八张后仍未满足内层/加热条件，先分析实际反馈误差及辐射慢模，不默认续算第三批。若内层稳定但外层仍不收缩，则对齐基态/候选精度并审方向，不能无限减半。当前不提交额外默认4核任务，因为已有基态审计且本次新候选内层尚未稳定，无需为了占满资源重复它。

## 验证与保全

Mac相关46项测试通过，包含11个新用例：最新输出与反馈输入区分、未结算态、错误端点/协议/步幅/编码、缺门、物理域失败、已接受状态拒绝。真实73863小工件也通过新入口检查，返回的种子正是最新state1。shell语法通过。正式提交前还需Linux同组测试。

`half-step-73863-small.tar.gz`在学校家目录和Mac `outputs/review-20260920/`双存；84649129 bytes、1555文件，SHA256 `d53058a6f179ad36717b20f31bb1ec3089db965ae5a0b4cd70b42ff559407f6a`。整包及逐文件SHA全核，提取到新`half-step-73863-received`目录；dat不打包、不删除。Git存state、native audit、声明和两份正式summary。改前Git保全`pre-half-precision-320c3cf.bundle`。

仍没有接受的物质步、自洽代表柱或可用于整盘的角分辨强度。这轮不提供模型无解的证据，也不足以估计最终自洽光谱的交付日期。
