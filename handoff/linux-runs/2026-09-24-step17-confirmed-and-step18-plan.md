# 第17步独立确认及第18步计划

## 计算事实

76260在2026-09-24 00:36:47—01:47:41完成，COMPLETED 0:0，70分54秒，anode18，32CPU/128GiB，stderr空。SSH中断未终止计算。09时重连后读取终态，未重交旧作业。

归档222988430字节，SHA256 e2ded86fa0629e0cb8e73a3389cbd504db78a8ca3e2c2ea6462e5fc681b8ad05。独立审计4021文件、456反馈worker、608映射worker，校验所有文件SHA、逐块源替换和汇总/归层、能量账本、响应编码、原16门、四组合三范数、物质身份、种子连续性和双重资源门。图已查看，全部数组有限、六端点气体热能为正；深层尾部仍有残差，未剪裁或设floor。最大/proc 4041832 KiB，小于6GiB；map计算墙钟总计1074.094秒，其余时间包括散列、初始化、复制及反馈。

|阶段|加热L1（门1e-3）|噪声/信号（门0.1）|最不利L2比|最不利质量加权比|最不利单元比|
|---|---:|---:|---:|---:|---:|
|x16零控制|0.0001626966691|不适用|不适用|不适用|不适用|
|x17确认1|0.0001943358178|0.01689198029|0.9515104828|0.9815646011|0.9828816690|
|x17确认2|0.0003197459175|0.01843443109|0.9520468244|0.9834088824|0.9830421863|

最不利比值取候选previous/final相对新基态previous/final的四组合最大值。两轮确认原16门、信赖域及四组合收缩均通过，因此独立记录`20260924-accepted-step17.json`接受第17个有限非线性步。接受对象为trial x17，绝非响应目标G(x17)。旧run接受数16原样保留；新接受记录是唯一计数更新。

## 保留的不确定性

确认1→2的final残差L2从8.703314605升到8.708220436，质量加权从0.3006034124升到0.3011682221，最差单元从2.735279543升到2.735726250；加热指标也回升，仍通过原门。不能声称所有指标单调。

两轮final残差向量之差三范数为0.05373708431、0.003178624376、0.02009760893；零控制两端点之差为0.02927358356、0.001588224163、0.009789219366。它们是同物质内层漂移诊断，不是严格误差界。原子率与共同频域四力一致不等于有限频窗截断已经收敛；本项目仍只是H/He基态连续过程，不含已验证的真实谱线，更没有整盘I_nu。

## 审计失败记录及修正

第一轮新审计在“先前pair08相对新基态”的Python字典逐位相等检查处停止。逐项打印证实仅两项L2比相差2.22e-16和1.11e-16，其余十项精确一致，重算和原记录均通过。新审计将该跨平台浮点重算检查改为既有8 epsilon、atol=0后，从归档SHA开始完整重跑通过。原文件SHA、编码向量、来源身份及学校科学门保持精确。未覆盖第一轮失败日志，两个日志位于outputs/review-20260924。

## 下一有界任务

从x17及最新确认的r17构造x18=x17+r17/64，allocation内原生重放两端点响应并逐位核对r17；固定物理旧层、rho、phase1367及dt889.419892762322秒。新驱动common_outer_step18、独立run common-step18-20260924。上限8张map，在第4/8张保留独立端点并各评估一对反馈；最多2对反馈，完整原门及四组合收缩，非正气体热能/资源/代码失败立即停。无自动接受第18步、无扩预算。通过后按同态确认路径推进，不等待用户逐小步许可。

32CPU/128GiB、16worker、BLAS1、hugepage0，4小时墙钟上限。复用已验证算法，新版本只更新来源、计数和来源终态检查；旧冻结文件不改。Mac pre-step18.bundle已保存；学校备份、双端测试、提交和新作业信息执行后补记。

## 已执行

Mac54项1.11秒、Linux54项19.25秒全部通过，bash语法及diff检查通过。数值提交ae6b6d7ae184420b3d229954069f854ecdc060aa已同步Mac、学校和GitHub；学校pre-step18.bundle备份完成。76383于09:23:36启动在anode02，32CPU128GiB16worker，4h上限13:23:36；运行1分54秒时仍preparing，stderr空，不将散列/初始化阶段说成已开始worker。只读watcher step18-76383最长5h，终态写outputs/review-20260924/scheduler-76383。

定时任务USTC HHe 运行审阅与决策已切换76383，ACTIVE、30分钟，读回prompt逐字一致。无变化静默；真SSH断线提醒用户并暂停，Slurm继续。
