# 外层收缩审计与一次有界减半试验

## 73846的实测结果

默认4CPU、16GiB只读审计完成，未计算新map，stderr为空。重放73824、73675及73070的真实反馈，核全部76块所有权与哈希；候选最终残差逐元素复现实际判决文件。73824仍只有L2、质量加权收缩两门失败，原判决未改。

| 分母 | candidate/base L2 | candidate/base质量加权 | candidate/base最差单元 |
|---|---:|---:|---:|
| 正式冻结基态 | 1.002998115 | 1.026201637 | 0.9959313025 |
| 零位移对照previous | 1.002998040 | 1.026200922 | 0.9959313016 |
| 零位移对照final | 1.003388159 | 1.027099395 | 0.9957877257 |

所以已有基态对照并不能翻转拒绝结论。但这不是严格内层误差界，不能据此证明精确解下的方向一定不下降。当前候选相邻残差向量差的L2为0.02027649，基态对照为0.02743526，二者之和约0.04771；正式候选与基态的L2范数差仅0.04673720。只能报告这个尺度关系，不能把相邻差当误差上界。当前候选与此前73675最终残差的向量差L2为1.88567652，也不能被两个标量范数很接近掩盖。

有符号逐层分解的L2平方差合计1.459344129，质量加权范数平方差0.02009847154。四个编码分量依次为气体比热能对数、H II/H I对数、He II/He I对数、He III/He I对数。第一分量贡献分别1.332433729、0.01968608086，即总差的约91.3%、97.95%。这是编码范数的贡献，不能称为物理加热份额。

正贡献主要出现在cell91/101及cell约35—50；负贡献完整保留。已打开`cell_excess.png`：正负两侧及尖峰均可见，图轴明确是半柱单元索引，不是几何高度；没有借图把索引解释为表层/深层。

## 决策与试验边界

不再无上限追加原0.00390625候选的map。做一次沿原冻结方向的减半试验，比较真实反馈是否改善外层收缩；它是受控敏感性试验，不以本审计证明方向正确为前提。

记冻结编码基态为$x_{0}$、原方向为$d$、新候选为$x_{\alpha}$，只改变无量纲物质迭代幅度$\alpha$：

$$
x_{\alpha}=x_{0}+\alpha d,\qquad \alpha=0.001953125.
$$

旧幅度为0.00390625。物理相位1367、物理时间步889.419892762322秒、密度、原方向、基态残差、能量定义、网格和科学接受门全部不变。没有温度floor、布居裁剪、强度截负、删除失败层或改变物理时间步。

新run为`outputs/hpc/half-step-a001953125-20260920`。旧73824最终state2只作为数值初猜，SHA为`c69cfa1195e62b8fa424023572ddd7c8ec4c4feaf7bb53b5cf0fd4523da529b4`；它不是新候选的已收敛辐射，也不是已完成反馈的端点。新run不嫁接旧map历史。

预算为cpu_long 32CPU、128GiB、16worker、60分钟，最多4张新map，每2张正式反馈一次，至多两对；接受一个物质步即停。信号/故障停止；不得直接重跑会拒绝已有目录的准备脚本，恢复必须先检查已写状态，再用现有interval驱动、同一声明参数结算欠下反馈。预算耗尽后不自动扩张。

若新步稳定且三范数正式收缩，仍需审查基态精度敏感性，再考虑后续外层推进。若稳定但仍不收缩，不能无限减半：优先将基态与候选放在相同精度诊断下，再评估重建物质方向的必要性。若加热或噪声门不稳，只能结论为当前新候选的内层精度未足够；四map预算不是收敛保证。

## 代码与验证

新`operations/prepare_half_step_after_audit.py`仅在全部16个正式gate存在且恰好两个收缩门失败时运行；校验审计与来源反馈NPZ、协议、物质及基态血缘。准备前先写新trial、逐字段回读，native镜像温度/布居与相位/时间步必须完全吻合，初始化之后再次核trial身份。大态哈希和真实数值计算仅在allocation内执行。

Mac合成与既有回归29项通过，覆盖冻结方向、真实编码位移、物理时间步、字段/候选错配、缺gate/额外gate、已接受态和加热失败拒绝。shell语法通过。Mac真实Linux候选重解码触发严格逐位守卫：2个温度元素差异最大1.455191523e-11 K（全局相对2.4781e-16），H完全相同，7个He分量差异最大1.3553e-20。这只是已测差异的规模，尚未证明其来源；不放宽同平台身份门，Linux必须原样通过才能起map。不能把Mac真实工件路径宣称通过。

## 备份与当前完成边界

改前Git全历史bundle：`outputs/review-20260920/pre-half-step-589ced9.bundle`。审计包`outer-audit-73846-small.tar.gz`在学校家目录和Mac同目录各一份；114664624 bytes，1093文件，SHA256 `31521e5eb1d12acb155a5e5bde9ab76ad7b06250b08603d8b36090f031864e54`。整包及全部1093文件已核SHA，提取到新目录；原dat未改，Git仅收小型审计JSON、CSV、图与代码/报告。

当前没有被接受的物质步、收敛耦合柱或可用于整盘的自洽角分辨强度。此次是代表柱单时间步的连续谱研究，不证明静态模型无解，也不提供最终完成日期。

## 提交与监督记录

代码`5e68bfe`在Mac、学校、GitHub三端一致；Linux相关29项测试通过，shell语法通过。作业73863于2026-09-20 12:21:52 CST在anode22开始，实际申请为`qos_stu_cpu_long`、32CPU、128G、60分钟，16worker。12:23:34检查为RUNNING、准备状态preparing，stdout/stderr为空；此时尚不能称真实候选同平台核验通过或新map完成。

已有`ustc-hhe` heartbeat保持每30分钟ACTIVE，内容已替换为73863的预算、候选身份、结果分支和停止条件；旧配置保全为`outputs/review-20260920/automation-before-half-step-73863.toml`。SSH认证失败时通知用户重连并暂停跟进，不因断线终止Slurm。无实质变化不重复通知。
