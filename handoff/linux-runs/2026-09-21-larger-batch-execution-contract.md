# 扩大单批工作量：阶段判据通过后立即衔接

用户在2026-09-21明确要求每次多安排任务，避免短作业完成后长时间等待下一次审阅。本合同更新下一批的工作组织与总预算；不修改74235、旧协议或已有科学判决。09:22现场SSH可用，队列为空，代码HEAD为07ac181。

## 批次单位与职责

以完成一次可解释的科学决策闭环为单位安排工作。Codex提前完成驱动、阶段判据、失败分支、测试与任务声明，再提交能在平台独立推进的批次；不再每完成一个短阶段就停下来等待30分钟heartbeat。平台驱动执行已冻结流程、记录和阶段转换；监督器只观察已声明状态、识别异常和保留调度终态，不自行修改算法、阈值或预算。

30分钟heartbeat保留，负责审阅新证据和后续决策，不承担阶段之间唯一的启动开关。计算完成立即退出，不为延长运行而补任务。SSH断开只影响人工审阅与传输；平台上已声明批次可以在预算内继续，Codex提醒用户一次后暂停心跳，重连后恢复。

## 下一批明确预算

下一批主计算申请cpu_long32CPU/128GiB，完整map最多16worker；单次allocation墙钟上限6小时。这是资源上限，不是预测必须运行6小时。不绕过每worker6GiB加2GiB父进程的守卫，不并行启动两套各16worker的完整map。候选构造、小块校正可在明确内存预算下独立并行；默认4CPU/16GiB可用于不竞争同一输出的审计。

| 阶段 | 工作 | 新全频map上限 | 新反馈对上限 | 转换条件 |
|---|---|---:|---:|---|
| A | 按74235结果报告重建基态与候选的两块校正，分别验证完整全步和半步 | 4 | 0 | 两个物质状态的全场物理、血缘、原算子及预声明收益/插值检查通过 |
| B | 从通过A的各自全步真实映射输出起跑，基态与候选各再求两张map；各取连续两个已验证输入端点求原反馈 | 4 | 2 | 每个反馈对必须先满足原内层、边界和端点身份门；不满足则记录，不为凑反馈继续追加map |
| C | 原响应/独立账本、基态候选同尺度误差诊断、逐块加热与完整残差比较，归档小工件 | 0 | 0 | 所需物理工件完整且哈希一致；失败先保全 |

整批硬上限为8张新全频map、2对反馈。未触发B时只花A预算；发生物理域、源码/输入SHA、有限性、频组所有权、资源或阶段门失败时停止依赖该结果的后续阶段，不补跑无关任务填满预算。每个物质状态最多1对新反馈，实际反馈端点必须是新批次连续两个已测map输入，不能把未评价的最新输出冒充端点。

阶段A仍保持最多4张map的原研究范围；新增B是用户此次授权下的条件性后续工作。候选原16正式接受门完全不变。基态alpha0的反馈仅为控制和精度诊断，即使其数值门通过也不能将零位移称为新物质步接受；候选仍以冻结正式基态残差作接受分母，基态新反馈不能静默替换该分母。

## 执行器必须实现的行为

1. 新文件、新批次目录和声明。固定全部代码/输入SHA、trial完整字段、旧物理时间层、phase、dt、运行参数、总预算，以及A阶段全部数值门；必须在看到新结果前确定。不能根据旧脚本默认迁移成0.0625候选。
2. 状态机区分准备、A待完成、A判定、B待完成、欠反馈、欠账本、完成、科学拒绝和程序失败。每次阶段转换先持久化输入端点和SHA，再执行下一阶段；预算按已完成及活动map统一计数，恢复不能重复计数或偷偷获得新额度。
3. A完成且全部门通过时，在同一次allocation内立即进入B；正常完成一张map或一对反馈不需要等待Mac心跳。若剩余墙钟不够，保存检查点并退出；只有明确识别的资源/调度中断才允许受控恢复，总批次预算不增加。
4. 候选本身、其原算子输出和半步探针都保留。所有76块/9632组完整重算；两块以外的变化、block62/73和零尾部必须报告。加速只用于已经验证的块，不意味着省略其余频率的物理求值。
5. USR1/TERM传到实际worker派发标志；已提交批次收尾后停止派新worker。恢复先结清已欠反馈/账本，不先跑新map。失败不能由预算结束或后续成功覆盖；已接受仅代表一个有限物质步，不自动展开新的外层物质迭代。
6. 启动前完成接口检查、组件/负路径测试、恢复与总预算测试、真实准备器身份核验。代码先提交并三端同步，发出具体PRE-RUN CHECK后再提交Slurm。保留单一提交权，防止平台监督器、Claude与Codex同时重复提交。
7. 各阶段结束自动制作带manifest的小工件包并保存调度终态；本地连通时同步和逐文件校验，不必等整批结束才保全。原dat不删除、不覆盖、不进入Git。运行报告和小证据入库，图像经目视检查。

## 当前实施状态

本次已更新批次合同和自动任务要求；组合驱动尚未实现，也尚未提交上述新作业。下一次执行任务应直接完成驱动、测试和起跑，不再只重复写计划。阶段A全场准入的具体数值门须在该新声明中明确，并保持已有正式门不变。

改前备份：`outputs/review-20260921/pre-larger-batch-policy.bundle`、`automation-before-larger-batch-policy.toml`。旧实验仍由原合同解释，此文件不追溯修改它们。
