# cpu_long 到时限前的只读诊断

2026-09-19 13:52:11，在作业 73241 现有 allocation 内通过 `srun --overlap` 做了有 20 s 超时的只读检查，没有修改物理输入、配置、并发或时限。

- 主驱动仍活着，previous 反馈已提交 48/76 块，块 48—63 正在计算；final 反馈尚未开始。状态仍为 feedback。
- 16 个反馈 worker 的 `%CPU` 约 99.9，各累计 CPU 时间约等于其存活墙钟；未见进程睡死的证据。
- 抽查三个真实 worker 均为单线程，允许 CPU 列表为 `12-17,26-31,47-50,76-81,90-95,111-114`，共 32 个逻辑 CPU。没有观察到所有 worker 被错误绑定到同一个逻辑 CPU。
- 抽查 RSS 约 1.9—2.3 GB；stderr 为空。该瞬时样本不代表所有阶段峰值，也不单独排除共享内存系统的性能影响。
- 控制声明记录 CPU affinity count=32，与调度器申请一致。

结论只到这里：worker 确实在不同 CPU 上耗费 CPU 时间，并非没有启动或统一单核绑定。尚未确定巨大性能退化的原因，不把本次诊断升级为内存带宽、节点干扰或 CPU 型号的因果结论。

离原 14:11:33 时限约 19 分钟，仍有较多反馈工作，因此存在超时风险。维持原预算，不在原 run 修改协议或并发。若超时，先保存当前全部小工件及完成块哈希，检查可恢复端点和反馈状态，再实现明确的反馈恢复路径；不得重新跑两张已完成 map，不直接重跑拒绝已有目录的主脚本。

原始探针：`handoff/evidence/cpu-long-73241-live-probe.txt`。没有产生新科学反馈或接受判决。
