# 同一半步物质、不同辐射初猜的有界对照

## 问题与边界

73888半步候选已通过内层辐射、率、加热、噪声及物理域门，但L2和质量加权物质残差仍不收缩。73906显示半步与原步的有限差商差异约83%；这些差商混合物质非线性和辐射误差，不能当可信Jacobian。此前不同辐射历史曾造成远大于相邻漂移的反馈差距，因此本次先直接控制初猜。

新run `outputs/hpc/half-step-base-seeded-20260920`与73888使用完全相同的物质文件，SHA `07a3700d8767f5bd7addf7b2932d3b44742e803e2aed52c83e2e86b82ce07b86`。无量纲幅度0.001953125、物理相位1367、物理时间步889.419892762322秒、密度、基态、方向、网格、能量定义与所有接受门不变。

仅将辐射初猜改为73070零位移基态最后输出`outputs/hpc/baseline-zero-control-20260919/state_2.dat`，记录SHA `565a19526e7fb1ab106326b3efc123ba2a589b99525759eca8b3d95fd024a0af`。这不是半步物质的已求解辐射，不能继承基态的收敛结论，也不继承旧map历史。原半步链和零位移链不续算、不覆盖。

## 执行契约

新`prepare_half_step_base_seed.py`核零位移run确为`baseline_control_complete`、无半态、恰有两张完成map，当前槽和最后输出一致；再核预先声明的seed路径与SHA。原helper不支持这一终态，故新建检查函数，未改冻结helper。

参考73888必须是8map/2round、最后pair准确对应map7/8输入、完整16门只剩L2和质量收缩失败。两协议的物理旧时间层与基态残差声明一致。基态物质必须确实等于半步的冻结编码基态，direction/rho/phase/dt等逐位一致。两run依赖校验、大态哈希均在Slurm allocation内执行。

新trial先逐字复制、核全字段再初始化，native T/H/He镜像、phase和dt须精确一致；初始化后再次核全字段和初猜SHA，历史必须为空。配置冻结新入口、batch脚本、引用的方向检查模块、两源状态、协议、控制摘要及依赖声明。发现变化就停止，不能自行改哈希接受。

预算：cpu_long 32CPU、128GiB、16worker、60分钟；最多8张新map，每4张正式反馈，至多两轮；接受即停。严格辐射门1e-4，加热/噪声/三范数门不变。停止信号或故障后先审现场，不直接重跑会拒绝已有目录的准备脚本；需要恢复时沿用8map/每4张反馈的声明参数，先结算欠下反馈，总预算不增加。

## 验证与结果判读

Mac48项相关测试通过，其中19项新测试覆盖基态终态/最新槽、未结算状态、基态物理身份、参考协议及完整门错配。真实73906快照中的两源trial、baseline state和73888正式summary通过同一入口检查。shell语法通过；提交前仍须Linux同组验证。没有在Mac运行大态计算。

完成后与73888比较完整encoded残差向量、L2/质量/最差单元、逐层热能、率和加热，以及各链相邻漂移。不同初猜运行相同map数不代表误差匹配，单轮加热稳定也不是累计误差界。若跨历史差异仍影响外层结论，先解决内层历史敏感性；若两历史趋同但外层仍失败，才设计有证据的新方向。

改前保全`outputs/review-20260920/pre-half-base-seed-7246fb7.bundle`。新工件在独立目录，完成后按既有规则双端备份并核整包/逐文件SHA。默认4核暂不重复跑已完成方向审计；若出现独立有效的诊断再使用。

本次不是已接受物质步、耦合柱收敛或整盘自洽强度交付，也不能用于宣称模型无解。

## 启动与监督记录

代码`237f28a`已在Mac、学校和GitHub同步并核HEAD；Linux同组48测试通过。作业73929于2026-09-20 15:15:38 CST在anode16开始，实际为qos_stu_cpu_long、32CPU/128GiB、16worker、60分钟。15:15:55检查RUNNING、preparing、stderr空，此时尚未确认初始化完成或新map完成。

每30分钟的`ustc-hhe`已更新为73929的预算、恢复规则及与73888对照要求，并回读配置确认prompt完全一致。旧自动任务配置备份为`outputs/review-20260920/automation-before-base-seed-launch.toml`。SSH断线按已有约定提醒重连并暂停跟进，不终止Slurm。
