# 夜间运行状态（2026-09-15 23:58 起）

给夜间及明早接手用。**本文件只描述安排，不改变任何预算或阈值。**

## 正在跑什么

| 项 | 值 |
|---|---|
| 科学 run | `outputs/hpc/hhe-r025-cont` |
| 种子 | `hhe-diag-r025/state_2.dat`（已提交 SHA `eb4c30283322c41c…`，最收敛的可信 Linux 态） |
| 物质候选 | 与 diag run 同一份 `trial_material.npz` —— **固定 0.0625**，未重传 Mac 检查点 |
| 预算 | `maximum_maps = 8`，每 4 张一轮正式反馈 + 逐层账本，**最多 2 轮** |
| 资源 | 2 workers、4 CPU / 16 G、`Students` / `qos_stu_default`，原资源门不变 |
| 提交者 | `diagnostics/interval_supervise.py --max-jobs 8`，分离进程（`PPID=1`），日志 `outputs/hpc/hhe-r025-cont/supervisor.log` |
| 首个作业 | `63698` `tde-interval` on `anode17` |
| 巡检 | cron `e96bd09f`，10 分钟一次 |

## 停止条件（按优先级）

故障 > 已接受 > 待恢复反馈 > 正常运行 > 预算结束。到 8 张或故障即停，**不自动扩额、不改物理阈值**。

## 预计时长与不确定性

单张 map 实测 628–3587 s（**节点间 5.7 倍差异**），单轮反馈实测 1159–2290 s。

| 情形 | 8 张 + 2 轮 |
|---|---|
| 快节点 | ≈ 2.0 h |
| 慢节点 | ≈ 9.3 h |

**所以这一夜未必被占满**：若在快节点上跑完，运行会按设计停下，剩余时间将空闲。
**我不会为了让机器不闲而自动扩预算**——那需要用户明确授权。

## 夜间产出（无论何时结束）

1. 每张 map 与每轮反馈的实测数据（残差、边界量、账本、墙钟、RSS）。
2. 一轮中文报告：`handoff/linux-runs/` 下新文件，含具体能量账本与失败门。
3. 新科学反馈的小工件包（供 Mac 复核；排除 `.dat` 与重复旧工件）。
4. 无推送凭据时导出新 bundle。

## 纪律（不变）

负热能或加热不达标**如实报告**，不把代码测试通过当成物理门通过；
不裁剪、不 floor、不改物理 dt、不改能量定义；预算耗尽写成"预算耗尽"，不写成收敛失败。
