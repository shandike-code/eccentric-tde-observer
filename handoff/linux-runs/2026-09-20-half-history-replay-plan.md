# 固定物质、两种辐射历史的只读重放

## 目的与边界

73929与73888的trial全部字段一致，但final编码残差向量的L2距离为3.836479051。新入口`operations/audit_half_step_histories.py`只读两条链最后一个正式反馈pair：重放previous/final物质响应、验证final与正式保存向量逐位相同，定位率、加热和目标气体热能的层间差异。零新map，不接受物质步，不改任何冻结源或工件。

所有输入按SHA复制到新run的`inputs/`，原始频块按原顺序重新汇总。每个频率组必须恰好属于一个核心块；全柱4096子层数组、256母层均值和前128层物质输入逐位验证。比较完整trial、物理旧时间层、质量测度、native输入、协议血缘和正式门。气体热能沿用原账本；对数编码分量的平方贡献不解释为物理能量份额。相邻漂移和跨历史差距都不是真实误差界。

## 验证与资源

本地28项相关测试通过，包括相同范数掩盖相反向量、额外或改变物质字段、非有限值、频块重叠/空洞/重复以及错误half层映射。真实备份内四端点的全部频块SHA及full/parent/half数组重组逐位成功，trial全部字段一致。首次本地真实核验发现新脚本误用adapter的局部导入名称，已改为显式导入原汇总模块；尚未提交作业时即发现修正。shell语法检查通过。

学校先复跑同一测试集，再提交`operations/audit_half_step_histories.sbatch`，新目录`outputs/hpc/half-history-replay-20260920`，默认4CPU/16GiB、20分钟上限，无自动续交。这不是20分钟交付承诺。运行状态、进度、环境、逐文件清单、重放NPZ、逐层CSV、JSON与图均存新目录；失败保留现场。

完成后检查stderr、有限性、重放一致性、能量量纲及图像。原正式判决保持不变，再依据实测决定是否开展固定物质下的辐射慢模实验。
