# 74075：基态替换未消除拒绝，下一步仍控制内层历史影响

## 实测与可复核范围

74075使用代码`1c41132942003b3f269b36f2dd6f8d7b93197aa2`，默认4CPU/16GiB、anode22。`scontrol`记录2026-09-20 20:22:05至20:22:48 CST，43秒、COMPLETED/0:0；`sacct`没有完成记录，不能编造其MaxRSS。stderr空。新目录`outputs/hpc/mixture-material-residual-audit-20260920`，零新map。

重新核验74057混合态、73070零位移控制的四个反馈端点：76块哈希与频组归属、全柱/256母层/128物质层重组、原物质响应和独立能量账本。74057 final编码残差逐位复现。两条旧历史使用74031已验证且SHA冻结的重放向量，本轮没有重复求解旧响应。trial、物理旧时间层、phase、dt、密度、质量测度一致性检查通过。数组有限；两混合端点仍无非正目标气体热能层。

图`mixture_audit.png`已逐幅视觉检查：坐标、图例、三历史目标热能、编码差和带符号范数增量可读，横轴是物质单元索引，不是几何高度。对数编码分量的平方贡献不是物理能量份额。

## 正式拒绝不变

74057保留原16门，14通过，只有L2和质量加权收缩失败。没有接受新的物质步。

| final候选的比较基准 | L2比 | 质量加权比 | 最差单元比 |
|---|---:|---:|---:|
| 冻结正式基态 | 1.000214777921 | 1.003852332615 | 0.998520391899 |
| 零位移previous，仅诊断 | 1.000214703409 | 1.003851633602 | 0.998520391065 |
| 零位移final，仅诊断 | 1.000603739602 | 1.004730539096 | 0.998376441912 |

三个比较均未使两个失败比值降到1以下。此结果排除“只换成已有零位移基准即可通过”的具体解释；它不证明所有可能基态精度误差都无关。正式接受基准未改变。

候选相对正式基态的平方L2增量0.1043987201、平方质量范数增量0.002922412011，均为正。四编码分量的L2增量约[0.01004759221, 0.006454728437, 0.04394436544, 0.04395203405]；质量平方增量约[0.002623884120, 0.000004611241, 0.000146971941, 0.000146944708]。不能将“历史差异主要来自气体热能编码分量”替换为“当前所有范数失败都由同一个分量造成”。

## 相邻稳定仍不足以消除历史影响

| 比较的完整编码残差向量 | 差的L2范数 | 差的质量加权范数 |
|---|---:|---:|
| 混合态final−previous | 0.01556619623 | 0.001931087318 |
| 混合态final−73929 final | 1.909928691757 | 0.240138008814 |
| 混合态final−73888 final | 1.928984069584 | 0.245102735756 |

对应原子净加热变化指标：自身相邻0.000461199101，对73929为0.06256663681，对73888为0.04622217882。不同历史差异仍远大于相邻漂移；这不是严格误差界，也不能据此判定哪条历史最接近真解。混合态处在旧结果之间，不等于已经找到正确解。

## 下一步的有限实验

不自动增加74057 map预算，不再次机械减半物质步，不优化物质范数以挑选辐射初值。先用已经保全的相同物质、相同算子下的真实输入/输出对，设计一个新的受正性和系数幅度约束的辐射残差子空间扫描。优先核74057最后两张map及73929/73888最后真实map；选择和排序须显式声明，不能用旧脚本硬编码的两条来源和六态数组布局冒充通用接口。

第一阶段限一个新命名只读扫描，默认4CPU/16GiB、最长60分钟、零新map、零候选dat写入。优化目标为全场辐射映射缺陷L2，独立检查最大范数较最好实测至少改善1%、边界门、全场正性/有限性、系数和为1及L1上限32。正性约束扫描最多6轮、最多4096条约束；不逐单元裁剪。秩不足、预算耗尽或全场检查失败均保留失败，停止该候选，不改阈值。

只在上述预测完整通过后，另起32CPU/128GiB、16worker的新run，最多一次原算子map验证候选，核实际残差、正性、边界和全场预测误差。一次局部仿射验证不证明全局仿射或固定点唯一。实际验证通过后才设计最多2张map与1对原正式反馈；每阶段都有独立声明、代码测试、PRE-RUN CHECK、提交、保全和决策。旧run与钉住的源码不改。

若新子空间没有可行改进，应分析未消除缺陷的频率/空间分布、正性约束与秩限制，再决定预条件或其他内层求解改进；不能无限重复扫描或用相邻差商冒充Jacobian。现有证据仍不足以给出自洽柱或整盘角分辨强度的交付日期，也不足以否定模型存在解。

## 保全和运行前后检查

本轮运行前Mac/Linux各46项相关测试通过，真实74057频块重组和零位移物质身份检查通过；PRE-RUN CHECK已记录同物理旧时间层、原能量定义和诊断分母不替代正式门。运行后重放成功、有限性检查通过、stderr空、正式拒绝保留、图已检查。

双端备份`mixture-material-audit-74075-small.tar.gz`：82,608,560 bytes，976文件，SHA256 `99671add69d082563e345cdbd3b0bb242c73748b163dc411baf72e0a6c0c1616`。学校家目录与Mac `outputs/review-20260920/`各一份；整包与全部逐文件哈希已核。Mac解包`mixture-material-audit-74075-received`。不含dat，旧dat未删除。

小证据在`handoff/evidence/20260920-74075-mixture-audit-summary.json`。改前Git备份`outputs/review-20260920/pre-74075-closeout.bundle`；定时任务配置备份`automation-before-74075-closeout.toml`。本报告写入时队列为空；下一扫描尚未提交，不将计划描述成已运行。
