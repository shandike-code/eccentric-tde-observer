# x18未通过质量加权收缩：复核、定位和两次回溯

76405于10:10:54—11:13:03正常完成，COMPLETED 0:0、62:09，anode02/32CPU128GiB，stderr空。Slurm队列旧ID已过期，watcher保存的终态是依据；不是SSH故障。8张新增map与2对反馈均完整，末辐射残差7.049598160e-5。正式接受仍17。

|反馈对|加热L1|噪声/信号|final L2|final质量加权范数|final最差单元|
|---|---:|---:|---:|---:|---:|
|新增map4|2.377215080e-4|0.01290804431|8.466875980|0.3481064270|2.708784359|
|新增map8|2.724618735e-4|0.01131913458|8.482032368|0.3500694471|2.709374713|

两对都15/16，唯一失败candidate_mass_weighted_contraction_pass。相对已确认x17的final质量范数0.3011682221，新增map8 final的比为1.162371796；四组合最不利1.163745191。L2和最差单元小于基态不覆盖质量加权失败。四个端点气体热能全部正，最低5.781982115e12 erg/g；加热与噪声门全部通过，但同态final残差仍有漂移，后4map质量范数没有改善。

独立小归档148735195字节，SHA256 2abe408407261ae61a0d07453972e84dc185d47ef6686977eb42f9001d70d4e5。Mac审计代码review_common_step18_precision，完整重算所有文件SHA、逐块源替换/汇总/深度/拥有权/完整态、账本/编码、原门及四组合，明确保留失败而非硬断言全绿。图已检查，无裁剪或删点；原阈值和物理量不变。dat不传Mac、物质ODE不在Mac重跑。

共核3082文件、304反馈进程回执、608map回执；最大/proc4040196KiB，map计算总1305.256秒。逐层定位脚本localize_step18_mass复现JSON逐字节相等。Mac新回溯及关联回归82项1.17秒通过。

## 定位

沿用audit_outer_contraction.decomposition的恒等式：每层有符号贡献为m_i/sum(m)乘以候选四分量残差平方和减基态平方和。分量顺序由GroundStateLogSimplexCodec源码核实为log气体热能、log(HII/HI)、log(HeII/HeI)、log(HeIII/HeI)。这些是无量纲编码残差，不是物理能量分配。

新增map8 final的质量范数平方净增加0.03184631979，其中log热能0.03123748772（占净增加约98.09%），另三项为3.140149044e-5、3.949122580e-6、5.734814558e-4。半柱网格编号96–127贡献+0.03968735772，其他层合计-0.007841037933；正负项抵消后才是净增加，不能只报正贡献百分比。最大的十层是编号127至118。这里只采用物质half网格编号，不在未复核几何方向时替它命名表面/中面。

四个端点均出现相同的主导层序和log热能分量。新增map4final→8final残差向量差L2 0.1214155585、质量0.006845947855、max0.04089809001，这是漂移诊断而非误差界。尚不足以断言连续残差对步长的导数符号，更不能声称模型无解或回溯必然有效。

## 决策

不继续对同一1/64候选增加map。新基态对照4map，稳定后沿原r17依次尝试1/128、条件1/256，每个最多8map2pairs；上限20map5pairs。原门和物理dt保持不变，不接受18。只有已解决内层/加热/噪声/物质域而收缩仍失败，才允许第二个幅度；若两者都失败，停止并审查方向。新协议common-step18-backtrack-v1.md中有完整条件。

代码把原固定1/64身份检查留在旧模块；新独立函数仅允许预先声明的三种幅度，并核查全部encoded和物理身份、解码及原信赖域。原native worker、共同频域算子和物质求解器均未改。Mac pre-step18-backtrack.bundle已备份，执行结果随后补记。
