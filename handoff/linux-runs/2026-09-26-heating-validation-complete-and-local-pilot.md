# 77577完成：热代理有效，但八map窗口仍有两类范数未通过

77577于2026-09-26 13:13:25–15:23:11正常完成，2:09:46，32CPU/128GiB/anode02，COMPLETED0:0，stderr为空。
当前`scontrol`已清除旧job记录；只读监督已在终态保存原调度输出，不能把Invalid job id当作作业失败。
证据`handoff/evidence/20260926-heating-validation-77577-terminal.json`。

## 完整审计

完整包`complete-1790407360172479630.tar.gz`及receipt在`outputs/review-20260925`；
Mac解包`heating-validation-77577-complete-received`。大小/SHA见完整审计JSON的archive字段。
`review_step21_heating_feedback.py`真实E2E核3387文件、691代码声明、760map和304feedback进程回执，
完整trial/原旧层/r20、逐块汇总/归层、反馈域与能量账本、代码/来源SHA、两对原七门均通过。
新热/窗口范数用math.fsum独立归约，原七门复用原判定函数；Mac没有重算大辐射场或ODE。
最大进程4042824KiB；已看完整图。零新物质接受。

首对实际热代理比0.7827750126391803，预期收益21.72%已经实测；六误差最大5.55e-10，小于1e-3。
但pair10相对pair02的八map四组合最大L2/mass/max漂移除原r20分别为：
0.0014561771365109452 / 0.00499379235128165 / 0.0008278986688149568。
原门为每项<0.001，故前两项失败，最差单元这一项通过，不能挑一项宣布稳定。

末map residual=2.784807855914952e-6，boundary L1=1.4678262081625415e-6、bol=1.467089187362852e-6。
block74自身残差0.15420606701542988仍大。末对atomic heat稳定指标3.781995067713588e-5，原七门全true。
最低气体比热能previous7.035910954565004e12、final7.034708960888727e12 erg/g，均正。
末final-r20绝对L2/mass/max为0.8379540994046122 / 0.0814440442482212 / 0.1207404149376397。
正域通过不是残差归零，原r20不替换。

POST-RUN CHECK：完整有限性/非负强度、物质正域、频率覆盖、能量账本、来源/资源均通过。
局部热代理收益真实，较长窗口不稳定也真实；没有证据说明整个模型无解，也不能给整盘完成时间。

## 为什么不再拼一组旧历史或盲续十张

新只读小工件分析`diagnose_step21_heating_subspace.py`绑定完整原包，使用已实算的三组
配对77371 pair11、77577 pair02、77577 pair10，固定原u_ref/质量尺度，解二维无约束热代理最小二乘。
奇异值9.740324826768e-5、1.704870681856e-5，条件数5.71323；系数
[-1.0159471561913282,-2.919524143021071,4.9354712992123995]。
最优比0.9083390904883979，只改善9.17%，原20%成本门失败。
这是**恰好这三个配对历史张成空间**的无约束最优；加上正性等约束不会更好。
因此不花大场扫描/真实map去验证该组合；不把它当作任意求解方法的下界或无解证明。

同一分析对本批pair10 final减pair02 previous做逐块Q变化，正确reshape(256,16).mean()后取前128格，
乘冻结dt/rho/u_ref，再取质量范数。最大为block24=2.3432098205052092e-4、48=2.100327220973081e-4。
范数只用于定位；不能把76块范数直接加成净加热范数，因为各块变化有相消。
证据`handoff/evidence/20260926-heating-subspace-and-block-ranking.json`。

## 新有限试验：当前两个块的混合系源校正

新driver `operations/pilot_step21_heating_blocks.py/.sbatch`和独立协议
`handoff/protocols/step21-heating-block-pilot-v1.md`。源码核对旧paired_block_krylov实际调用与历史7B9q/r/w/x/u及74454报告。
新源为77577最后一次真实map的输入/输出保留态，固定当前物质20，只选block24/48。
4CPU16GiB、2worker、2小时总硬限，默认队列足够；每worker30分钟、每例16步有限GMRES/至多3次原局部map。
先重放原图，后正性受限原方向和原算子新求值；L2/Linf至少减半、边界不增、预测精度/成本/RSS全过。
保存局部candidate/mapped NPZ与SHA，零全频map、零正式反馈、零全局候选、零外层接受。
两例局部通过后仍须另行评估邻块halo耦合和全场真实性，不默认放大到76块。

本地28测试通过（0.97s），覆盖末map配对与血缘错配/半成品拒绝、停止组处理/故障不记成功、
原正性/非线性误差/坏方向与重放检查；shell与Python语法通过。Linux同组测试通过后方可提交。

[PRE-RUN CHECK]
Code: PASS — 固定源配对/完整trial/本地测试与语法通过；allocation先核大SHA，worker再核代码和原map重放。
Logic: PASS — 当前热变化定位→两块有限方向→原算子局部检验；不跨越为全频/物质结论。
Physics: WARNING — halo固定，局部逆不能保证全频改善；旧74454反例保留，62/73失败不绕过。
Key Issues:
1. 全局历史组合收益不足，不重复该路线；物理时间/原r20/能量定义不变。
2. 有限GMRES未收敛也只能提供诊断方向，所有实际局部门不可缺省。
3. 4核仅对应两例worker，无额外无目的32核任务；若局部过门再登记全场验证。
Decision: RUN — Linux同组测试通过后提交一次两块试验。
