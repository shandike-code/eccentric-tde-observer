# 32 核测速与有界内层续算

## 已完成的科学试验

作业 64633 在 2026-09-17 00:05:14 以 COMPLETED / 0:0 结束，耗时 1:18:05。0.03125 候选完成 8 张 map、两轮反馈，但未接受。原始小型摘录在 `handoff/evidence/ustc-backtrack-v2-closeout-20260917.json`。

| 指标 | 第一轮 | 第二轮 |
| --- | ---: | ---: |
| 加热相邻变化 | 0.0265868 | 0.00904787 |
| previous / final 非物理单元 | 38 / 34 | 32 / 32 |
| final 最小剩余气体热能 / 旧气体热能 | -1.95342 | -1.53760 |

最后 map 辐射残差 0.00168677，仍高于 0.00025。噪声和三个物质收缩比未求值，不能用缺省值判定。八张的映射时间约 372–377 秒/张；正式反馈约 733–735 秒/轮。该耗时是实测，不能推断达到科学验收的时间。

## 已声明的下一项任务

申请单个 `cpu_long` allocation：32 CPU、128G、8 小时，独立目录 `outputs/hpc/scale32-cont-20260917`，科学子目录 `science`。旧 run 保持停止和原字节。

1. 先做六张完整性能映射，worker 顺序 **4、8、16、16、8、4**；每张使用同一冻结种子 `hhe-backtrack-r003125-20260916-v2/state_2.dat` 和逐字节相同的 0.03125 trial。它们不是六张累计科学迭代。
2. 要求六个输出哈希一致、原 worker RSS 门通过；每档取两次 map 墙钟的中位数，选择最快档。顺逆序降低顺序混淆，但不能消除共享节点负载影响，也不是充分统计学性能结论。
3. 现有保护要求每 worker 6 GiB 加主进程 2 GiB，32 worker 需 194 GiB，超过 128G。故这次申请 32 核，但最多运行 16 worker；不绕过内存保护。记录 CPU affinity、环境和每档实测资源。
4. 性能验证通过后，**同一作业内自动启动**科学子任务，不再等待下一次人工提醒：复制原候选及最终辐射态，新增最多 **32 张 map，每 8 张正式反馈，最多 4 轮**。种子不是收敛解，不复用旧反馈；物质候选、物理 dt、所有科学门不变。
5. 正常预算结束或一个物质步接受后停；代码、哈希或资源故障立即停。USR1 转交子进程，保留可恢复状态；恢复须核对具体阶段，不盲目重交整个复合任务。

此次有限续算的依据是：新候选只做了 8 张，其辐射残差和加热变化明显下降但尚未稳定，物质响应仍离开物理域；现阶段再减步长会再次混入辐射初始暂态。继续同一候选的内层精度，检验非物理响应是否消退，是一次有界诊断，不是断言 32 张后必通过。到点后必须比较加热、失败单元/热能缺口、噪声和三种残差，再选择下一步，不能无限重复本预算。

实现新增在 `operations/scale_then_continue.py` 与 `.sbatch`；旧 `src/`、`scripts/`、`hpc/`、`diagnostics/` 未修改。科学子目录提前拥有自己的 trial，初始化直接调用支持零 map 的既有 API，避免旧迁移回退。配置固定原输入及新增脚本/声明的哈希。

平台 Claude Code 只读监督；其实际后端依旧为已配置模型，不授予提交或修改权限。监督提示已改为读取真实性能顺序及反馈间隔，不再硬编码 2/4/8 workers 和每 4 张反馈。

Mac 相关测试 28 项通过，覆盖性能重复计时选择、输出差异/内存拒绝、不完整比较拒绝、精确 trial 复制与禁止覆盖、原初始化及监督逻辑。尚需 Linux 复测与实际起跑核验。

## 实际提交与监督交接

2026-09-17 09:19:38 提交 **71606**，随后 RUNNING，计算节点 `anode17`，Slurm 分配 32 CPU / 128G，进程 affinity 含 32 个逻辑 CPU ID。部署提交 `b51b583`，Mac 与 Linux 各 28 项相关测试通过，GitHub 已同步。

2026-09-17T09:21:03.501423+08:00 核查首档 worker=4 的初始化完成，已提交 4 个块；监督会话 `tde-scale32-watch` 正常。提交参数、原始调度器状态、affinity 与首档进度见 `handoff/evidence/ustc-scale32-start-20260917.json`。平台监督目录 `outputs/hpc/codex-scale32-watch-20260917`；本地自动跟进已更新到此 job/run。SSH 断开不影响作业内的测速到科学阶段衔接。
