# 77531已有预测，终态与完整审计仍待完成

12:24现场77531仍RUNNING，stderr为空，status停在scanning/frequency=9600，prediction.json已写。
驱动在写prediction后会再次核验全部来源与代码SHA，之后才写complete并归档；
因此不能仅凭status未变化说它卡死，也不能凭prediction已出现宣布作业完成。
本记录只保存平台监督快照，尚非完整归档独立审计。

当前八门均true：alpha=-2.222250421897835，热代理比0.7827750126445281；
全局辐射预测残差2.89178754246241e-6，相对77371 map11比0.9281833102456314；
边界L1 3.005159096579182e-6、bol3.000739305409263e-6；q/p全点非负，峰值290807808字节。
净加热代理21.72%与辐射全局残差7.18%是不同指标，不能互换。
未生成大候选、未实算新映射/候选加热、未接受21；守卫true尚须原归档复核。

## 完整复核通过后，真实验证应怎样安排

设计依据：旧wide验证在map3才做首反馈，无法直接比较原初猜q与T(q)的净加热。
本任务必须直接检验已登记的热代理，因此新批次应改为**最多10map、两轮反馈，节点2和10**：

1. 写一个新q大态，按原四态公式/float64顺序逐位核验。正确control trial先落地并完整核身份。
2. 第1张真实T(q)检验预测场误差、全点非负、严格内层/边界、相对77371 map11的辐射残差改善。
   不能误套旧“全局残差下降20%”门；本试验的20%是热代理成本门，辐射门仍是至少1%。
3. 第1张通过才算第2张，让q与T(q)这两个输入端点各有已实算的后继，满足正式pair_ready。
   pair02的previous/final输入分别为q、T(q)，其Q可直接组成真实热代理。
4. 用与预测完全相同的u_ref、dt、rho、质量权重和原late proxy分母，比较实际
   s=(dt/rho/u_ref)[Q(T(q))-Q(q)]与预报s。真实代理比必须<0.8；不得改分母或使用新响应能量作u_ref。
   还应单独核Q(q)的仿射预报与Q(T(q))的场预测，记录大项相消，不能只比较一个范数大小。
5. 只有原反馈七门、物理域及热代理实算验证均过，才继续map3至10及pair10。
   pair10与pair02才是这个候选之后的八map窗口；完整四组合三范数仍除原r20，门0.001。
   同时保留与77371 pair11的位移，不能替代或重新定义r20。

这只是设计记录，尚未实现/测试/注册新数值协议或提交32核作业。
在77531最终归档与独立复核通过后，落实准确源路径、冻结预测的逐层s向量和u_ref、
原子/实际预测加热差的数值容差、终态优先级与条件预算，再做负路径测试和平台预检。
特别测试：首真实场失败禁止后继；pair02热代理失败禁止八map追加；所有guard false不能被预算完成覆盖。
沿用经修复的finite模板构造零控制协议路径，所有接受权限false，禁止默默迁移trial。
32CPU/128GiB、16worker、独立新目录及合理硬上限；一次完整条件批次，不盲续交或试第二候选。

完整独立审计仍用现有`review_step21_heating_projection.py`，等生产complete包及调度COMPLETED证据后执行。
任何筛选失败都关闭此候选，不继续本设计。当前77531运行代码和协议保持冻结。
