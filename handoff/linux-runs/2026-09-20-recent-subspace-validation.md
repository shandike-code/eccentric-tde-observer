# 09:56巡检：最新基底候选通过预测门，安排单次真实算子验证

73693已complete，stderr为空，原队列空。全场前后原态SHA检查通过，未写候选或运行map。

## 最新四张连续映射的扫描结果

差残差Gram特征值为2.30464357551e-11、2.89385747723e-8和4.80289192993e-6；相对阈值仍1e-12，可分辨秩3，条件数约208400.64。未经约束的权重很大，产生155350个候选负元素和184654个预测映射负元素；因此没有直接使用该最小二乘解。

共三遍全场检查、两次约束优化、320条累计约束后，最终原权重为[1.0000000000000269, -2.842170943040401e-14, -15.500000000000048, 15.50000000000005]。原系数L1为32.000000000000156；按预声明系数上限统一回溯得到步长0.999999999999995，有效L1恰为32。正性本身允许步长1。没有将小负系数或小强度截成0，也没有增加系数上限。

| 全场预测指标 | 值 |
|---|---:|
| 最大归一残差 | 5.66848439549e-5 |
| 当前真实比较基准 | 6.23244982144e-5 |
| 残差平方L2 | 7.95724898088e-5 |
| 起点残差平方L2 | 1.77919235221e-4 |
| 边界谱L1 | 5.16399575477e-5 |
| bolometric变化 | 8.60991262616e-6 |
| 候选／预测map负元素 | 0 / 0 |

预测最大残差比最新实测低约9.05%，超过预登记至少1%的改善门。所有保护条件通过，值得验证。图`20260920-73693-constraint_rounds.png`已实际打开核验，三轮负元素和步长趋势与JSON吻合，标题明确没有真实map或物质接受。

最弱尾端仍产生约束：原始限制元素包括1e-323强度，第一次优化后的限制元素包括5e-324强度。保留这些元素和严格正性检查，不从subnormal幅度推出净加热贡献可忽略。

## 新验证器与预算

新增`operations/validate_recent_subspace.py/.sbatch`，不改任何历史模块。输入是扫描声明的六原态路径、四对映射、起点权重、原权重和统一步长。先核两段完整映射链、相同物质trial、native配置、所有源码及大态SHA，检查比较基准没有过时、六个必要预测门存在且为真。只检查一个总通过标志不够。

候选及预测都采用原表达式`anchor_field+step*(raw_four_map_combination-anchor_field)`，运算次序相同；不替换为重排有效系数和。流式构造一个候选dat，任何负值、非有限量、短读、超系数上限都会停止，半成品不发布为候选。源字节构造前后校验。

显式携带所有trial字段、逐位核对后初始化，32CPU/128GiB/16worker，NUMPY_MADVISE_HUGEPAGE=0，最多一张真实map，墙钟60分钟；不执行正式反馈或物质接受，不自动扩大预算。物理相位1367、dt889.419892762322秒、alpha0.00390625及1e-4严格门保持不变。

真实map后逐元素复算候选，确认它与写入字节相同，再比较实际输出和预测输出。输出全场相对L2、最大误差/强度尺度、边界预测误差，以及预测误差L2/实际映射改变量L2；最后这一比值须小于0.01。实际最大残差须优于当前真实基准至少1%，边界两门须小于1e-3，worker峰值须小于6144MiB。即使全部通过，也只验证这个组合，没有证明算子全局仿射或物质反馈稳定。

当前系数L1已到32，因此误差放大检验不可省略。若预测不符，保留候选和输出定位差异；不改权重、门槛或物理强度来掩盖。若通过，再从已验证输出另建有界反馈对，依据完整加热、噪声及三个物质收缩门判定。当前尚无被接受物质步。

## 保全与验证

Mac相关62测试通过，包含表达式字节一致、基底不变、短读不发布、负值/超系数拒绝、候选字节不符拒绝、相同标量但不同场拒绝、缺失门和过时比较基准拒绝，及原有物质/连续映射血缘检查。改前bundle为`outputs/review-20260920/pre-subspace-validation-e204058.bundle`。

学校家目录和Mac `outputs/review-20260920/`保存`recent-scan-73693-small.tar.gz`，20086492 bytes、595文件，SHA256为`112cafb1de1a526daf0c8ee598b581a3ca5f9bd5d6ee9de24b3f0c3dccd1d16a`。整包及每个文件SHA全部核对后解入独立目录；大dat原地保留。Git保存prediction、residual_system和已视觉核验的小图。

## 提交记录

代码`ee98a22`已同步学校、Mac与GitHub；Linux同组62测试通过。新作业73768，run为`outputs/hpc/recent-subspace-validation-20260920`，cpu_long32CPU/128GiB/16worker，上限60分钟。旧73693不续跑。30分钟heartbeat保持ACTIVE，已写明逐场验证门、故障恢复边界及通过后才另建反馈对的顺序。
