# 18:08 巡检与单块性能剖析

73308仍在第二轮final反馈，46/76块、累计反馈墙钟4511.75s；四张map均已完成，previous反馈76/76。没有新的科学判决。原Slurm截止19:00:58不变，不追加map、不修改正式反馈7200s状态资源门。若触及门或时限，先保留现场，再审阅恢复条件。

## 新的有限任务

作业 **73385**，run `outputs/hpc/profile-round2-final-block24-20260919`，代码 `f936a3e`，QOS cpu_long，2CPU/8GiB、1重型worker、45分钟，USR1提前120s；分配anode16。它与73308在同一节点、不同allocation，仍有共享资源和剖析开销，不能把墙钟比当作严格加速比。单块不使用32核；这项任务旨在识别函数级CPU耗时，不是重跑一张map。

调用原 `run_worker_adapter` 对第二轮final的已提交block24重放一次。原块耗时1030.9s。物理输入、物质候选、网格、率核与能量定义均不变；协议SHA `4f33550f7239afbaa6565e74b541e9f1e6d499adfbd36a2b021871cfce4f737e`。先校验完整协议与继承输入源，完成后再核原输入，原run完全只读，产物写入新目录。

## 预检、备份、完成判据

- 变更前代码备份 `outputs/review-20260919/pre-feedback-profile-1d8bd9b.bundle`；新代码bundle同步学校。
- Mac/Linux各4项测试通过，覆盖有限数组差异报告、NaN/Inf与形状变化拒绝；sbatch通过bash语法检查。
- 保持原资源守卫：1重型worker至少6GiB+2GiB余量，8GiB满足。没有关闭守卫。
- 新输出包含声明（Git/源/代码SHA、资源环境）、cProfile二进制、累计和函数自身耗时排名、块工件与原块逐数组比较。
- `status=complete`仅表示剖析完成，不给予科学接受。非有限值、字段/形状/类型变化直接失败；数值差异如实报告，不能自动加入正式反馈。
- 信号/异常尽量保留当前profile和失败状态；若Slurm强杀，缺少终态就是不完整，不能补记完成。

提交后22s检查时处于preflight且stderr为空；这不是剖析成功或物理验证结论。下次同时检查73308和73385：原反馈完成则先备份对照；剖析完成则检查源身份、数值差异及耗时排名，定位根因后再设计受控性能实验，不在当前冻结run内热改代码。
