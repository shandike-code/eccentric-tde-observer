# CPU 并发实测与第 15 轮复核

2026-09-16 21:10 左右，Codex 经已认证 SSH 直接读取。科学 run 仍在运行；本报告不改变其配置。

## 性能结果与决定

作业 64408，anode16，8 CPU / 52G / 2h，qos_stu_cpu_long。环境记录确认 CPU affinity=8。同一个停止态种子，按 2、4、8 workers 顺序各做一次完整 76 块 map，没有物质候选变化。

| workers | map 墙钟 / s | 含初始化总墙钟 / s | 单 worker 峰值 RSS / MiB | 相对 2 workers 的 map 加速比 |
| --- | ---: | ---: | ---: | ---: |
| 2 | 698.682 | 757.618 | 3500.258 | 1.000 |
| 4 | 511.996 | 570.187 | 3500.801 | 1.365 |
| 8 | 2233.926 | 2292.251 | 3498.539 | 0.313 |

三组完整输出 SHA 全相同：`e0961399c903ec9c491909c74b7bec119f0965f71e44b73afaa450a5a102cf2f`。辐射残差也相同，为 `0.00019301557036992647`。这里的 RSS 是单进程峰值，不是整个作业的总内存占用。

Git 内保留原始小结果 `handoff/evidence/ustc-cpu-scaling-20260916.json`，SHA256 为 `d7ae59cedafc2af23787d2fd6631531db333df5cbf9bfc66a5f3797619601245`。它包含来源、三个完整输出哈希及运行环境，不含大态。

8-worker 测试期间节点记录 CPUAlloc=126/128、CPULoad=106.34、FreeMem=603674 MB。节点并不空闲，且顺序试验没有控制缓存、邻居负载和时间变化。**实测能支持“本次 4 workers 最快”，不能证明 8 workers 本质上更慢，也未证明慢因是 OOM、内存带宽或 CPU 竞争。** 用 `srun --overlap` 加一个只读探针未能在 20 秒内启动，已超时终止该探针 step；64408 主 batch 和科学作业随后均仍 RUNNING，未取消它们。

决定：下一个新科学 run 暂选 **4 workers / 8 CPU / 32G**，保留与此次试验相同的 8 CPU 亲和规模，并满足原 26G 最低内存门、留出余量；正式提交前再预检。当前 cont64 的 worker 数和声明保持不变。暂不升级至 8/16/32 workers，不为追求满核修改物理问题。

## 科学进展

截至读取时，cont64 已完成 map61，map62 已提交 36/76 块，第 15 轮（端点 59/60）完整落地：

| 指标 | 第 14 轮 | 第 15 轮 |
| --- | ---: | ---: |
| atomic heating 相邻变化 | 0.0034099503 | 0.0032688487 |
| 内层噪声 / 试步信号 | 0.01784380 | 0.01147812 |
| L2 residual candidate/base | 1.143266 | 1.133515 |
| 质量加权 residual candidate/base | 2.201306 | 2.191939 |
| 最差单元 residual candidate/base | 1.232466 | 1.092535 |
| previous/final 负热能层数 | 0 / 0 | 0 / 0 |

第 15 轮最小剩余气体热能仍在 cell64，previous/final 分别为该层旧气体热能的 2.034% / 2.247%。正物理域和噪声门通过；三种加热稳定性与三种残差收缩仍未通过。不要把加热变化指标解释成“低于信号”，它与噪声/试步信号是不同诊断。

继续已有 64 张/16 轮预算，不自动追加相同预算。最终需审阅加热门及真实物质残差后选择下一项有界研究。若走原方向回溯，必须固定基态、encoded direction 与旧物理时间层，并对新候选重新计算辐射及反馈。现有 HPC 迁移/适配路径固定在 0.0625；仅改配置里的步长不是完整实现，需独立声明的迁移路径，不能绕过旧 lineage 守卫。

## 监督证据补强

21:10 时 `scontrol show job 64408` 已返回 Invalid job id；因此本报告只确认三组计算均完整落盘并通过逐位比较，**不补造 Slurm COMPLETED/0:0 证据**。旧监督器只覆盖写最新快照，未永久保存调度器终态，这是记录缺口。

已补 `operations/claude_watch.py`：首次观察到调度器终态时单独保存，过期后注明引用保存记录；没有真实终态记录时保持未知，不能由 benchmark.json 的 complete 推造退出码。每次模型评阅还保存其原始 JSON 快照。停滞检查加入 previous/final 反馈块计数，不再只看 pending stage。

新增两项测试覆盖终态过期保留及禁止由工件推造调度器状态；本次相关 operations 测试 **10 passed**。不修改正在计算的 diagnostics、src、scripts 或 hpc。
