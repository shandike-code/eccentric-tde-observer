# 较长跨度外推审计通过：选择一个候选做真实映射与两轮反馈

77366于2026-09-25 20:19:18—20:45:25完成，26分7秒，COMPLETED 0:0，4CPU/16GiB/anode04，stderr无内容。
这是只读预测扫描：0新map、0反馈、0候选大态写入、0物质接受。接受步仍20。

## 独立复核与数值结果

小包`complete-1790340321567141965.tar.gz`为756731字节，
SHA256 `05727ff19ad504fa6f1a0b8a5edf8b36ebd0b55dab692a5fd6ffc5d84a168e69`。
Mac在`outputs/review-20260925/`保存包与receipt；原字节解包目录为`step21-wide-plane-77366-original-received`。
较早同名不含original的目录只是初次审计重序列化副本，不作原字节证据，原包始终未改。

审计工具`handoff/audit_tools/review_step21_wide_plane.py`核7文件、687代码哈希声明，
核77299的已审归档、16行连续map血缘、六态输入/映射对应、固定物质/残差/旧时间层来源声明。
独立fsum重聚合602段Gram/RHS、最新残差、每次扫描全段负值/局部变化/全局残差/边界积分；
核六hex见证确实复现负值、正规化不遗漏subnormal尾、累计约束与扫描预算、所有门和选择规则。
没有在Mac重算全场，也没有独立重写二维QP优化器；小矩阵重放不等于独立优化证明。
输出`handoff/evidence/20260925-wide-plane-review.json/csv/png`，图已目视，三个审计负路径测试通过。

| 输入基组 | 有效u | 有效v | 预测全局残差 | 与原map16比 | 预测边界L1 |
|---|---:|---:|---:|---:|---:|
| x6,x14,x15 | -80.37545614416956 | 0.99 | 3.2487565180437647e-6 | 0.7016517740493879 | 5.690838977011825e-6 |
| x7,x14,x15 | -79.12080484563582 | 0.99 | 3.2532305262956954e-6 | 0.7026180501644627 | 5.76740749397428e-6 |

两组各2遍；第一遍存在负值，新增18个点约束，第二遍q/p负值计数均0。全局门/边界门/成本门均通过。
Gram条件数分别8767.11、9084.52，小于1e10病态界；最高RSS319856640字节（约305MiB）。
不删失败首遍，不把第二遍非负误写成第一遍也有效，不把预测下降29.83%写成实际映射下降。
目前没有非有限量，弱尾正性完整保留；含负权外推不是凸平均，仍可能有真实算子误差。

## 选择和下一批

选预测比最低的`x6-x14-x15`，只检验一个，不重复跑另一个相近候选。
新驱动`operations/validate_step21_wide_plane.py/.sbatch`，协议`handoff/protocols/step21-wide-plane-validation-v1.md`。
在新目录`outputs/hpc/step21-wide-plane-validation-20260925`写一个大态候选，然后真实首张map。
全602片重建存储候选、实际T(q)对预测p，全局/局部/L2误差均留。
严格内层/边界/资源/预测误差门保持，另要求真实残差比也<0.8。首张失败不派发后续。
通过后再两张及pair03；原零控制七门全部有效才追加8张及pair11。最多11map/2pair，32CPU/128GiB/16worker，4小时硬上限。

pair03对77299 pair16比较是外推后响应位移；pair11对pair03才是8map窗口漂移。
所有比较保留完整向量三范数与固定原r20分母、相邻差及距原参考差，不接受21、不换r20、不推进物理dt。
7门通过不能预写第二窗口过门；窗口过也不是精确误差界。模型无解和整盘完成日期仍无依据。

## 代码与验证边界

模板复用77299已实测的修复构造器：finite模板合法构造后，写盘/执行前切换零control trial并关闭接受授权；
旧协议原字节保留。真实模板native预检在初始化前，正确trial复制也在初始化前。
信号复用已有派发/反馈停止路径、轮次目录与端点保留；不提供未经审计的原目录自动续跑。

Mac新增/复用58项测试通过（0.97秒），覆盖六态写入、八态实际比较、解析仿射算子、错误候选/负值拒绝、
真实构造器契约、先验审计选择、11map硬上限、首映射/首反馈/中断停止及已有恢复预算测试。
本轮备份Mac`outputs/review-20260925/pre-wide-plane-audit.bundle`、`automation-before-wide-plane-audit.toml`，
学校`/home/scc/pb24511938/pre-wide-plane-audit.bundle`。学校测试/实际预检/启动证据后续另记，不预填。
