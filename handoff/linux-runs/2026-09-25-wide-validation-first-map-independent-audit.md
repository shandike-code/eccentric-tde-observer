# 77371首张真实映射：29.83%的残差收益得到验证

## 已核实的结果

首张真实映射全局残差为3.2487565237894585e-6，除以77299最后map16的4.63015506865246e-6，
得到0.701651775290317，即实际下降29.83%。扫描预测是3.2487565180437647e-6。
这一步不再只是预测：原转移算子确实计算了新初猜的完整输出。
但它只证明固定物质态上的一次数值改进，没有接受第21物质步，更没有得到整盘大气表。

边界L1为5.690838976932627e-6，bolometric变化5.686579625300755e-6。
实际映射、严格内层、边界、资源、预测误差和20%真实收益共8门均通过。
map记录墙钟177.5656秒；这不包含此前全输入SHA、候选写入和初始化，不能当整作业耗时。
最大进程3587196KiB，低于6GiB门；76块覆盖全部9632频组，输入/输出最小强度非负，无非有限量。

## 全局小误差与弱尾未收敛必须一起报告

真实映射对仿射预测的最大误差/全局场尺度为1.2335284521858484e-13，
最大误差/实际最大变化为3.79692489465791e-8，均过已注册验证门。
最坏预测相对误差仍位于频率片[9488,9504)：

| 量 | 数值 |
|---|---:|
| 该片最大预测差 | 4.3542225778737e-311 |
| 该片场尺度 | 1.23200516068376e-307 |
| 预测差/该片场尺度 | 0.0003534256768418972（0.03534%） |
| 该片实际迭代残差 | 0.1627953139357116（16.28%） |
| 自然block74自身相对变化 | 0.1567607369993955（15.68%） |

两个分块方式不同，不混用16.28%和15.68%。该片场尺度仍大于float64最小正规数，
只有这里的预测差是subnormal；不能把整片场强度笼统叫作subnormal，更不能据此归因全部舍入问题。
没有删去弱尾，也没有证明弱尾主导物质响应漂移。图已目视检查，全局一致性不能推广为每点1e-13相对准确。

## 工件与独立性

包`true-map-validation-1790344071440319568.tar.gz`，1634045字节，
SHA256 `51feab45e8c5e18774ebe4fc99d2d39e19d6e727417b690fe0c951694fab5e28`。
Mac包与receipt在`outputs/review-20260925/`，原字节解包到`step21-wide-validation-77371-map1-received`。

新增入口`handoff/audit_tools/review_step21_wide_validation.py`：核164文件、688代码哈希声明、76进程回执；
核已审77366/77299源归档与清单、六态配对和候选SHA、trial/x20/r20/旧时间层、76块频率/资源及全局边界归约，
并从602片极值记录重新计算预测误差门。输出`handoff/evidence/20260925-wide-validation-map1-review.json/csv/png`。
Mac没有重读大dat、没有重积分物质ODE。驱动未保存逐片L2平方和，无法由极值还原全场L2；
工具只保留源记录、不声称独立重算，报告对应标志为false。没有新反馈属于本包，反馈计数为0。

新增7项审计测试和6项既有窗口/预算测试，共13 passed（0.99秒）。覆盖遗漏/重复片、伪误差、伪门、
非有限值与真实成本失败保留。后续反馈分支复用已实测的控制账本审计；本次只验证了首张真实工件路径，
不提前声称新pair03/pair11端到端已通过。若正式反馈失败，需单独审查失败门，不绕过审计里的成功前提。

## 正在继续的授权任务

21:50:36现场：77371 RUNNING，首张已完成，map2已提交48/76块，stderr为空、只读监督存活、
Claude首次审阅is_error=false。证据`handoff/evidence/20260925-wide-validation-map1-audit-snapshot.json`。
按原11map/2pair协议继续，没有扩预算、没有新提交。下一关键证据是pair03的实际H/He反馈，随后是8map窗口pair11。
原接受步仍20，原r20不换。4小时硬上限仍为2026-09-26 01:09:27。

本轮备份Mac`outputs/review-20260925/pre-wide-true-map-audit.bundle`及`automation-before-wide-true-map-audit.toml`；
学校`/home/scc/pb24511938/pre-wide-true-map-audit.bundle`。只增加handoff审计、测试、报告与讲义，没有修改活跃数值代码。
