# 74437停止：实际完成一张map，先分开审计科学门与资源门

终态watcher保存了Slurm FAILED、ExitCode1:0、墙钟18:03，10:39:45结束。新批次完成了四个局部方向重建及一张基态全步完整map（76块，162.206829秒），随后资源守卫停止。其余三张A map、全部B/C未执行，无反馈对。

父status中0map/0pair是阶段开始时的旧计数，异常捕获没有刷新它。实际完成map为1，子state.history和76份块报告一致；不能把父计数当实际用量。这是新编排的报告缺陷，未导致继续执行或额度重置。旧文件冻结不原位修补。

两个独立问题：

1. 新辐射R=1.8055062771452576e-4，原输入对应R=5.3201781558811255e-5；即使资源计量最终被解释，已测全步也不满足本批预定R<1e-4。最大绝对缺陷前四块为15、48、46、13，恰为校正块14/47的相邻块。这支持优先检查halo耦合，但单凭块号不能证明全部变化的原因。
2. 全76个worker所报峰值RSS逐位相同：19486.76171875MiB，超过原6GiB门；局部worker仍约3.1GiB。准备器曾触碰两个9.41GiB的全场memmap，怀疑Linux进程峰值记录继承或启动行为，需实测确认，不擅自把19GiB重标为3GiB或取消守卫。

已从学校自动归档保全819文件：`outputs/review-20260921/composite-74437-small.tar.gz`，20029577字节，SHA c3859be6c9e69848aa40392f78b9633903891cacf04cbe306cfc35464b54bfd8。整包及819项大小/SHA均核验，原dat不传输不更改。另保存终态JSON。改前备份pre-composite-failure-review.bundle和automation-before-composite-failure-review.toml。

下一项有界工作已写成一个组合审计：默认4CPU/16GiB、30分钟，只读四个完整场，逐块比较原/新缺陷、改动对邻块的影响和频组定位；同一allocation做512MiB合成内存计量实验，比对/proc自身VmHWM与ru_maxrss，以及直接子进程和轻量中继孙进程。使用分块fromfile，避免诊断器自身把四个完整mmap都留在RSS中。不增加全频map、不求反馈、不放宽门。

同时给出同一全局方向的有界二次L2最小化预测；它只利用已测两端缺陷，是下一步研究线索，不是新候选验证，不写候选、不自动续跑。本次审计将输出完整JSON和全部76块对比图，再决定是否有必要发展包含跨块耦合的新方向。原两块全步方案不继续进入反馈。
