# 74038扫描通过：只验证一张真实原算子map

## 扫描判决与保全

74038已`complete`，七项显式检查全部为true，无新map、无候选写入，stderr空。扫描代码`e6097bb`，作业记录`c806343`。队列为空；本轮没有重复提交扫描。

全局仿射系数为-0.5843914981899507，对两条半步历史的权重分别为0.41560850181004927和0.5843914981899507，权重绝对值和为1。非负可行区间为[-9.2321024826616,0]；选择值处于内部。预测最大归一残差3.107577054e-5，对照两历史较好的实测7.436605723e-5，约为其41.79%；预测边界L1为6.127993464e-5、bolometric变化3.344206522e-6。候选及预测输出的全场负值计数均为0，最小值为0，没有给零值加floor。

沿历史差的像范数比0.9866143380，仅描述这个方向上的两次真实映射，不是全局收缩常数或唯一性证明。全场未加权L2是扫描目标；另外要求最大归一残差和边界改善。当前只有代数预测，尚未验证新的真实map和物质反馈。

包`half-history-scan-74038-small.tar.gz`在学校家目录与Mac `outputs/review-20260920/`双端保存，19,818,884 bytes，637文件，SHA256 `6392a1b8530d9753c3440beb4a57a50d8031977cf179811434a6d1fa665488d9`。整包与全部逐文件SHA已核，Mac解包`half-history-scan-74038-received`。四个大态原样保留；大态SHA已由扫描在allocation内前后核验，新验证还会再核。

## 新验证契约

新入口`operations/validate_half_history_candidate.py/.sbatch`，新run `outputs/hpc/half-history-map-validation-20260920`。资源cpu_long32CPU/128GiB、16worker、45分钟上限，最多写一份候选、最多算一张真实原算子map，无自动续交、无反馈求值。16worker满足每worker6GiB加父进程2GiB守卫；不以32CPU名义启用32worker。

入口消费当前73929/73888半步物质与74038固定声明。先核两run与扫描快照一致、最新真实X/Y、完整trial全部字段、native物质/phase/dt、源文件SHA和扫描七门的数值；不调用旧验证器硬编码全步幅度与旧两map历史的main。候选写入使用扫描相同的浮点运算顺序。物质trial先复制校验，再初始化；初始化后再核全部字段、种子SHA、零map/空历史并再次核native，再执行1map。USR1转给pipeline，当前batch提交后停止派发；若中断只标incomplete，禁止直接重交拒绝已有目录的准备器。

验证比较完整实际输出与预测输出，要求误差L2低于实际映射残差L2的1%；另要求真实最大归一残差比两历史最佳值至少改善1%，边界L1与bolometric均小于1e-3，每worker RSS小于6144MiB，全场非负且有限、76频块完整。输入大态在验证前后核SHA；失败不改门、不把预测输出当真实解、不转正式反馈。

本地守卫测试覆盖缺失/失败门、伪通过数值、错系数/区间/输入态、继承旧历史、初始化换种子/物质；实际74038备份已通过新入口的声明、末次map选择和完整trial身份核验。提交前需学校端重跑同一测试集。
