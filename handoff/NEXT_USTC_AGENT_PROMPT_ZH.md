# 给学校 agent：先修诊断与恢复，再继续当前候选

## 最新入口：6b5349f 后的 Mac 修复

请先读 `handoff/MAC_REVIEW_6b5349f_ZH.md`。Mac 已校验新 bundle，复核 d8bcd50 和 6b5349f，并修复真实账本接口的 SHA 类型混用、map 提交后的反馈恢复窗口、末张接受被预算覆盖及来源核验等遗漏。当前 59 项测试及真实反馈文件的账本接口复核通过；Slurm 新版本信号/恢复实测仍待完成。

立即执行的顺序以该报告第 3 节为准：保全当前 6b5349f 小任务结果；在不影响活动作业的独立工作区同步整合；新命名小任务验证新版本；通过后在新科学 run 内执行最多 8 张、两轮反馈的既定预算。不要重新修改旧声明以绕过源码冻结，不要重复已经完成的旧修复，不再逐项请求批准。

下文保留先前基线和要求作为审计背景，其中“现在先不要追加”仅限制未经修复验证就启动完整科学批次；当前已授权的小验证和通过验证后的有界计算按最新报告执行。

---

Mac 已收到并验证完整 bundle，审阅基线为 `6423e4c363e0f5af97d39a07b6de30b70f4bdf6f`，原有 26 项测试在 Mac 通过。请先完整阅读 `handoff/MAC_REVIEW_6423e4c_ZH.md`：其中有已复现的归层、归档、恢复和状态控制缺陷，不能仅凭原测试通过继续无修改续交。

数值工件已补齐并在 Mac 独立回放，新增证据见 `handoff/MAC_FEEDBACK_REPLAY_2026-09-15_ZH.md`。六端点的负热能报错和五类正式反馈指标复现；正确归层后 cell96 占加热变化约 1.57%，最新失败层合计约 8.69%，原“空间不重合”结论需撤回。可复用 `diagnostics/mac_feedback_artifact_audit.py` 及其 4 项测试。此项回放不代表旧 driver 的运行控制缺陷已经修复。

现在先不要追加辐射作业。若此前授权的批次已经在运行，停止新的自动续交，不覆盖运行中的代码，也不直接杀掉有效计算；让当前作业到可恢复边界，保存状态。以下修复、必要测试、旧反馈回放及限定预算已获授权，不用逐项再问。

## 1. 安全同步与版本保全

- 可先 `git fetch origin` 读取 Mac 审阅分支；fetch 不等于更新运行工作区。
- 若本地仍在 6423e4c 且没有活动作业，可确认快进关系后更新；若有后续学校提交，在独立 worktree 整合，保留所有提交，不 force、不 reset，不对冻结运行目录 rebase。
- 改动保持在新诊断模块和测试/文档中；不修改历史 src/scripts/hpc 数值核、旧协议和旧结果。用新命名修订版运行声明固定诊断文件 SHA、Git 版本、反馈间隔、并发和预算。

## 2. 优先修正科学诊断

- 4096 辐射子胞映射到 256 parent 层，每层 16 子胞；half_* 是前 128 parent。用原适配器实际映射逐值验证。
- 加热变化分子已含宽度权重：先按 (256,16) 求和，再分别报告两半柱；需要折回物质 128 层时用前半加逆序后半，说明归一化分母是半柱还是全柱。
- 加测试：已知 parent96 的单层扰动必须定位到 96，不能变成 48；镜像扰动也映射正确。
- 撤回旧报告中“cell96 贡献 0.109%”“两种失败空间不重合”及其错误层号，保留旧文件并另写勘误和重新计算值。
- 修正 old/new 布居字段、绝对/相对最差层混用、多分量率的分子分母配对。保留每分量原指标；全零/未定义值明确表示，拒绝非有限 JSON，不加 floor。
- 保留原物质能量账本，直接与原响应在相同物理输入下对照；不要因为展示错误就重算全部辐射态。

## 3. 修复后再允许续交的运行条件

- 每轮从创建时就拥有独立 protocol、反馈工作区、输出与清单。归档不能 os.replace 覆盖不同内容，不能 rmtree 已有证据；不能把所有根目录残留都自动视为上一轮。
- 对历史移动过的工件建立新位置到旧路径/SHA 的迁移索引，不改写旧协议伪造验证。验证所有可保留的小工件引用；大态槽位已被复用时如实标明，不伪称仍能重跑旧端点。
- 账本子进程非零、缺失 JSON、来源哈希不符均必须保留“诊断未完成”，不得登记完整轮次。使用参数列表和 check=True 或等价处理，支持只恢复账本而不重做已完成反馈。
- 持久化 pending feedback 及两个端点的 SHA；在新 map 前先恢复该反馈，不能覆盖端点。每张 map 后检查 cadence，不能因 maps-per-job 大于 1 跳过 map4。
- 将停止信号传到实际 pipeline.batches，验证收到信号后当前块 batch 提交完成但不会启动下一批 worker。
- 所有物理验收通过后停止为 one_material_trial_accepted；资源故障保持故障终态，不得被预算完成覆盖。map 预算到点后仍允许恢复最后一对反馈，但不允许新增 map。
- 故障、接受、待恢复反馈、正常运行、预算结束的优先级显式定义并测试。

新增回归必须覆盖：同轮重试、归档后路径解析、不同内容拒绝覆盖、账本失败、map4 后反馈中断恢复、跨 cadence 的多 map 作业、USR1 后不再派发、已接受停止、资源失败不被覆盖、诊断源码/配置篡改拒绝。先模拟注入验证；已有账本和传输测试保持通过。实际 Slurm 信号恢复验证在授权 allocation 内用小任务完成，不先启动完整新增 8 张。

## 4. 回放现有小工件

在新诊断目录，用 warm 对照及两轮各自的 previous/final_feedback.npz 回放，不改变原文件。

交付同端点表：物理相位、dt、密度、真实 old/new 布居、旧气体热能、Δ电离能、dt*Q/rho、剩余热能、绝对与相对最差层、失败质量占比。另固定跟踪 cell96，并给出正确归层后的加热贡献与失败层的重合程度。

记录变化前后哪些指标不变、哪些因诊断修正而改变。不能把“有限且下降”当成严格收敛，也不能把相邻端点差当作剩余误差界。

为 Mac 复核导出小工件包：三组反馈对 NPZ、各自协议/清单/摘要、原与修订账本、trial_material、运行 config/state、源码与输入 SHA 清单。物理旧时间层如果已有相同哈希副本可只附来源，否则补必要输入。不要包含 dat、凭据、环境目录或所有逐块大文件。给出包大小和 SHA，放在学校家目录供下载；现有 Git bundle 只有代码和 Markdown，不能代替这些数值工件。

这批原始小工件已经交付并通过 Mac 校验，不用重复上传。后续只补新修订诊断或新反馈轮次，并保持新旧版本分别可追溯。

## 5. 有条件推进下一组计算

上述修复、回放和恢复验证通过，且没有发现原反馈输入或原物理记账错误后，直接在新命名 run 中从最新可信 Linux 态起跑，固定 0.0625 候选。

预算：最多新增 8 张完整 map，每新增 4 张对最后两个相邻输入端点做一次正式反馈与账本，共最多两轮；初始化入口如已做 map 也计入新增预算。沿用已核实的 2 worker、Slurm allocation 与资源门，不自动提高额度。

目标：尝试让两个响应都进入正热能域，从而获得原 encoded residual、内层噪声/信号比和三种收缩指标。不是预设八张能过。

- 所有门通过：停止，保存一个被接受的非线性物质步，不宣称完成耦合柱。
- 两响应成功但加热或噪声门失败：输出全部可求指标，保留不接受判定；再评估内层精度成本。
- 稳定反馈与噪声控制通过后收缩失败：评估原基态/原 encoded direction 的有界回溯。
- 稳定反馈仍负热能：基于修正版账本评估回溯或响应闭合。
- 预算到点仍不明确：停止自动续交，报告未解决问题，不扩预算、不改阈值。

任何阶段不改变物理 dt、不裁剪布居、不添加温度 floor、不更改能量定义。所有状态和异常如实记录。提交新代码、测试和勘误报告到诊断分支；无法推送时重新导出 bundle。仅在进度有实质变化、完成或出现需处理的异常时通知，不对空队列持续巡检。
